---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: managedtools.adhar.example.com
spec:
  group: adhar.example.com
  names:
    kind: ManagedTool
    listKind: ManagedToolList
    plural: managedtools
    singular: managedtool
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.environment
      name: Environment
      type: string
    - jsonPath: .spec.chart.name
      name: Chart
      type: string
    - jsonPath: .spec.chart.version
      name: Version
      type: string
    - jsonPath: .spec.targetNamespace
      name: TargetNamespace
      type: string
    - jsonPath: .status.helmReleaseStatus
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ManagedTool is the Schema for the managedtools API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ManagedToolSpec defines the desired state of ManagedTool
            properties:
              chart:
                description: Chart specifies the Helm chart to install for this tool.
                properties:
                  name:
                    description: Name is the name of the Helm chart.
                    type: string
                  repository:
                    description: Repository is the URL of the Helm repository.
                    type: string
                  version:
                    description: Version is the version of the Helm chart.
                    type: string
                required:
                - name
                - repository
                - version
                type: object
              createNamespace:
                description: |-
                  CreateNamespace indicates whether the TargetNamespace should be created if it doesn't exist.
                  Defaults to false.
                type: boolean
              environment:
                description: |-
                  Environment specifies the target Adhar Environment where this tool should be managed.
                  The controller uses this to determine the target cluster.
                minLength: 1
                type: string
              syncPolicy:
                description: |-
                  SyncPolicy defines how the tool's Helm release should be managed (e.g., automated updates).
                  Placeholder for future enhancement, could mirror Argo CD sync policy.
                properties:
                  automated:
                    description: |-
                      Automated controls whether the Helm release should be automatically upgraded
                      when the ManagedTool spec changes. Defaults to false.
                    type: boolean
                type: object
              targetNamespace:
                description: |-
                  TargetNamespace specifies the namespace where the tool should be installed.
                  If omitted, it might default based on the chart or a controller default (e.g., tool name).
                type: string
              values:
                description: Values are Helm values to customize the tool installation.
                x-kubernetes-preserve-unknown-fields: true
            required:
            - chart
            - environment
            type: object
          status:
            description: ManagedToolStatus defines the observed state of ManagedTool
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the tool's installation state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deployedVersion:
                description: DeployedVersion is the version of the Helm chart currently
                  deployed.
                type: string
              helmReleaseStatus:
                description: |-
                  HelmReleaseStatus reflects the status reported by Helm for the release.
                  Examples: "deployed", "failed", "pending-install", "uninstalled".
                type: string
              notes:
                description: Notes contains any output notes provided by the Helm
                  chart upon installation/upgrade.
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the ManagedTool
                  Spec that was last processed.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
