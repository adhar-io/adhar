---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: environments.adhar.example.com
spec:
  group: adhar.example.com
  names:
    kind: Environment
    listKind: EnvironmentList
    plural: environments
    singular: environment
  scope: Cluster // Environments are cluster-scoped in the management cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.region
      name: Region
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Environment is the Schema for the environments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EnvironmentSpec defines the desired state of Environment
            properties:
              addons:
                description: Addons lists additional tools or services to be installed
                  in the environment.
                items:
                  description: AddonSpec defines the configuration for an optional
                    addon.
                  properties:
                    chart:
                      description: Chart specifies the Helm chart to install for this
                        addon.
                      properties:
                        name:
                          description: Name is the name of the Helm chart.
                          type: string
                        repository:
                          description: Repository is the URL of the Helm repository.
                          type: string
                        version:
                          description: Version is the version of the Helm chart.
                          type: string
                      required:
                      - name
                      - repository
                      - version
                      type: object
                    createNamespace:
                      description: |-
                        CreateNamespace indicates whether the TargetNamespace should be created if it doesn't exist.
                        Defaults to false.
                      type: boolean
                    name:
                      description: Name is a unique identifier for the addon within
                        the environment.
                      minLength: 1
                      type: string
                    targetNamespace:
                      description: TargetNamespace specifies the namespace where the
                        addon should be installed.
                      type: string
                    values:
                      description: Values are Helm values to customize the addon installation.
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - chart
                  - name
                  type: object
                type: array
              clusterConfig:
                description: |-
                  ClusterConfig holds provider-specific configuration for the Kubernetes cluster.
                  The structure depends on the specified provider.
                x-kubernetes-preserve-unknown-fields: true
              coreServices:
                description: CoreServices defines the configuration for essential
                  platform services like CNI, Ingress, GitOps.
                properties:
                  argocd:
                    description: Configuration for Argo CD GitOps controller.
                    properties:
                      chart:
                        description: Chart specifies the Helm chart details.
                        properties:
                          name:
                            description: Name is the name of the Helm chart.
                            type: string
                          repository:
                            description: Repository is the URL of the Helm repository.
                            type: string
                          version:
                            description: Version is the version of the Helm chart.
                            type: string
                        required:
                        - name
                        - repository
                        - version
                        type: object
                      values:
                        description: Values are Helm values to customize the installation.
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - chart
                    type: object
                  cilium:
                    description: Configuration for Cilium CNI.
                    properties:
                      chart:
                        description: Chart specifies the Helm chart details.
                        properties:
                          name:
                            description: Name is the name of the Helm chart.
                            type: string
                          repository:
                            description: Repository is the URL of the Helm repository.
                            type: string
                          version:
                            description: Version is the version of the Helm chart.
                            type: string
                        required:
                        - name
                        - repository
                        - version
                        type: object
                      values:
                        description: Values are Helm values to customize the installation.
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - chart
                    type: object
                  gitProvider:
                    description: GitProvider specifies the Git provider to use for
                      the Adhar platform.
                    type: string
                  gitea:
                    description: Configuration for Gitea Git server.
                    properties:
                      chart:
                        description: Chart specifies the Helm chart details.
                        properties:
                          name:
                            description: Name is the name of the Helm chart.
                            type: string
                          repository:
                            description: Repository is the URL of the Helm repository.
                            type: string
                          version:
                            description: Version is the version of the Helm chart.
                            type: string
                        required:
                        - name
                        - repository
                        - version
                        type: object
                      values:
                        description: Values are Helm values to customize the installation.
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - chart
                    type: object
                  nginx:
                    description: Configuration for Nginx Ingress Controller.
                    properties:
                      chart:
                        description: Chart specifies the Helm chart details.
                        properties:
                          name:
                            description: Name is the name of the Helm chart.
                            type: string
                          repository:
                            description: Repository is the URL of the Helm repository.
                            type: string
                          version:
                            description: Version is the version of the Helm chart.
                            type: string
                        required:
                        - name
                        - repository
                        - version
                        type: object
                      values:
                        description: Values are Helm values to customize the installation.
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - chart
                    type: object
                  values:
                    description: Values are common Helm values merged into each core
                      service chart defined above.
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              provider:
                description: Provider specifies the infrastructure provider for this
                  environment.
                enum:
                - kind
                - do
                - gke
                - aws
                - azure
                - civo
                type: string
              region:
                description: |-
                  Region specifies the geographic region for cloud providers.
                  Required for cloud providers, ignored for kind.
                type: string
              type:
                description: Type categorizes the environment, influencing default
                  configurations (e.g., nonprod, prod).
                minLength: 1
                type: string
            required:
            - provider
            - type
            type: object
          status:
            description: EnvironmentStatus defines the observed state of Environment
            properties:
              addonStatus:
                additionalProperties:
                  type: string
                description: |-
                  AddonStatus tracks the installation status of addons.
                  Key is the addon name (from spec.addons.name), Value is the status (e.g., "Installed", "Failed").
                type: object
              clusterEndpoint:
                description: ClusterEndpoint is the API server endpoint of the provisioned
                  Kubernetes cluster.
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              coreServiceStatus:
                additionalProperties:
                  type: string
                description: |-
                  CoreServiceStatus tracks the installation status of core services.
                  Key is the service name (e.g., "argocd"), Value is the status (e.g., "Ready", "Installing", "Failed").
                type: object
              kubeconfigSecretRef:
                description: |-
                  KubeconfigSecretRef is the name of the secret containing the kubeconfig for the provisioned cluster.
                  Typically populated for cloud providers via Crossplane Claim status.
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the Environment
                  Spec that was last processed.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
