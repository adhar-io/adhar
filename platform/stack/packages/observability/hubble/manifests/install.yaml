# Hubble relay + UI are installed by the bootstrap Cilium install (single render →
# single Cilium CA so relay↔agent mTLS verifies). This GitOps package only adds
# the HTTPRoute (manifests/httproute.yaml) exposing hubble-ui through the gateway.
