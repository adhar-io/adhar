# Keycloak SSO in front of Tempo (same pattern as Nexus/Tekton/Harbor): the
# UI is gated by an oauth2-proxy using the `tempo` Keycloak client, which is
# provisioned generically from the labelled ConfigMap below by the keycloak
# package (config Job + 5-minute client-reconcile CronJob) and whose secret
# arrives via ESO from keycloak-clients (TEMPO_CLIENT_SECRET).
# The oidc-issuer-url auto-discovers Keycloak's endpoints via
# /.well-known/openid-configuration. The HTTPRoute (sync-wave 6) points at
# tempo-oauth2-proxy:4180; the proxy forwards to http://tempo.adhar-system.svc.cluster.local:4318.
# Machine-facing (SSO_BEARER=1): besides the browser login, requests carrying a
# Keycloak-issued bearer access token for the `tempo` client (client_credentials
# with TEMPO_CLIENT_SECRET from keycloak-clients) pass straight through.
#
# GENERATED by hack/gen-sso.sh — regenerate rather than hand-edit:
#   SSO_BEARER=1 hack/gen-sso.sh platform/stack/packages/observability/tempo tempo tempo 4318
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: tempo-keycloak-client
  namespace: adhar-system
  labels:
    # Picked up by the keycloak package's client reconciler (see
    # security/keycloak/manifests/keycloak-config.yaml, configure.sh).
    adhar.io/keycloak-client: "true"
    app.kubernetes.io/name: tempo-keycloak-client
    app.kubernetes.io/part-of: tempo
  # Wave 0: a ConfigMap is healthy instantly and blocks nothing; the earlier
  # it exists, the sooner the reconciler exports the client secret the
  # wave-5 proxy waits on.
  annotations: {argocd.argoproj.io/sync-wave: "0"}
data:
  # Keep "description" under 255 chars (Keycloak varchar; longer fails with 500).
  client.json: |
    {
      "protocol": "openid-connect",
      "clientId": "tempo",
      "name": "Tempo Client",
      "description": "Used for Tempo SSO via oauth2-proxy",
      "publicClient": false,
      "authorizationServicesEnabled": false,
      "serviceAccountsEnabled": true,
      "implicitFlowEnabled": false,
      "directAccessGrantsEnabled": true,
      "standardFlowEnabled": true,
      "frontchannelLogout": true,
      "redirectUris": ["https://tempo.adhar.localtest.me:8443/oauth2/callback"],
      "webOrigins": ["https://tempo.adhar.localtest.me:8443"],
      "protocolMappers": [
        {
          "name": "self-audience",
          "protocol": "openid-connect",
          "protocolMapper": "oidc-audience-mapper",
          "config": {
            "included.custom.audience": "tempo",
            "id.token.claim": "false",
            "access.token.claim": "true"
          }
        }
      ]
    }
---
apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: tempo-oauth2-cookie
  namespace: adhar-system
  # Wave 5: the SSO front-end deploys AFTER the app core (wave 0-3). Otherwise
  # this oauth2-proxy — which waits on a Keycloak-provisioned ExternalSecret —
  # sits unhealthy at wave 0 and blocks the core from ever deploying.
  annotations: {argocd.argoproj.io/sync-wave: "5"}
spec:
  length: 32
  digits: 8
  symbols: 0
  noUpper: false
  allowRepeat: true
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tempo-oauth2-proxy
  namespace: adhar-system
  annotations: {argocd.argoproj.io/sync-wave: "5"}
spec:
  refreshPolicy: CreatedOnce
  refreshInterval: 5m
  secretStoreRef:
    name: keycloak
    kind: ClusterSecretStore
  target:
    name: tempo-oauth2-proxy
    template:
      engineVersion: v2
      data:
        OAUTH2_PROXY_CLIENT_SECRET: "{{ .TEMPO_CLIENT_SECRET }}"
        OAUTH2_PROXY_COOKIE_SECRET: "{{ .COOKIE_SECRET }}"
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: tempo-oauth2-cookie
      rewrite:
        - transform:
            template: "COOKIE_SECRET"
  data:
    - secretKey: TEMPO_CLIENT_SECRET
      remoteRef:
        key: keycloak-clients
        property: TEMPO_CLIENT_SECRET
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tempo-oauth2-proxy
  namespace: adhar-system
  annotations: {argocd.argoproj.io/sync-wave: "5"}
  labels:
    app.kubernetes.io/name: tempo-oauth2-proxy
    app.kubernetes.io/part-of: tempo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tempo-oauth2-proxy
  template:
    metadata:
      labels:
        app: tempo-oauth2-proxy
    spec:
      enableServiceLinks: false
      containers:
        - name: oauth2-proxy
          image: quay.io/oauth2-proxy/oauth2-proxy:v7.7.1
          args:
            - --http-address=0.0.0.0:4180
            - --provider=oidc
            - --oidc-issuer-url=https://keycloak.adhar.localtest.me:8443/realms/adhar
            - --ssl-insecure-skip-verify=true
            - --client-id=tempo
            - --redirect-url=https://tempo.adhar.localtest.me:8443/oauth2/callback
            - --upstream=http://tempo.adhar-system.svc.cluster.local:4318
            - --email-domain=*
            - --scope=openid profile email groups
            - --cookie-secure=true
            - --skip-provider-button=true
            - --reverse-proxy=true
            # Forward the authenticated identity (X-Forwarded-User/Email/
            # Preferred-Username) so apps with header/proxy auth can use it.
            - --pass-user-headers=true
            # Machine clients (workload-cluster spokes) authenticate with a
            # Keycloak bearer access token instead of the browser login flow:
            #   POST https://keycloak.<host>/realms/adhar/protocol/openid-connect/token
            #        grant_type=client_credentials client_id=tempo client_secret=<TEMPO_CLIENT_SECRET>
            - --skip-jwt-bearer-tokens=true
          envFrom:
            - secretRef:
                name: tempo-oauth2-proxy
          ports:
            - name: http
              containerPort: 4180
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
          readinessProbe:
            httpGet:
              path: /ping
              port: 4180
            initialDelaySeconds: 5
            timeoutSeconds: 10
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: tempo-oauth2-proxy
  namespace: adhar-system
  annotations: {argocd.argoproj.io/sync-wave: "5"}
  labels:
    app.kubernetes.io/name: tempo-oauth2-proxy
    app.kubernetes.io/part-of: tempo
spec:
  selector:
    app: tempo-oauth2-proxy
  ports:
    - name: http
      port: 4180
      targetPort: 4180
