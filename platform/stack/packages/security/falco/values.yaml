# Falco Helm Chart Values
# Runtime security via the modern eBPF probe (no kernel module / driver build).

driver:
  enabled: true
  kind: modern_ebpf

# Falcosidekick forwards events; UI disabled (no ingress, Gateway-API only platform).
falcosidekick:
  enabled: true
  webui:
    enabled: false
