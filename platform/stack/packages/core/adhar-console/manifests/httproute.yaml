# Backstage (adhar-console) routed through the Cilium Gateway (adhar-gateway).
# TLS terminates at the Gateway (adhar-cert), so the old nginx ssl-redirect
# annotation is no longer needed. Backstage on its own host console.adhar.localtest.me, served at root.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: backstage
  namespace: backstage
spec:
  parentRefs:
    - name: adhar-gateway
      namespace: adhar-system
  hostnames:
    - console.adhar.localtest.me
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: backstage
          port: 7007
