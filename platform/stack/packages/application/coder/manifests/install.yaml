# CODER INSTALL RESOURCES
# This file is auto-generated with 'platform/stack/packages/application/coder/generate-manifests.sh'
---
# Source: coder/templates/coder.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations: {}
  labels:
    app.kubernetes.io/instance: coder
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: coder
    app.kubernetes.io/part-of: coder
    app.kubernetes.io/version: 2.37.0
    helm.sh/chart: coder-2.37.0
  name: coder
  namespace: adhar-system
---
# Source: coder/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: coder-workspace-perms
  namespace: adhar-system
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs:
    - create
    - delete
    - deletecollection
    - get
    - list
    - patch
    - update
    - watch
  - apiGroups: [""]
    resources: ["persistentvolumeclaims"]
    verbs:
    - create
    - delete
    - deletecollection
    - get
    - list
    - patch
    - update
    - watch
  - apiGroups:
    - apps
    resources:
    - deployments
    verbs:
    - create
    - delete
    - deletecollection
    - get
    - list
    - patch
    - update
    - watch
---
# Source: coder/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: "coder"
  namespace: adhar-system
subjects:
  - kind: ServiceAccount
    name: "coder"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: coder-workspace-perms
---
# Source: coder/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: coder
  namespace: adhar-system
  labels:
    helm.sh/chart: coder-2.37.0
    app.kubernetes.io/name: coder
    app.kubernetes.io/instance: coder
    app.kubernetes.io/part-of: coder
    app.kubernetes.io/version: "2.37.0"
    app.kubernetes.io/managed-by: Helm
  annotations:
    {}
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
    - name: "http"
      port: 80
      targetPort: "http"
      protocol: TCP
  selector:
    app.kubernetes.io/name: coder
    app.kubernetes.io/instance: coder
---
# Source: coder/templates/coder.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations: {}
  labels:
    app.kubernetes.io/instance: coder
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: coder
    app.kubernetes.io/part-of: coder
    app.kubernetes.io/version: 2.37.0
    helm.sh/chart: coder-2.37.0
  name: coder
  namespace: adhar-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: coder
      app.kubernetes.io/name: coder
  template:
    metadata:
      annotations:
        app.kubernetes.io/component: coderd
      labels:
        app.kubernetes.io/instance: coder
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/name: coder
        app.kubernetes.io/part-of: coder
        app.kubernetes.io/version: 2.37.0
        helm.sh/chart: coder-2.37.0
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app.kubernetes.io/instance
                  operator: In
                  values:
                  - coder
              topologyKey: kubernetes.io/hostname
            weight: 1
      containers:
      - args:
        - server
        command:
        - /opt/coder
        env:
        - name: CODER_HTTP_ADDRESS
          value: 0.0.0.0:8080
        - name: CODER_PROMETHEUS_ADDRESS
          value: 0.0.0.0:2112
        - name: CODER_PPROF_ADDRESS
          value: 0.0.0.0:6060
        - name: KUBE_POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: CODER_DERP_SERVER_RELAY_URL
          value: http://$(KUBE_POD_IP):8080
        - name: CODER_CLUSTER_HOST
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: CODER_ACCESS_URL
          value: https://coder.adhar.localtest.me:8443
        - name: CODER_OIDC_ISSUER_URL
          value: https://keycloak.adhar.localtest.me:8443/realms/adhar
        - name: CODER_OIDC_CLIENT_ID
          value: coder
        - name: CODER_OIDC_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              key: clientSecret
              name: coder-oidc
              optional: true
        - name: CODER_OIDC_SIGN_IN_TEXT
          value: Sign in with Keycloak
        - name: CODER_OIDC_ALLOW_SIGNUPS
          value: "true"
        - name: CODER_OIDC_SCOPES
          value: openid,profile,email,groups
        - name: CODER_OIDC_GROUP_FIELD
          value: groups
        - name: CODER_OIDC_USER_ROLE_FIELD
          value: groups
        - name: CODER_OIDC_USER_ROLE_MAPPING
          value: '{"platform-admin":["owner"],"admin":["owner"]}'
        - name: CODER_DISABLE_PASSWORD_AUTH
          value: "false"
        # The platform CNPG database (manifests/platform-postgres.yaml). WITHOUT
        # this, `coder server` uses its built-in PostgreSQL inside the container
        # filesystem and loses every user, template and workspace on any pod
        # replacement — see that file's header for the live failure this caused.
        #
        # Composed from the parts rather than read from a `<cluster>-app` Secret:
        # CNPG only generates that Secret when it invents the credentials itself.
        # This Cluster supplies its own (bootstrap.initdb.secret: coder-db-credentials),
        # so no `coder-db-app` exists and referencing it left the pod in
        # CreateContainerConfigError — `secret "coder-db-app" not found` — for 148
        # minutes. The host/user/password shape below is what data/metabase and the
        # other CNPG consumers already use.
        #
        # Kubernetes expands $(VAR) from env vars declared EARLIER in this same
        # container, which is what lets a single connection URL be assembled without
        # putting the password in the manifest.
        - name: CODER_PG_USER
          valueFrom:
            secretKeyRef:
              name: coder-db-credentials
              key: username
        - name: CODER_PG_PASSWORD
          valueFrom:
            secretKeyRef:
              name: coder-db-credentials
              key: password
        - name: CODER_PG_CONNECTION_URL
          value: "postgres://$(CODER_PG_USER):$(CODER_PG_PASSWORD)@coder-db-rw.adhar-system.svc.cluster.local:5432/coder?sslmode=require"
        - name: SSL_CERT_DIR
          value: /etc/ssl/certs:/etc/adhar-ca
        image: ghcr.io/coder/coder:v2.37.0
        imagePullPolicy: IfNotPresent
        lifecycle: {}
        name: coder
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /healthz
            port: http
            scheme: HTTP
          initialDelaySeconds: 0
        resources:
          limits:
            cpu: 2000m
            memory: 4096Mi
          requests:
            cpu: 2000m
            memory: 4096Mi
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: null
          runAsGroup: 1000
          runAsNonRoot: true
          runAsUser: 1000
          seccompProfile:
            type: RuntimeDefault
        volumeMounts:
        - mountPath: /etc/adhar-ca
          name: adhar-ca
          readOnly: true
      restartPolicy: Always
      serviceAccountName: coder
      terminationGracePeriodSeconds: 60
      volumes:
      - name: adhar-ca
        secret:
          items:
          - key: tls.crt
            path: adhar-gateway.crt
          secretName: adhar-cert
