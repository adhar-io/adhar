apiVersion: v1
kind: Namespace
metadata:
  name: ${{values.name | dump}}
  labels:
    entity-id: ${{values.name}}
    app.kubernetes.io/name: ${{values.name}}
    adhar.io/owner: ${{values.owner}}
    # ADR-0023 placement: application workloads run on the data plane.
    adhar.io/plane: data
