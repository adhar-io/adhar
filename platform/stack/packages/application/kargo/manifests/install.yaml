apiVersion: v1
kind: ServiceAccount
metadata:
  name: kargo-api
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: api
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kargo-controller
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kargo-external-webhooks-server
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: external-webhooks-server
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kargo-garbage-collector
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: garbage-collector
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kargo-webhooks-server
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: kubernetes-webhooks-server
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kargo-management-controller
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: management-controller
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kargo-admin
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    rbac.kargo.akuity.io/system-role: 'true'
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kargo-project-creator
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    rbac.kargo.akuity.io/system-role: 'true'
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kargo-user
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    rbac.kargo.akuity.io/system-role: 'true'
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kargo-viewer
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    rbac.kargo.akuity.io/system-role: 'true'
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kargo-api
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: api
data:
  KARGO_NAMESPACE: kargo
  LOG_LEVEL: INFO
  LOG_FORMAT: CONSOLE
  TLS_ENABLED: 'true'
  TLS_CERT_PATH: /etc/kargo/tls.crt
  TLS_KEY_PATH: /etc/kargo/tls.key
  SECRET_MANAGEMENT_ENABLED: 'true'
  SYSTEM_RESOURCES_NAMESPACE: kargo-system-resources
  SHARED_RESOURCES_NAMESPACE: kargo-shared-resources
  PERMISSIVE_CORS_POLICY_ENABLED: 'false'
  ADMIN_ACCOUNT_ENABLED: 'true'
  ADMIN_ACCOUNT_TOKEN_ISSUER: https://adhar.localtest.me
  ADMIN_ACCOUNT_TOKEN_AUDIENCE: adhar.localtest.me
  ADMIN_ACCOUNT_TOKEN_TTL: 24h
  ROLLOUTS_INTEGRATION_ENABLED: 'true'
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kargo-controller
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
data:
  API_SERVER_BASE_URL: https://adhar.localtest.me
  LOG_LEVEL: INFO
  LOG_FORMAT: CONSOLE
  KARGO_NAMESPACE: kargo
  SYSTEM_RESOURCES_NAMESPACE: kargo-system-resources
  IS_DEFAULT_CONTROLLER: 'true'
  SHARED_RESOURCES_NAMESPACE: kargo-shared-resources
  ALLOW_CREDENTIALS_OVER_HTTP: 'false'
  GITCLIENT_NAME: Kargo
  GITCLIENT_EMAIL: no-reply@kargo.io
  GIT_PUSH_INTEGRATION_POLICY: AlwaysRebase
  GITCLIENT_SIGNING_KEY_TYPE: gpg
  GITHUB_PUSH_MAX_REVISIONS: '10'
  GITHUB_PUSH_VERIFY_UNTRUSTED_COMMITS: 'false'
  CACHE_BY_TAG_POLICY: Allow
  MAX_IMAGE_CACHE_ENTRIES: '100000'
  IMAGE_REGISTRY_RATE_LIMIT: '20'
  MAX_OCI_PUSH_ARTIFACT_SIZE: '1073741824'
  ARGOCD_INTEGRATION_ENABLED: 'true'
  ARGOCD_NAMESPACE: argocd
  ARGOCD_WATCH_ARGOCD_NAMESPACE_ONLY: 'false'
  ROLLOUTS_INTEGRATION_ENABLED: 'true'
  ROLLOUTS_CONTROLLER_INSTANCE_ID: ''
  MAX_CONCURRENT_CONTROL_FLOW_RECONCILES: '4'
  MAX_CONCURRENT_PROMOTION_RECONCILES: '4'
  MAX_CONCURRENT_STAGE_RECONCILES: '4'
  MAX_CONCURRENT_WAREHOUSE_RECONCILES: '4'
  MIN_WAREHOUSE_RECONCILIATION_INTERVAL: 5m0s
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kargo-external-webhooks-server
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: external-webhooks-server
data:
  EXTERNAL_WEBHOOK_SERVER_BASE_URL: https://localhost
  KARGO_NAMESPACE: kargo
  SYSTEM_RESOURCES_NAMESPACE: kargo-system-resources
  LOG_LEVEL: INFO
  LOG_FORMAT: CONSOLE
  TLS_ENABLED: 'true'
  TLS_CERT_PATH: /etc/kargo/tls.crt
  TLS_KEY_PATH: /etc/kargo/tls.key
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kargo-garbage-collector
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: garbage-collector
data:
  LOG_LEVEL: INFO
  LOG_FORMAT: CONSOLE
  NUM_WORKERS: '3'
  MAX_RETAINED_PROMOTIONS: '20'
  MIN_PROMOTION_DELETION_AGE: 336h
  MAX_RETAINED_FREIGHT: '20'
  MIN_FREIGHT_DELETION_AGE: 336h
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kargo-webhooks-server
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: kubernetes-webhooks-server
data:
  KARGO_NAMESPACE: kargo
  LOG_LEVEL: INFO
  LOG_FORMAT: CONSOLE
  CONTROLPLANE_USER_REGEX: ^system:serviceaccount:kargo:(kargo-api|kargo-controller|kargo-garbage-collector|kargo-management-controller)$
  MANAGEMENT_CONTROLLER_USERNAME: system:serviceaccount:kargo:kargo-management-controller
  CACHE_BY_TAG_POLICY: Allow
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kargo-management-controller
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: management-controller
data:
  EXTERNAL_WEBHOOK_SERVER_BASE_URL: https://localhost
  KARGO_NAMESPACE: kargo
  CLUSTER_SECRETS_NAMESPACE: kargo-cluster-secrets
  SYSTEM_RESOURCES_NAMESPACE: kargo-system-resources
  SHARED_RESOURCES_NAMESPACE: kargo-shared-resources
  LOG_LEVEL: INFO
  LOG_FORMAT: CONSOLE
  MAX_CONCURRENT_NAMESPACE_RECONCILES: '4'
  MAX_CONCURRENT_PROJECT_RECONCILES: '4'
  MAX_CONCURRENT_PROJECT_CONFIG_RECONCILES: '4'
  MAX_CONCURRENT_SERVICE_ACCOUNT_RECONCILES: '4'
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
    helm.sh/resource-policy: keep
  name: clusterconfigs.kargo.akuity.io
spec:
  group: kargo.akuity.io
  names:
    kind: ClusterConfig
    listKind: ClusterConfigList
    plural: clusterconfigs
    shortNames:
    - clusterconfig
    - clusterconfigs
    singular: clusterconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'ClusterConfig is a resource type that describes cluster-level
          Kargo

          configuration.'
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object.

              Servers should convert recognized schemas to the latest internal value,
              and

              may reject unrecognized values.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents.

              Servers may infer this from the endpoint the client submits requests
              to.

              Cannot be updated.

              In CamelCase.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec describes the configuration of a cluster.
            properties:
              gitClient:
                description: 'GitClient describes cluster-level configuration for
                  Kargo''s Git client,

                  including committer identity and an optional signing key. If set,
                  these

                  values take precedence over any configuration provided at install
                  time

                  via the Helm chart.'
                properties:
                  email:
                    description: Email is the email address used for Git commits made
                      by Kargo.
                    format: email
                    minLength: 1
                    type: string
                  name:
                    description: Name is the name used for Git commits made by Kargo.
                    minLength: 1
                    type: string
                  signingKeySecret:
                    description: 'SigningKeySecret references a Secret in the system
                      namespace containing

                      a GPG signing key for commit signing. The Secret must contain
                      a data

                      key named "signingKey" with the GPG private key material.'
                    properties:
                      name:
                        default: ''
                        description: 'Name of the referent.

                          This field is effectively required, but due to backwards
                          compatibility is

                          allowed to be empty. Instances of this type with an empty
                          value here are

                          almost certainly wrong.

                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - email
                - name
                type: object
              webhookReceivers:
                description: 'WebhookReceivers describes cluster-scoped webhook receivers
                  used for

                  processing events from various external platforms'
                items:
                  description: 'WebhookReceiverConfig describes the configuration
                    for a single webhook

                    receiver.'
                  properties:
                    artifactory:
                      description: 'Artifactory contains the configuration for a webhook
                        receiver that is

                        compatible with JFrog Artifactory payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe Secret's data map is expected to contain\
                            \ a `secret-token` key whose\nvalue is the shared secret\
                            \ used to authenticate the webhook requests sent\nby JFrog\
                            \ Artifactory. For more information please refer to the\
                            \ JFrog\nArtifactory documentation:\n  https://jfrog.com/help/r/jfrog-platform-administration-documentation/webhooks"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                        virtualRepoName:
                          description: 'VirtualRepoName is the name of an Artifactory
                            virtual repository.


                            When unspecified, the Artifactory webhook receiver depends
                            on the value of

                            the webhook payload''s `data.repo_key` field when inferring
                            the URL of the

                            repository from which the webhook originated, which will
                            always be an

                            Artifactory "local repository." In cases where a Warehouse
                            subscribes to

                            such a repository indirectly via a "virtual repository,"
                            there will be a

                            discrepancy between the inferred (local) repository URL
                            and the URL

                            actually used by the subscription, which can prevent the
                            receiver from

                            identifying such a Warehouse as one in need of refreshing.
                            When specified,

                            the value of the VirtualRepoName field supersedes the
                            value of the webhook

                            payload''s `data.repo_key` field to compensate for that
                            discrepancy.


                            In practice, when using virtual repositories, a separate
                            Artifactory

                            webhook receiver should be configured for each, but one
                            such receiver can

                            handle inbound webhooks from any number of local repositories
                            that are

                            aggregated by that virtual repository. For example, if
                            a virtual repository

                            `proj-virtual` aggregates container images from all of
                            the `proj`

                            Artifactory project''s local image repositories, with
                            a single webhook

                            configured to post to a single receiver configured for
                            the `proj-virtual`

                            virtual repository, an image pushed to

                            `example.frog.io/proj-<local-repo-name>/<path>/image`,
                            will cause that

                            receiver to refresh all Warehouses subscribed to

                            `example.frog.io/proj-virtual/<path>/image`.'
                          type: string
                      required:
                      - secretRef
                      type: object
                    azure:
                      description: 'Azure contains the configuration for a webhook
                        receiver that is compatible

                        with Azure Container Registry (ACR) and Azure DevOps payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe Secret's data map is expected to contain\
                            \ a `secret` key whose value\ndoes NOT need to be shared\
                            \ directly with Azure when registering a webhook.\nIt\
                            \ is used only by Kargo to create a complex, hard-to-guess\
                            \ URL,\nwhich implicitly serves as a shared secret. For\
                            \ more information about\nAzure webhooks, please refer\
                            \ to the Azure documentation:\n\n Azure Container Registry:\n\
                            \thttps://learn.microsoft.com/en-us/azure/container-registry/container-registry-repositories\n\
                            \n Azure DevOps:\n\thttp://learn.microsoft.com/en-us/azure/devops/service-hooks/services/webhooks?view=azure-devops"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    bitbucket:
                      description: 'Bitbucket contains the configuration for a webhook
                        receiver that is

                        compatible with Bitbucket payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe Secret's data map is expected to contain\
                            \ a `secret` key whose\nvalue is the shared secret used\
                            \ to authenticate the webhook requests sent\nby Bitbucket.\
                            \ For more information please refer to the Bitbucket\n\
                            documentation:\n  https://support.atlassian.com/bitbucket-cloud/docs/manage-webhooks/"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    dockerhub:
                      description: 'DockerHub contains the configuration for a webhook
                        receiver that is

                        compatible with DockerHub payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nThe Secret's data map is expected to contain a `secret`\
                            \ key whose value\ndoes NOT need to be shared directly\
                            \ with Docker Hub when registering a\nwebhook. It is used\
                            \ only by Kargo to create a complex, hard-to-guess URL,\n\
                            which implicitly serves as a shared secret. For more information\
                            \ about\nDocker Hub webhooks, please refer to the Docker\
                            \ documentation:\n  https://docs.docker.com/docker-hub/webhooks/"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    generic:
                      description: Generic contains the configuration for a generic
                        webhook receiver.
                      properties:
                        actions:
                          description: Actions is a list of actions to be performed
                            when a webhook event is received.
                          items:
                            description: 'GenericWebhookAction describes an action
                              to be performed on a resource

                              and the conditions under which it should be performed.'
                            properties:
                              action:
                                description: 'ActionType indicates the type of action
                                  to be performed. `Refresh` is the

                                  only currently supported action.'
                                enum:
                                - Refresh
                                type: string
                              parameters:
                                additionalProperties:
                                  type: string
                                description: 'Parameters contains additional, action-specific
                                  parameters. Values may be

                                  static or extracted from the request using expressions.'
                                type: object
                              targetSelectionCriteria:
                                description: 'TargetSelectionCriteria is a list of
                                  selection criteria for the resources on which the

                                  action should be performed.'
                                items:
                                  description: 'GenericWebhookTargetSelectionCriteria
                                    describes selection criteria for resources to
                                    which some

                                    action is to be applied. Name, LabelSelector,
                                    and IndexSelector are all optional

                                    however, at least one must be specified. When
                                    multiple criteria are specified, the

                                    results are the combined (logical AND) of the
                                    criteria.'
                                  properties:
                                    indexSelector:
                                      description: 'IndexSelector is a selector used
                                        to identify cached target resources by cache
                                        key.

                                        If used with LabelSelector and/or Name, the
                                        results are the combined (logical AND) of
                                        all the criteria.'
                                      properties:
                                        matchIndices:
                                          description: MatchIndices is a list of index
                                            selector requirements.
                                          items:
                                            description: 'IndexSelectorRequirement
                                              encapsulates a requirement used to select
                                              indexes

                                              based on specific criteria.'
                                            properties:
                                              key:
                                                description: Key is the key of the
                                                  index.
                                                enum:
                                                - subscribedURLs
                                                - receiverPaths
                                                type: string
                                              operator:
                                                description: 'Operator indicates the
                                                  operation that should be used to
                                                  evaluate

                                                  whether the selection requirement
                                                  is satisfied.


                                                  kubebuilder:validation:Enum=Equal;NotEqual;'
                                                type: string
                                              value:
                                                description: 'Value can be a static
                                                  string or an expression that will
                                                  be evaluated.


                                                  kubebuilder:validation:Required'
                                                type: string
                                            required:
                                            - key
                                            - operator
                                            - value
                                            type: object
                                          minItems: 1
                                          type: array
                                      type: object
                                    kind:
                                      description: Kind is the kind of the target
                                        resource.
                                      enum:
                                      - Warehouse
                                      type: string
                                    labelSelector:
                                      description: 'LabelSelector is a label selector
                                        to identify the target resources.

                                        If used with IndexSelector and/or Name, the
                                        results are the combined (logical AND) of
                                        all the criteria.'
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: 'A label selector requirement
                                              is a selector that contains values,
                                              a key, and an operator that

                                              relates the key and values.'
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: 'operator represents
                                                  a key''s relationship to a set of
                                                  values.

                                                  Valid operators are In, NotIn, Exists
                                                  and DoesNotExist.'
                                                type: string
                                              values:
                                                description: 'values is an array of
                                                  string values. If the operator is
                                                  In or NotIn,

                                                  the values array must be non-empty.
                                                  If the operator is Exists or DoesNotExist,

                                                  the values array must be empty.
                                                  This array is replaced during a
                                                  strategic

                                                  merge patch.'
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: 'matchLabels is a map of {key,value}
                                            pairs. A single {key,value} in the matchLabels

                                            map is equivalent to an element of matchExpressions,
                                            whose key field is "key", the

                                            operator is "In", and the values array
                                            contains only "value". The requirements
                                            are ANDed.'
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    name:
                                      description: 'Name is the name of the target
                                        resource. If LabelSelector and/or IndexSelectors

                                        are also specified, the results are the combined
                                        (logical AND) of the criteria.'
                                      type: string
                                  required:
                                  - kind
                                  type: object
                                minItems: 1
                                type: array
                              whenExpression:
                                description: 'WhenExpression defines criteria that
                                  a request must meet to run this

                                  action.'
                                type: string
                            required:
                            - action
                            type: object
                          minItems: 1
                          type: array
                        secretRef:
                          description: 'SecretRef contains a reference to a Secret.
                            For Project-scoped webhook

                            receivers, the referenced Secret must be in the same namespace
                            as the

                            ProjectConfig.


                            For cluster-scoped webhook receivers, the referenced Secret
                            must be in the

                            designated "system resources" namespace.


                            The Secret''s data map is expected to contain a `secret`
                            key whose value

                            does NOT need to be shared directly with the sender. It
                            is used only by

                            Kargo to create a complex, hard-to-guess URL, which implicitly
                            serves as a

                            shared secret.'
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    gitea:
                      description: 'Gitea contains the configuration for a webhook
                        receiver that is compatible

                        with Gitea payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe Secret's data map is expected to contain\
                            \ a `secret` key whose value is\nthe shared secret used\
                            \ to authenticate the webhook requests sent by Gitea.\n\
                            For more information please refer to the Gitea documentation:\n\
                            \  https://docs.gitea.io/en-us/webhooks/"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    github:
                      description: 'GitHub contains the configuration for a webhook
                        receiver that is compatible

                        with GitHub payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe Secret's data map is expected to contain\
                            \ a `secret` key whose value is\nthe shared secret used\
                            \ to authenticate the webhook requests sent by GitHub.\n\
                            For more information please refer to GitHub documentation:\n\
                            \  https://docs.github.com/en/webhooks/using-webhooks/validating-webhook-deliveries"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    gitlab:
                      description: 'GitLab contains the configuration for a webhook
                        receiver that is compatible

                        with GitLab payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe secret is expected to contain a `secret-token`\
                            \ key containing the\nshared secret specified when registering\
                            \ the webhook in GitLab. For more\ninformation about this\
                            \ token, please refer to the GitLab documentation:\n \
                            \ https://docs.gitlab.com/user/project/integrations/webhooks/"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    harbor:
                      description: 'Harbor contains the configuration for a webhook
                        receiver that is compatible

                        with Harbor payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe secret is expected to contain an `auth-header`\
                            \ key containing the \"auth\nheader\" specified when registering\
                            \ the webhook in Harbor. For more\ninformation, please\
                            \ refer to the Harbor documentation:\n  https://goharbor.io/docs/main/working-with-projects/project-configuration/configure-webhooks/"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    name:
                      description: Name is the name of the webhook receiver.
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    quay:
                      description: 'Quay contains the configuration for a webhook
                        receiver that is compatible

                        with Quay payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe Secret's data map is expected to contain\
                            \ a `secret` key whose value\ndoes NOT need to be shared\
                            \ directly with Quay when registering a\nwebhook. It is\
                            \ used only by Kargo to create a complex, hard-to-guess\
                            \ URL,\nwhich implicitly serves as a shared secret. For\
                            \ more information about\nQuay webhooks, please refer\
                            \ to the Quay documentation:\n  https://docs.quay.io/guides/notifications.html"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                  required:
                  - name
                  type: object
                type: array
            type: object
          status:
            description: Status describes the current status of a ClusterConfig.
            properties:
              conditions:
                description: 'Conditions contains the last observations of the ClusterConfig''s
                  current

                  state.'
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: 'lastTransitionTime is the last time the condition
                        transitioned from one status to another.

                        This should be when the underlying condition changed.  If
                        that is not known, then using the time when the API field
                        changed is acceptable.'
                      format: date-time
                      type: string
                    message:
                      description: 'message is a human readable message indicating
                        details about the transition.

                        This may be an empty string.'
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: 'observedGeneration represents the .metadata.generation
                        that the condition was set based upon.

                        For instance, if .metadata.generation is currently 12, but
                        the .status.conditions[x].observedGeneration is 9, the condition
                        is out of date

                        with respect to the current state of the instance.'
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: 'reason contains a programmatic identifier indicating
                        the reason for the condition''s last transition.

                        Producers of specific condition types may define expected
                        values and meanings for this field,

                        and whether the values are considered a guaranteed API.

                        The value should be a CamelCase string.

                        This field may not be empty.'
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - 'True'
                      - 'False'
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledRefresh:
                description: 'LastHandledRefresh holds the value of the most recent
                  AnnotationKeyRefresh

                  annotation that was handled by the controller. This field can be
                  used to

                  determine whether the request to refresh the resource has been handled.'
                type: string
              observedGeneration:
                description: 'ObservedGeneration represents the .metadata.generation
                  that this

                  ClusterConfig was reconciled against.'
                format: int64
                type: integer
              webhookReceivers:
                description: WebhookReceivers describes the status of cluster-scoped
                  webhook receivers.
                items:
                  description: WebhookReceiverDetails encapsulates the details of
                    a webhook receiver.
                  properties:
                    name:
                      description: Name is the name of the webhook receiver.
                      type: string
                    path:
                      description: Path is the path to the receiver's webhook endpoint.
                      type: string
                    url:
                      description: URL includes the full address of the receiver's
                        webhook endpoint.
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
    helm.sh/resource-policy: keep
  name: clusterpromotiontasks.kargo.akuity.io
spec:
  group: kargo.akuity.io
  names:
    kind: ClusterPromotionTask
    listKind: ClusterPromotionTaskList
    plural: clusterpromotiontasks
    shortNames:
    - clusterpromotask
    - clusterpromotasks
    singular: clusterpromotiontask
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object.

              Servers should convert recognized schemas to the latest internal value,
              and

              may reject unrecognized values.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents.

              Servers may infer this from the endpoint the client submits requests
              to.

              Cannot be updated.

              In CamelCase.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: 'Spec describes the desired transition of a specific Stage
              into a specific

              Freight.'
            properties:
              steps:
                description: 'Steps specifies the directives to be executed as part
                  of this

                  PromotionTask. The steps as defined here are inflated into a

                  Promotion when it is built from a PromotionTemplate.'
                items:
                  description: PromotionStep describes a directive to be executed
                    as part of a Promotion.
                  properties:
                    as:
                      description: As is the alias this step can be referred to as.
                      type: string
                    config:
                      description: 'Config is opaque configuration for the PromotionStep
                        that is understood

                        only by each PromotionStep''s implementation. It is legal
                        to utilize

                        expressions in defining values at any level of this block.

                        See https://docs.kargo.io/user-guide/reference-docs/expressions
                        for details.'
                      x-kubernetes-preserve-unknown-fields: true
                    continueOnError:
                      description: 'ContinueOnError is a boolean value that, if set
                        to true, will cause the

                        Promotion to continue executing the next step even if this
                        step fails. It

                        also will not permit this failure to impact the overall status
                        of the

                        Promotion.'
                      type: boolean
                    if:
                      description: 'If is an optional expression that, if present,
                        must evaluate to a boolean

                        value. If the expression evaluates to false, the step will
                        be skipped.

                        If the expression does not evaluate to a boolean value, the
                        step will be

                        considered to have failed.'
                      type: string
                    retry:
                      description: Retry is the retry policy for this step.
                      properties:
                        errorThreshold:
                          description: 'ErrorThreshold is the number of consecutive
                            times the step must fail (for

                            any reason) before retries are abandoned and the entire
                            Promotion is marked

                            as failed.


                            If this field is set to 0, the effective default will
                            be a step-specific

                            one. If no step-specific default exists (i.e. is also
                            0), the effective

                            default will be the system-wide default of 1.


                            A value of 1 will cause the Promotion to be marked as
                            failed after just

                            a single failure; i.e. no retries will be attempted.


                            There is no option to specify an infinite number of retries
                            using a value

                            such as -1.


                            In a future release, Kargo is likely to become capable
                            of distinguishing

                            between recoverable and non-recoverable step failures.
                            At that time, it is

                            planned that unrecoverable failures will not be subject
                            to this threshold

                            and will immediately cause the Promotion to be marked
                            as failed without

                            further condition.'
                          format: int32
                          type: integer
                        timeout:
                          description: 'Timeout is the soft maximum interval in which
                            a step that returns a Running

                            status (which typically indicates it''s waiting for something
                            to happen)

                            may be retried.


                            The maximum is a soft one because the check for whether
                            the interval has

                            elapsed occurs AFTER the step has run. This effectively
                            means a step may

                            run ONCE beyond the close of the interval.


                            If this field is set to nil, the effective default will
                            be a step-specific

                            one. If no step-specific default exists (i.e. is also
                            nil), the effective

                            default will be the system-wide default of 0.


                            A value of 0 will cause the step to be retried indefinitely
                            unless the

                            ErrorThreshold is reached.'
                          type: string
                      type: object
                    task:
                      description: 'Task is a reference to a PromotionTask that should
                        be inflated into a

                        Promotion when it is built from a PromotionTemplate.'
                      properties:
                        kind:
                          description: 'Kind is the type of the PromotionTask. Can
                            be either PromotionTask or

                            ClusterPromotionTask, default is PromotionTask.'
                          enum:
                          - PromotionTask
                          - ClusterPromotionTask
                          type: string
                        name:
                          description: Name is the name of the (Cluster)PromotionTask.
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                          type: string
                      required:
                      - name
                      type: object
                    uses:
                      description: Uses identifies a runner that can execute this
                        step.
                      minLength: 1
                      type: string
                    vars:
                      description: 'Vars is a list of variables that can be referenced
                        by expressions in

                        the step''s Config. The values override the values specified
                        in the

                        PromotionSpec.'
                      items:
                        description: 'ExpressionVariable describes a single variable
                          that may be referenced by

                          expressions in the context of a ClusterPromotionTask, PromotionTask,

                          Promotion, AnalysisRun arguments, or other objects that
                          support expressions.


                          It is used to pass information to the expression evaluation
                          engine, and to

                          allow for dynamic evaluation of expressions based on the
                          variable values.'
                        properties:
                          name:
                            description: Name is the name of the variable.
                            minLength: 1
                            pattern: ^[a-zA-Z_]\w*$
                            type: string
                          value:
                            description: 'Value is the value of the variable. It is
                              allowed to utilize expressions

                              in the value.

                              See https://docs.kargo.io/user-guide/reference-docs/expressions
                              for details.'
                            type: string
                        required:
                        - name
                        type: object
                      type: array
                  type: object
                  x-kubernetes-validations:
                  - message: PromotionTask step must have uses set and must not reference
                      another task
                    rule: has(self.uses) && !has(self.task)
                minItems: 1
                type: array
              vars:
                description: 'Vars specifies the variables available to the PromotionTask.
                  The

                  values of these variables are the default values that can be

                  overridden by the step referencing the task.'
                items:
                  description: 'ExpressionVariable describes a single variable that
                    may be referenced by

                    expressions in the context of a ClusterPromotionTask, PromotionTask,

                    Promotion, AnalysisRun arguments, or other objects that support
                    expressions.


                    It is used to pass information to the expression evaluation engine,
                    and to

                    allow for dynamic evaluation of expressions based on the variable
                    values.'
                  properties:
                    name:
                      description: Name is the name of the variable.
                      minLength: 1
                      pattern: ^[a-zA-Z_]\w*$
                      type: string
                    value:
                      description: 'Value is the value of the variable. It is allowed
                        to utilize expressions

                        in the value.

                        See https://docs.kargo.io/user-guide/reference-docs/expressions
                        for details.'
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - steps
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
    helm.sh/resource-policy: keep
  name: freights.kargo.akuity.io
spec:
  group: kargo.akuity.io
  names:
    kind: Freight
    listKind: FreightList
    plural: freights
    singular: freight
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.labels.kargo\.akuity\.io/alias
      name: Alias
      type: string
    - jsonPath: .origin.kind
      name: Origin (Kind)
      type: string
    - jsonPath: .origin.name
      name: Origin (Name)
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Freight represents a collection of versioned artifacts.
        properties:
          alias:
            description: 'Alias is a human-friendly alias for a piece of Freight.
              This is an optional

              field. A defaulting webhook will sync this field with the value of the

              kargo.akuity.io/alias label. When the alias label is not present or
              differs

              from the value of this field, the defaulting webhook will set the label
              to

              the value of this field. If the alias label is present and this field
              is

              empty, the defaulting webhook will set the value of this field to the
              value

              of the alias label. If this field is empty and the alias label is not

              present, the defaulting webhook will choose an available alias and assign

              it to both the field and label.'
            type: string
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object.

              Servers should convert recognized schemas to the latest internal value,
              and

              may reject unrecognized values.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          artifacts:
            description: 'Artifacts describes specific versions of artifacts other

              than Git repository commits, container images, and Helm charts.'
            items:
              description: ArtifactReference is a reference to a specific version
                of an artifact.
              properties:
                artifactType:
                  description: 'ArtifactType specifies the type of artifact this is.
                    Often, but not always,

                    it will be the media type (MIME type) of the artifact referenced
                    by this

                    ArtifactReference.'
                  minLength: 1
                  type: string
                metadata:
                  description: 'Metadata is a JSON object containing a mostly opaque
                    collection of artifact

                    attributes. (It must be an object. It may not be a list or a scalar
                    value.)

                    "Mostly" because Kargo may understand how to interpret some documented,

                    well-known, top-level keys. Those aside, this metadata is only
                    understood

                    by a corresponding Subscriber implementation that created it.'
                  x-kubernetes-preserve-unknown-fields: true
                subscriptionName:
                  description: 'SubscriptionName is the name of the Subscription that
                    discovered this

                    artifact.'
                  minLength: 1
                  type: string
                version:
                  description: Version identifies a specific revision of this artifact.
                  minLength: 1
                  type: string
              required:
              - subscriptionName
              - version
              type: object
            type: array
          charts:
            description: Charts describes specific versions of specific Helm charts.
            items:
              description: Chart describes a specific version of a Helm chart.
              properties:
                name:
                  description: Name specifies the name of the chart.
                  type: string
                repoURL:
                  description: 'RepoURL specifies the URL of a Helm chart repository.
                    Classic chart

                    repositories (using HTTP/S) can contain differently named charts.
                    When this

                    field points to such a repository, the Name field will specify
                    the name of

                    the chart within the repository. In the case of a repository within
                    an OCI

                    registry, the URL implicitly points to a specific chart and the
                    Name field

                    will be empty.'
                  type: string
                version:
                  description: Version specifies a particular version of the chart.
                  type: string
              type: object
            type: array
          commits:
            description: Commits describes specific Git repository commits.
            items:
              description: GitCommit describes a specific commit from a specific Git
                repository.
              properties:
                author:
                  description: Author is the author of the commit.
                  type: string
                branch:
                  description: Branch denotes the branch of the repository where this
                    commit was found.
                  type: string
                committer:
                  description: Committer is the person who committed the commit.
                  type: string
                id:
                  description: 'ID is the ID of a specific commit in the Git repository
                    specified by

                    RepoURL.'
                  type: string
                message:
                  description: 'Message is the message associated with the commit.
                    At present, this only

                    contains the first line (subject) of the commit message.'
                  type: string
                repoURL:
                  description: RepoURL is the URL of a Git repository.
                  type: string
                tag:
                  description: 'Tag denotes a tag in the repository that matched selection
                    criteria and

                    resolved to this commit.'
                  type: string
              type: object
            type: array
          images:
            description: Images describes specific versions of specific container
              images.
            items:
              description: Image describes a specific version of a container image.
              properties:
                annotations:
                  additionalProperties:
                    type: string
                  description: Annotations is a map of arbitrary metadata for the
                    image.
                  type: object
                digest:
                  description: 'Digest identifies a specific version of the image
                    in the repository

                    specified by RepoURL. This is a more precise identifier than Tag.'
                  type: string
                repoURL:
                  description: RepoURL describes the repository in which the image
                    can be found.
                  type: string
                tag:
                  description: 'Tag identifies a specific version of the image in
                    the repository specified

                    by RepoURL.'
                  type: string
              type: object
            type: array
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents.

              Servers may infer this from the endpoint the client submits requests
              to.

              Cannot be updated.

              In CamelCase.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          origin:
            description: Origin describes a kind of Freight in terms of its origin.
            properties:
              kind:
                description: 'Kind is the kind of resource from which Freight may
                  have originated. At

                  present, this can only be "Warehouse".'
                enum:
                - Warehouse
                type: string
              name:
                description: 'Name is the name of the resource of the kind indicated
                  by the Kind field

                  from which Freight may originate.'
                type: string
            required:
            - kind
            - name
            type: object
          status:
            description: Status describes the current status of this Freight.
            properties:
              approvedFor:
                additionalProperties:
                  description: 'ApprovedStage describes a Stage for which Freight
                    has been (manually)

                    approved.'
                  properties:
                    approvedAt:
                      description: ApprovedAt is the time at which the Freight was
                        approved for the Stage.
                      format: date-time
                      type: string
                  type: object
                description: 'ApprovedFor describes the Stages for which this Freight
                  has been approved

                  preemptively/manually by a user. This is useful for hotfixes, where
                  one

                  might wish to promote a piece of Freight to a given Stage without

                  transiting the entire pipeline.'
                type: object
              currentlyIn:
                additionalProperties:
                  description: CurrentStage reflects a Stage's current use of Freight.
                  properties:
                    since:
                      description: 'Since is the time at which the Stage most recently
                        started using the

                        Freight. This can be used to calculate how long the Freight
                        has been in use

                        by the Stage.'
                      format: date-time
                      type: string
                  type: object
                description: CurrentlyIn describes the Stages in which this Freight
                  is currently in use.
                type: object
              metadata:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                description: 'Metadata is a map of arbitrary metadata associated with
                  the Freight.

                  This is useful for storing additional information about the Freight

                  or Promotion that can be shared across steps or stages.'
                type: object
              verifiedIn:
                additionalProperties:
                  description: VerifiedStage describes a Stage in which Freight has
                    been verified.
                  properties:
                    longestSoak:
                      description: 'LongestCompletedSoak represents the longest definite
                        time interval wherein

                        the Freight was in CONTINUOUS use by the Stage. This value
                        is updated as

                        Freight EXITS the Stage. If the Freight is currently in use
                        by the Stage,

                        the time elapsed since the Freight ENTERED the Stage is its
                        current soak

                        time, which may exceed the value of this field.'
                      type: string
                    verifiedAt:
                      description: VerifiedAt is the time at which the Freight was
                        verified in the Stage.
                      format: date-time
                      type: string
                  type: object
                description: 'VerifiedIn describes the Stages in which this Freight
                  has been verified

                  through promotion and subsequent health checks.'
                type: object
            type: object
        required:
        - origin
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
    helm.sh/resource-policy: keep
  name: projectconfigs.kargo.akuity.io
spec:
  group: kargo.akuity.io
  names:
    kind: ProjectConfig
    listKind: ProjectConfigList
    plural: projectconfigs
    singular: projectconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'ProjectConfig is a resource type that describes the configuration
          of a

          Project.'
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object.

              Servers should convert recognized schemas to the latest internal value,
              and

              may reject unrecognized values.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents.

              Servers may infer this from the endpoint the client submits requests
              to.

              Cannot be updated.

              In CamelCase.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec describes the configuration of a Project.
            properties:
              promotionPolicies:
                description: 'PromotionPolicies defines policies governing the promotion
                  of Freight to

                  specific Stages within the Project.'
                items:
                  description: 'PromotionPolicy defines policies governing the promotion
                    of Freight to a

                    specific Stage.'
                  properties:
                    autoPromotionEnabled:
                      description: 'AutoPromotionEnabled indicates whether new Freight
                        can automatically be

                        promoted into the Stage referenced by the Stage field. Note:
                        There are may

                        be other conditions also required for an auto-promotion to
                        occur. This

                        field defaults to false, but is commonly set to true for Stages
                        that

                        subscribe to Warehouses instead of other, upstream Stages.
                        This allows

                        users to define Stages that are automatically updated as soon
                        as new

                        artifacts are detected.'
                      type: boolean
                    stage:
                      description: 'Stage is the name of the Stage to which this policy
                        applies.


                        Deprecated: Use StageSelector instead.'
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    stageSelector:
                      description: 'StageSelector is a selector that matches the Stage
                        resource to which

                        this policy applies.'
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: 'A label selector requirement is a selector
                              that contains values, a key, and an operator that

                              relates the key and values.'
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: 'operator represents a key''s relationship
                                  to a set of values.

                                  Valid operators are In, NotIn, Exists and DoesNotExist.'
                                type: string
                              values:
                                description: 'values is an array of string values.
                                  If the operator is In or NotIn,

                                  the values array must be non-empty. If the operator
                                  is Exists or DoesNotExist,

                                  the values array must be empty. This array is replaced
                                  during a strategic

                                  merge patch.'
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: 'matchLabels is a map of {key,value} pairs.
                            A single {key,value} in the matchLabels

                            map is equivalent to an element of matchExpressions, whose
                            key field is "key", the

                            operator is "In", and the values array contains only "value".
                            The requirements are ANDed.'
                          type: object
                        name:
                          description: 'Name is the name of the resource to which
                            this policy applies.


                            It can be an exact name, a regex pattern (with prefix
                            "regex:"), or a

                            glob pattern (with prefix "glob:").


                            When both Name and LabelSelector are specified, the Name
                            is ANDed with

                            the LabelSelector. I.e., the resource must match both
                            the Name and

                            LabelSelector to be selected by this policy.


                            NOTE: Using a specific exact name is the most secure option.
                            Pattern

                            matching via regex or glob can be exploited by users with
                            permissions to

                            match promotion policies that weren''t intended to apply
                            to their

                            resources. For example, a user could create a resource
                            with a name

                            deliberately crafted to match the pattern, potentially
                            bypassing intended

                            promotion controls.'
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                  type: object
                  x-kubernetes-validations:
                  - message: PromotionPolicy must have exactly one of stage or stageSelector
                      set
                    rule: 'has(self.stage) ? !has(self.stageSelector) : has(self.stageSelector)'
                type: array
              webhookReceivers:
                description: 'WebhookReceivers describes Project-specific webhook
                  receivers used for

                  processing events from various external platforms'
                items:
                  description: 'WebhookReceiverConfig describes the configuration
                    for a single webhook

                    receiver.'
                  properties:
                    artifactory:
                      description: 'Artifactory contains the configuration for a webhook
                        receiver that is

                        compatible with JFrog Artifactory payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe Secret's data map is expected to contain\
                            \ a `secret-token` key whose\nvalue is the shared secret\
                            \ used to authenticate the webhook requests sent\nby JFrog\
                            \ Artifactory. For more information please refer to the\
                            \ JFrog\nArtifactory documentation:\n  https://jfrog.com/help/r/jfrog-platform-administration-documentation/webhooks"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                        virtualRepoName:
                          description: 'VirtualRepoName is the name of an Artifactory
                            virtual repository.


                            When unspecified, the Artifactory webhook receiver depends
                            on the value of

                            the webhook payload''s `data.repo_key` field when inferring
                            the URL of the

                            repository from which the webhook originated, which will
                            always be an

                            Artifactory "local repository." In cases where a Warehouse
                            subscribes to

                            such a repository indirectly via a "virtual repository,"
                            there will be a

                            discrepancy between the inferred (local) repository URL
                            and the URL

                            actually used by the subscription, which can prevent the
                            receiver from

                            identifying such a Warehouse as one in need of refreshing.
                            When specified,

                            the value of the VirtualRepoName field supersedes the
                            value of the webhook

                            payload''s `data.repo_key` field to compensate for that
                            discrepancy.


                            In practice, when using virtual repositories, a separate
                            Artifactory

                            webhook receiver should be configured for each, but one
                            such receiver can

                            handle inbound webhooks from any number of local repositories
                            that are

                            aggregated by that virtual repository. For example, if
                            a virtual repository

                            `proj-virtual` aggregates container images from all of
                            the `proj`

                            Artifactory project''s local image repositories, with
                            a single webhook

                            configured to post to a single receiver configured for
                            the `proj-virtual`

                            virtual repository, an image pushed to

                            `example.frog.io/proj-<local-repo-name>/<path>/image`,
                            will cause that

                            receiver to refresh all Warehouses subscribed to

                            `example.frog.io/proj-virtual/<path>/image`.'
                          type: string
                      required:
                      - secretRef
                      type: object
                    azure:
                      description: 'Azure contains the configuration for a webhook
                        receiver that is compatible

                        with Azure Container Registry (ACR) and Azure DevOps payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe Secret's data map is expected to contain\
                            \ a `secret` key whose value\ndoes NOT need to be shared\
                            \ directly with Azure when registering a webhook.\nIt\
                            \ is used only by Kargo to create a complex, hard-to-guess\
                            \ URL,\nwhich implicitly serves as a shared secret. For\
                            \ more information about\nAzure webhooks, please refer\
                            \ to the Azure documentation:\n\n Azure Container Registry:\n\
                            \thttps://learn.microsoft.com/en-us/azure/container-registry/container-registry-repositories\n\
                            \n Azure DevOps:\n\thttp://learn.microsoft.com/en-us/azure/devops/service-hooks/services/webhooks?view=azure-devops"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    bitbucket:
                      description: 'Bitbucket contains the configuration for a webhook
                        receiver that is

                        compatible with Bitbucket payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe Secret's data map is expected to contain\
                            \ a `secret` key whose\nvalue is the shared secret used\
                            \ to authenticate the webhook requests sent\nby Bitbucket.\
                            \ For more information please refer to the Bitbucket\n\
                            documentation:\n  https://support.atlassian.com/bitbucket-cloud/docs/manage-webhooks/"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    dockerhub:
                      description: 'DockerHub contains the configuration for a webhook
                        receiver that is

                        compatible with DockerHub payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nThe Secret's data map is expected to contain a `secret`\
                            \ key whose value\ndoes NOT need to be shared directly\
                            \ with Docker Hub when registering a\nwebhook. It is used\
                            \ only by Kargo to create a complex, hard-to-guess URL,\n\
                            which implicitly serves as a shared secret. For more information\
                            \ about\nDocker Hub webhooks, please refer to the Docker\
                            \ documentation:\n  https://docs.docker.com/docker-hub/webhooks/"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    generic:
                      description: Generic contains the configuration for a generic
                        webhook receiver.
                      properties:
                        actions:
                          description: Actions is a list of actions to be performed
                            when a webhook event is received.
                          items:
                            description: 'GenericWebhookAction describes an action
                              to be performed on a resource

                              and the conditions under which it should be performed.'
                            properties:
                              action:
                                description: 'ActionType indicates the type of action
                                  to be performed. `Refresh` is the

                                  only currently supported action.'
                                enum:
                                - Refresh
                                type: string
                              parameters:
                                additionalProperties:
                                  type: string
                                description: 'Parameters contains additional, action-specific
                                  parameters. Values may be

                                  static or extracted from the request using expressions.'
                                type: object
                              targetSelectionCriteria:
                                description: 'TargetSelectionCriteria is a list of
                                  selection criteria for the resources on which the

                                  action should be performed.'
                                items:
                                  description: 'GenericWebhookTargetSelectionCriteria
                                    describes selection criteria for resources to
                                    which some

                                    action is to be applied. Name, LabelSelector,
                                    and IndexSelector are all optional

                                    however, at least one must be specified. When
                                    multiple criteria are specified, the

                                    results are the combined (logical AND) of the
                                    criteria.'
                                  properties:
                                    indexSelector:
                                      description: 'IndexSelector is a selector used
                                        to identify cached target resources by cache
                                        key.

                                        If used with LabelSelector and/or Name, the
                                        results are the combined (logical AND) of
                                        all the criteria.'
                                      properties:
                                        matchIndices:
                                          description: MatchIndices is a list of index
                                            selector requirements.
                                          items:
                                            description: 'IndexSelectorRequirement
                                              encapsulates a requirement used to select
                                              indexes

                                              based on specific criteria.'
                                            properties:
                                              key:
                                                description: Key is the key of the
                                                  index.
                                                enum:
                                                - subscribedURLs
                                                - receiverPaths
                                                type: string
                                              operator:
                                                description: 'Operator indicates the
                                                  operation that should be used to
                                                  evaluate

                                                  whether the selection requirement
                                                  is satisfied.


                                                  kubebuilder:validation:Enum=Equal;NotEqual;'
                                                type: string
                                              value:
                                                description: 'Value can be a static
                                                  string or an expression that will
                                                  be evaluated.


                                                  kubebuilder:validation:Required'
                                                type: string
                                            required:
                                            - key
                                            - operator
                                            - value
                                            type: object
                                          minItems: 1
                                          type: array
                                      type: object
                                    kind:
                                      description: Kind is the kind of the target
                                        resource.
                                      enum:
                                      - Warehouse
                                      type: string
                                    labelSelector:
                                      description: 'LabelSelector is a label selector
                                        to identify the target resources.

                                        If used with IndexSelector and/or Name, the
                                        results are the combined (logical AND) of
                                        all the criteria.'
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: 'A label selector requirement
                                              is a selector that contains values,
                                              a key, and an operator that

                                              relates the key and values.'
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: 'operator represents
                                                  a key''s relationship to a set of
                                                  values.

                                                  Valid operators are In, NotIn, Exists
                                                  and DoesNotExist.'
                                                type: string
                                              values:
                                                description: 'values is an array of
                                                  string values. If the operator is
                                                  In or NotIn,

                                                  the values array must be non-empty.
                                                  If the operator is Exists or DoesNotExist,

                                                  the values array must be empty.
                                                  This array is replaced during a
                                                  strategic

                                                  merge patch.'
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: 'matchLabels is a map of {key,value}
                                            pairs. A single {key,value} in the matchLabels

                                            map is equivalent to an element of matchExpressions,
                                            whose key field is "key", the

                                            operator is "In", and the values array
                                            contains only "value". The requirements
                                            are ANDed.'
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    name:
                                      description: 'Name is the name of the target
                                        resource. If LabelSelector and/or IndexSelectors

                                        are also specified, the results are the combined
                                        (logical AND) of the criteria.'
                                      type: string
                                  required:
                                  - kind
                                  type: object
                                minItems: 1
                                type: array
                              whenExpression:
                                description: 'WhenExpression defines criteria that
                                  a request must meet to run this

                                  action.'
                                type: string
                            required:
                            - action
                            type: object
                          minItems: 1
                          type: array
                        secretRef:
                          description: 'SecretRef contains a reference to a Secret.
                            For Project-scoped webhook

                            receivers, the referenced Secret must be in the same namespace
                            as the

                            ProjectConfig.


                            For cluster-scoped webhook receivers, the referenced Secret
                            must be in the

                            designated "system resources" namespace.


                            The Secret''s data map is expected to contain a `secret`
                            key whose value

                            does NOT need to be shared directly with the sender. It
                            is used only by

                            Kargo to create a complex, hard-to-guess URL, which implicitly
                            serves as a

                            shared secret.'
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    gitea:
                      description: 'Gitea contains the configuration for a webhook
                        receiver that is compatible

                        with Gitea payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe Secret's data map is expected to contain\
                            \ a `secret` key whose value is\nthe shared secret used\
                            \ to authenticate the webhook requests sent by Gitea.\n\
                            For more information please refer to the Gitea documentation:\n\
                            \  https://docs.gitea.io/en-us/webhooks/"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    github:
                      description: 'GitHub contains the configuration for a webhook
                        receiver that is compatible

                        with GitHub payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe Secret's data map is expected to contain\
                            \ a `secret` key whose value is\nthe shared secret used\
                            \ to authenticate the webhook requests sent by GitHub.\n\
                            For more information please refer to GitHub documentation:\n\
                            \  https://docs.github.com/en/webhooks/using-webhooks/validating-webhook-deliveries"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    gitlab:
                      description: 'GitLab contains the configuration for a webhook
                        receiver that is compatible

                        with GitLab payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe secret is expected to contain a `secret-token`\
                            \ key containing the\nshared secret specified when registering\
                            \ the webhook in GitLab. For more\ninformation about this\
                            \ token, please refer to the GitLab documentation:\n \
                            \ https://docs.gitlab.com/user/project/integrations/webhooks/"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    harbor:
                      description: 'Harbor contains the configuration for a webhook
                        receiver that is compatible

                        with Harbor payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe secret is expected to contain an `auth-header`\
                            \ key containing the \"auth\nheader\" specified when registering\
                            \ the webhook in Harbor. For more\ninformation, please\
                            \ refer to the Harbor documentation:\n  https://goharbor.io/docs/main/working-with-projects/project-configuration/configure-webhooks/"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                    name:
                      description: Name is the name of the webhook receiver.
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    quay:
                      description: 'Quay contains the configuration for a webhook
                        receiver that is compatible

                        with Quay payloads.'
                      properties:
                        secretRef:
                          description: "SecretRef contains a reference to a Secret.\
                            \ For Project-scoped webhook\nreceivers, the referenced\
                            \ Secret must be in the same namespace as the\nProjectConfig.\n\
                            \nFor cluster-scoped webhook receivers, the referenced\
                            \ Secret must be in the\ndesignated \"system resources\"\
                            \ namespace.\n\nThe Secret's data map is expected to contain\
                            \ a `secret` key whose value\ndoes NOT need to be shared\
                            \ directly with Quay when registering a\nwebhook. It is\
                            \ used only by Kargo to create a complex, hard-to-guess\
                            \ URL,\nwhich implicitly serves as a shared secret. For\
                            \ more information about\nQuay webhooks, please refer\
                            \ to the Quay documentation:\n  https://docs.quay.io/guides/notifications.html"
                          properties:
                            name:
                              default: ''
                              description: 'Name of the referent.

                                This field is effectively required, but due to backwards
                                compatibility is

                                allowed to be empty. Instances of this type with an
                                empty value here are

                                almost certainly wrong.

                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                  required:
                  - name
                  type: object
                type: array
            type: object
          status:
            description: Status describes the current status of a ProjectConfig.
            properties:
              conditions:
                description: 'Conditions contains the last observations of the Project
                  Config''s current

                  state.'
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: 'lastTransitionTime is the last time the condition
                        transitioned from one status to another.

                        This should be when the underlying condition changed.  If
                        that is not known, then using the time when the API field
                        changed is acceptable.'
                      format: date-time
                      type: string
                    message:
                      description: 'message is a human readable message indicating
                        details about the transition.

                        This may be an empty string.'
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: 'observedGeneration represents the .metadata.generation
                        that the condition was set based upon.

                        For instance, if .metadata.generation is currently 12, but
                        the .status.conditions[x].observedGeneration is 9, the condition
                        is out of date

                        with respect to the current state of the instance.'
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: 'reason contains a programmatic identifier indicating
                        the reason for the condition''s last transition.

                        Producers of specific condition types may define expected
                        values and meanings for this field,

                        and whether the values are considered a guaranteed API.

                        The value should be a CamelCase string.

                        This field may not be empty.'
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - 'True'
                      - 'False'
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledRefresh:
                description: 'LastHandledRefresh holds the value of the most recent
                  AnnotationKeyRefresh

                  annotation that was handled by the controller. This field can be
                  used to

                  determine whether the request to refresh the resource has been handled.'
                type: string
              observedGeneration:
                description: 'ObservedGeneration represents the .metadata.generation
                  that this

                  ProjectConfig was reconciled against.'
                format: int64
                type: integer
              webhookReceivers:
                description: 'WebhookReceivers describes the status of Project-specific
                  webhook

                  receivers.'
                items:
                  description: WebhookReceiverDetails encapsulates the details of
                    a webhook receiver.
                  properties:
                    name:
                      description: Name is the name of the webhook receiver.
                      type: string
                    path:
                      description: Path is the path to the receiver's webhook endpoint.
                      type: string
                    url:
                      description: URL includes the full address of the receiver's
                        webhook endpoint.
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
    helm.sh/resource-policy: keep
  name: projects.kargo.akuity.io
spec:
  group: kargo.akuity.io
  names:
    kind: Project
    listKind: ProjectList
    plural: projects
    singular: project
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Project is a resource type that reconciles to a specially labeled
          namespace

          and other TODO: TBD project-level resources.'
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object.

              Servers should convert recognized schemas to the latest internal value,
              and

              may reject unrecognized values.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents.

              Servers may infer this from the endpoint the client submits requests
              to.

              Cannot be updated.

              In CamelCase.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          status:
            description: Status describes the Project's current status.
            properties:
              conditions:
                description: 'Conditions contains the last observations of the Project''s
                  current

                  state.'
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: 'lastTransitionTime is the last time the condition
                        transitioned from one status to another.

                        This should be when the underlying condition changed.  If
                        that is not known, then using the time when the API field
                        changed is acceptable.'
                      format: date-time
                      type: string
                    message:
                      description: 'message is a human readable message indicating
                        details about the transition.

                        This may be an empty string.'
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: 'observedGeneration represents the .metadata.generation
                        that the condition was set based upon.

                        For instance, if .metadata.generation is currently 12, but
                        the .status.conditions[x].observedGeneration is 9, the condition
                        is out of date

                        with respect to the current state of the instance.'
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: 'reason contains a programmatic identifier indicating
                        the reason for the condition''s last transition.

                        Producers of specific condition types may define expected
                        values and meanings for this field,

                        and whether the values are considered a guaranteed API.

                        The value should be a CamelCase string.

                        This field may not be empty.'
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - 'True'
                      - 'False'
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              stats:
                description: 'Stats contains a summary of the collective state of
                  a Project''s

                  constituent resources.'
                properties:
                  stages:
                    description: Stages contains a summary of the collective state
                      of the Project's Stages.
                    properties:
                      count:
                        description: Count contains the total number of Stages in
                          the Project.
                        format: int64
                        type: integer
                      health:
                        description: Health contains a summary of the collective health
                          of a Project's Stages.
                        properties:
                          healthy:
                            description: Healthy contains the number of resources
                              that are explicitly healthy.
                            format: int64
                            type: integer
                        type: object
                    type: object
                  warehouses:
                    description: 'Warehouses contains a summary of the collective
                      state of the Project''s

                      Warehouses.'
                    properties:
                      count:
                        description: Count contains the total number of Warehouses
                          in the Project.
                        format: int64
                        type: integer
                      health:
                        description: 'Health contains a summary of the collective
                          health of a Project''s

                          Warehouses.'
                        properties:
                          healthy:
                            description: Healthy contains the number of resources
                              that are explicitly healthy.
                            format: int64
                            type: integer
                        type: object
                    type: object
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
    helm.sh/resource-policy: keep
  name: promotions.kargo.akuity.io
spec:
  group: kargo.akuity.io
  names:
    kind: Promotion
    listKind: PromotionList
    plural: promotions
    shortNames:
    - promo
    - promos
    singular: promotion
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.labels.kargo\.akuity\.io/shard
      name: Shard
      type: string
    - jsonPath: .spec.stage
      name: Stage
      type: string
    - jsonPath: .spec.freight
      name: Freight
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Promotion represents a request to transition a particular Stage
          into a

          particular Freight.'
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object.

              Servers should convert recognized schemas to the latest internal value,
              and

              may reject unrecognized values.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents.

              Servers may infer this from the endpoint the client submits requests
              to.

              Cannot be updated.

              In CamelCase.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: 'Spec describes the desired transition of a specific Stage
              into a specific

              Freight.'
            properties:
              freight:
                description: 'Freight specifies the piece of Freight to be promoted
                  into the Stage

                  referenced by the Stage field.'
                maxLength: 253
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                type: string
              stage:
                description: 'Stage specifies the name of the Stage to which this
                  Promotion

                  applies. The Stage referenced by this field MUST be in the same

                  namespace as the Promotion.'
                maxLength: 253
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                type: string
              steps:
                description: 'Steps specifies the directives to be executed as part
                  of this Promotion.

                  The order in which the directives are executed is the order in which
                  they

                  are listed in this field.'
                items:
                  description: PromotionStep describes a directive to be executed
                    as part of a Promotion.
                  properties:
                    as:
                      description: As is the alias this step can be referred to as.
                      type: string
                    config:
                      description: 'Config is opaque configuration for the PromotionStep
                        that is understood

                        only by each PromotionStep''s implementation. It is legal
                        to utilize

                        expressions in defining values at any level of this block.

                        See https://docs.kargo.io/user-guide/reference-docs/expressions
                        for details.'
                      x-kubernetes-preserve-unknown-fields: true
                    continueOnError:
                      description: 'ContinueOnError is a boolean value that, if set
                        to true, will cause the

                        Promotion to continue executing the next step even if this
                        step fails. It

                        also will not permit this failure to impact the overall status
                        of the

                        Promotion.'
                      type: boolean
                    if:
                      description: 'If is an optional expression that, if present,
                        must evaluate to a boolean

                        value. If the expression evaluates to false, the step will
                        be skipped.

                        If the expression does not evaluate to a boolean value, the
                        step will be

                        considered to have failed.'
                      type: string
                    retry:
                      description: Retry is the retry policy for this step.
                      properties:
                        errorThreshold:
                          description: 'ErrorThreshold is the number of consecutive
                            times the step must fail (for

                            any reason) before retries are abandoned and the entire
                            Promotion is marked

                            as failed.


                            If this field is set to 0, the effective default will
                            be a step-specific

                            one. If no step-specific default exists (i.e. is also
                            0), the effective

                            default will be the system-wide default of 1.


                            A value of 1 will cause the Promotion to be marked as
                            failed after just

                            a single failure; i.e. no retries will be attempted.


                            There is no option to specify an infinite number of retries
                            using a value

                            such as -1.


                            In a future release, Kargo is likely to become capable
                            of distinguishing

                            between recoverable and non-recoverable step failures.
                            At that time, it is

                            planned that unrecoverable failures will not be subject
                            to this threshold

                            and will immediately cause the Promotion to be marked
                            as failed without

                            further condition.'
                          format: int32
                          type: integer
                        timeout:
                          description: 'Timeout is the soft maximum interval in which
                            a step that returns a Running

                            status (which typically indicates it''s waiting for something
                            to happen)

                            may be retried.


                            The maximum is a soft one because the check for whether
                            the interval has

                            elapsed occurs AFTER the step has run. This effectively
                            means a step may

                            run ONCE beyond the close of the interval.


                            If this field is set to nil, the effective default will
                            be a step-specific

                            one. If no step-specific default exists (i.e. is also
                            nil), the effective

                            default will be the system-wide default of 0.


                            A value of 0 will cause the step to be retried indefinitely
                            unless the

                            ErrorThreshold is reached.'
                          type: string
                      type: object
                    task:
                      description: 'Task is a reference to a PromotionTask that should
                        be inflated into a

                        Promotion when it is built from a PromotionTemplate.'
                      properties:
                        kind:
                          description: 'Kind is the type of the PromotionTask. Can
                            be either PromotionTask or

                            ClusterPromotionTask, default is PromotionTask.'
                          enum:
                          - PromotionTask
                          - ClusterPromotionTask
                          type: string
                        name:
                          description: Name is the name of the (Cluster)PromotionTask.
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                          type: string
                      required:
                      - name
                      type: object
                    uses:
                      description: Uses identifies a runner that can execute this
                        step.
                      minLength: 1
                      type: string
                    vars:
                      description: 'Vars is a list of variables that can be referenced
                        by expressions in

                        the step''s Config. The values override the values specified
                        in the

                        PromotionSpec.'
                      items:
                        description: 'ExpressionVariable describes a single variable
                          that may be referenced by

                          expressions in the context of a ClusterPromotionTask, PromotionTask,

                          Promotion, AnalysisRun arguments, or other objects that
                          support expressions.


                          It is used to pass information to the expression evaluation
                          engine, and to

                          allow for dynamic evaluation of expressions based on the
                          variable values.'
                        properties:
                          name:
                            description: Name is the name of the variable.
                            minLength: 1
                            pattern: ^[a-zA-Z_]\w*$
                            type: string
                          value:
                            description: 'Value is the value of the variable. It is
                              allowed to utilize expressions

                              in the value.

                              See https://docs.kargo.io/user-guide/reference-docs/expressions
                              for details.'
                            type: string
                        required:
                        - name
                        type: object
                      type: array
                  type: object
                  x-kubernetes-validations:
                  - message: Promotion step must have uses set and must not reference
                      a task
                    rule: has(self.uses) && !has(self.task)
                minItems: 1
                type: array
              vars:
                description: 'Vars is a list of variables that can be referenced by
                  expressions in

                  promotion steps.'
                items:
                  description: 'ExpressionVariable describes a single variable that
                    may be referenced by

                    expressions in the context of a ClusterPromotionTask, PromotionTask,

                    Promotion, AnalysisRun arguments, or other objects that support
                    expressions.


                    It is used to pass information to the expression evaluation engine,
                    and to

                    allow for dynamic evaluation of expressions based on the variable
                    values.'
                  properties:
                    name:
                      description: Name is the name of the variable.
                      minLength: 1
                      pattern: ^[a-zA-Z_]\w*$
                      type: string
                    value:
                      description: 'Value is the value of the variable. It is allowed
                        to utilize expressions

                        in the value.

                        See https://docs.kargo.io/user-guide/reference-docs/expressions
                        for details.'
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - freight
            - stage
            - steps
            type: object
          status:
            description: 'Status describes the current state of the transition represented
              by this

              Promotion.'
            properties:
              currentStep:
                description: 'CurrentStep is the index of the current promotion step
                  being executed. This

                  permits steps that have already run successfully to be skipped on

                  subsequent reconciliations attempts.'
                format: int64
                type: integer
              finishedAt:
                description: FinishedAt is the time when the promotion was completed.
                format: date-time
                type: string
              freight:
                description: Freight is the detail of the piece of freight that was
                  referenced by this promotion.
                properties:
                  artifacts:
                    description: 'Artifacts describes specific versions of artifacts
                      other

                      than Git repository commits, container images, and Helm charts.'
                    items:
                      description: ArtifactReference is a reference to a specific
                        version of an artifact.
                      properties:
                        artifactType:
                          description: 'ArtifactType specifies the type of artifact
                            this is. Often, but not always,

                            it will be the media type (MIME type) of the artifact
                            referenced by this

                            ArtifactReference.'
                          minLength: 1
                          type: string
                        metadata:
                          description: 'Metadata is a JSON object containing a mostly
                            opaque collection of artifact

                            attributes. (It must be an object. It may not be a list
                            or a scalar value.)

                            "Mostly" because Kargo may understand how to interpret
                            some documented,

                            well-known, top-level keys. Those aside, this metadata
                            is only understood

                            by a corresponding Subscriber implementation that created
                            it.'
                          x-kubernetes-preserve-unknown-fields: true
                        subscriptionName:
                          description: 'SubscriptionName is the name of the Subscription
                            that discovered this

                            artifact.'
                          minLength: 1
                          type: string
                        version:
                          description: Version identifies a specific revision of this
                            artifact.
                          minLength: 1
                          type: string
                      required:
                      - subscriptionName
                      - version
                      type: object
                    type: array
                  charts:
                    description: Charts describes specific versions of specific Helm
                      charts.
                    items:
                      description: Chart describes a specific version of a Helm chart.
                      properties:
                        name:
                          description: Name specifies the name of the chart.
                          type: string
                        repoURL:
                          description: 'RepoURL specifies the URL of a Helm chart
                            repository. Classic chart

                            repositories (using HTTP/S) can contain differently named
                            charts. When this

                            field points to such a repository, the Name field will
                            specify the name of

                            the chart within the repository. In the case of a repository
                            within an OCI

                            registry, the URL implicitly points to a specific chart
                            and the Name field

                            will be empty.'
                          type: string
                        version:
                          description: Version specifies a particular version of the
                            chart.
                          type: string
                      type: object
                    type: array
                  commits:
                    description: Commits describes specific Git repository commits.
                    items:
                      description: GitCommit describes a specific commit from a specific
                        Git repository.
                      properties:
                        author:
                          description: Author is the author of the commit.
                          type: string
                        branch:
                          description: Branch denotes the branch of the repository
                            where this commit was found.
                          type: string
                        committer:
                          description: Committer is the person who committed the commit.
                          type: string
                        id:
                          description: 'ID is the ID of a specific commit in the Git
                            repository specified by

                            RepoURL.'
                          type: string
                        message:
                          description: 'Message is the message associated with the
                            commit. At present, this only

                            contains the first line (subject) of the commit message.'
                          type: string
                        repoURL:
                          description: RepoURL is the URL of a Git repository.
                          type: string
                        tag:
                          description: 'Tag denotes a tag in the repository that matched
                            selection criteria and

                            resolved to this commit.'
                          type: string
                      type: object
                    type: array
                  images:
                    description: Images describes specific versions of specific container
                      images.
                    items:
                      description: Image describes a specific version of a container
                        image.
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          description: Annotations is a map of arbitrary metadata
                            for the image.
                          type: object
                        digest:
                          description: 'Digest identifies a specific version of the
                            image in the repository

                            specified by RepoURL. This is a more precise identifier
                            than Tag.'
                          type: string
                        repoURL:
                          description: RepoURL describes the repository in which the
                            image can be found.
                          type: string
                        tag:
                          description: 'Tag identifies a specific version of the image
                            in the repository specified

                            by RepoURL.'
                          type: string
                      type: object
                    type: array
                  name:
                    description: 'Name is a system-assigned identifier derived deterministically
                      from

                      the contents of the Freight. I.e., two pieces of Freight can
                      be compared

                      for equality by comparing their Names.'
                    type: string
                  origin:
                    description: Origin describes a kind of Freight in terms of its
                      origin.
                    properties:
                      kind:
                        description: 'Kind is the kind of resource from which Freight
                          may have originated. At

                          present, this can only be "Warehouse".'
                        enum:
                        - Warehouse
                        type: string
                      name:
                        description: 'Name is the name of the resource of the kind
                          indicated by the Kind field

                          from which Freight may originate.'
                        type: string
                    required:
                    - kind
                    - name
                    type: object
                type: object
              freightCollection:
                description: 'FreightCollection contains the details of the piece
                  of Freight referenced

                  by this Promotion as well as any additional Freight that is carried
                  over

                  from the target Stage''s current state.'
                properties:
                  id:
                    description: 'ID is a unique and deterministically calculated
                      identifier for the

                      FreightCollection. It is updated on each use of the UpdateOrPush
                      method.'
                    type: string
                  items:
                    additionalProperties:
                      description: 'FreightReference is a simplified representation
                        of a piece of Freight -- not

                        a root resource type.'
                      properties:
                        artifacts:
                          description: 'Artifacts describes specific versions of artifacts
                            other

                            than Git repository commits, container images, and Helm
                            charts.'
                          items:
                            description: ArtifactReference is a reference to a specific
                              version of an artifact.
                            properties:
                              artifactType:
                                description: 'ArtifactType specifies the type of artifact
                                  this is. Often, but not always,

                                  it will be the media type (MIME type) of the artifact
                                  referenced by this

                                  ArtifactReference.'
                                minLength: 1
                                type: string
                              metadata:
                                description: 'Metadata is a JSON object containing
                                  a mostly opaque collection of artifact

                                  attributes. (It must be an object. It may not be
                                  a list or a scalar value.)

                                  "Mostly" because Kargo may understand how to interpret
                                  some documented,

                                  well-known, top-level keys. Those aside, this metadata
                                  is only understood

                                  by a corresponding Subscriber implementation that
                                  created it.'
                                x-kubernetes-preserve-unknown-fields: true
                              subscriptionName:
                                description: 'SubscriptionName is the name of the
                                  Subscription that discovered this

                                  artifact.'
                                minLength: 1
                                type: string
                              version:
                                description: Version identifies a specific revision
                                  of this artifact.
                                minLength: 1
                                type: string
                            required:
                            - subscriptionName
                            - version
                            type: object
                          type: array
                        charts:
                          description: Charts describes specific versions of specific
                            Helm charts.
                          items:
                            description: Chart describes a specific version of a Helm
                              chart.
                            properties:
                              name:
                                description: Name specifies the name of the chart.
                                type: string
                              repoURL:
                                description: 'RepoURL specifies the URL of a Helm
                                  chart repository. Classic chart

                                  repositories (using HTTP/S) can contain differently
                                  named charts. When this

                                  field points to such a repository, the Name field
                                  will specify the name of

                                  the chart within the repository. In the case of
                                  a repository within an OCI

                                  registry, the URL implicitly points to a specific
                                  chart and the Name field

                                  will be empty.'
                                type: string
                              version:
                                description: Version specifies a particular version
                                  of the chart.
                                type: string
                            type: object
                          type: array
                        commits:
                          description: Commits describes specific Git repository commits.
                          items:
                            description: GitCommit describes a specific commit from
                              a specific Git repository.
                            properties:
                              author:
                                description: Author is the author of the commit.
                                type: string
                              branch:
                                description: Branch denotes the branch of the repository
                                  where this commit was found.
                                type: string
                              committer:
                                description: Committer is the person who committed
                                  the commit.
                                type: string
                              id:
                                description: 'ID is the ID of a specific commit in
                                  the Git repository specified by

                                  RepoURL.'
                                type: string
                              message:
                                description: 'Message is the message associated with
                                  the commit. At present, this only

                                  contains the first line (subject) of the commit
                                  message.'
                                type: string
                              repoURL:
                                description: RepoURL is the URL of a Git repository.
                                type: string
                              tag:
                                description: 'Tag denotes a tag in the repository
                                  that matched selection criteria and

                                  resolved to this commit.'
                                type: string
                            type: object
                          type: array
                        images:
                          description: Images describes specific versions of specific
                            container images.
                          items:
                            description: Image describes a specific version of a container
                              image.
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: Annotations is a map of arbitrary metadata
                                  for the image.
                                type: object
                              digest:
                                description: 'Digest identifies a specific version
                                  of the image in the repository

                                  specified by RepoURL. This is a more precise identifier
                                  than Tag.'
                                type: string
                              repoURL:
                                description: RepoURL describes the repository in which
                                  the image can be found.
                                type: string
                              tag:
                                description: 'Tag identifies a specific version of
                                  the image in the repository specified

                                  by RepoURL.'
                                type: string
                            type: object
                          type: array
                        name:
                          description: 'Name is a system-assigned identifier derived
                            deterministically from

                            the contents of the Freight. I.e., two pieces of Freight
                            can be compared

                            for equality by comparing their Names.'
                          type: string
                        origin:
                          description: Origin describes a kind of Freight in terms
                            of its origin.
                          properties:
                            kind:
                              description: 'Kind is the kind of resource from which
                                Freight may have originated. At

                                present, this can only be "Warehouse".'
                              enum:
                              - Warehouse
                              type: string
                            name:
                              description: 'Name is the name of the resource of the
                                kind indicated by the Kind field

                                from which Freight may originate.'
                              type: string
                          required:
                          - kind
                          - name
                          type: object
                      type: object
                    description: 'Freight is a map of FreightReference objects, indexed
                      by their Warehouse

                      origin.'
                    type: object
                  verificationHistory:
                    description: 'VerificationHistory is a stack of recent VerificationInfo.
                      By default,

                      the last ten VerificationInfo are stored.'
                    items:
                      description: 'VerificationInfo contains the details of an instance
                        of a Verification

                        process.'
                      properties:
                        actor:
                          description: 'Actor is the name of the entity that initiated
                            or aborted the

                            Verification process.'
                          type: string
                        analysisRun:
                          description: 'AnalysisRun is a reference to the Argo Rollouts
                            AnalysisRun that implements

                            the Verification process.'
                          properties:
                            name:
                              description: Name is the name of the AnalysisRun.
                              type: string
                            namespace:
                              description: Namespace is the namespace of the AnalysisRun.
                              type: string
                            phase:
                              description: Phase is the last observed phase of the
                                AnalysisRun referenced by Name.
                              type: string
                          required:
                          - name
                          - namespace
                          - phase
                          type: object
                        finishTime:
                          description: FinishTime is the time at which the Verification
                            process finished.
                          format: date-time
                          type: string
                        id:
                          description: ID is the identifier of the Verification process.
                          type: string
                        message:
                          description: 'Message may contain additional information
                            about why the verification

                            process is in its current phase.'
                          type: string
                        phase:
                          description: 'Phase describes the current phase of the Verification
                            process. Generally,

                            this will be a reflection of the underlying AnalysisRun''s
                            phase, however,

                            there are exceptions to this, such as in the case where
                            an AnalysisRun

                            cannot be launched successfully.'
                          type: string
                        startTime:
                          description: StartTime is the time at which the Verification
                            process was started.
                          format: date-time
                          type: string
                      type: object
                    type: array
                required:
                - id
                type: object
              healthChecks:
                description: 'HealthChecks contains the health check directives to
                  be executed after

                  the Promotion has completed.'
                items:
                  description: 'HealthCheckStep describes a health check directive
                    which can be executed by

                    a Stage to verify the health of a Promotion result.'
                  properties:
                    config:
                      description: Config is the configuration for the directive.
                      x-kubernetes-preserve-unknown-fields: true
                    uses:
                      description: Uses identifies a runner that can execute this
                        step.
                      minLength: 1
                      type: string
                  required:
                  - uses
                  type: object
                type: array
              lastHandledRefresh:
                description: 'LastHandledRefresh holds the value of the most recent
                  AnnotationKeyRefresh

                  annotation that was handled by the controller. This field can be
                  used to

                  determine whether the request to refresh the resource has been handled.'
                type: string
              message:
                description: 'Message is a display message about the promotion, including
                  any errors

                  preventing the Promotion controller from executing this Promotion.

                  i.e. If the Phase field has a value of Failed, this field can be
                  expected

                  to explain why.'
                type: string
              phase:
                description: Phase describes where the Promotion currently is in its
                  lifecycle.
                type: string
              startedAt:
                description: StartedAt is the time when the promotion started.
                format: date-time
                type: string
              state:
                description: 'State stores the state of the promotion process between
                  reconciliation

                  attempts.'
                x-kubernetes-preserve-unknown-fields: true
              stepExecutionMetadata:
                description: 'StepExecutionMetadata tracks metadata pertaining to
                  the execution

                  of individual promotion steps.'
                items:
                  description: 'StepExecutionMetadata tracks metadata pertaining to
                    the execution of

                    a promotion step.'
                  properties:
                    alias:
                      description: Alias is the alias of the step.
                      type: string
                    continueOnError:
                      description: 'ContinueOnError is a boolean value that, if set
                        to true, will cause the

                        Promotion to continue executing the next step even if this
                        step fails. It

                        also will not permit this failure to impact the overall status
                        of the

                        Promotion.'
                      type: boolean
                    errorCount:
                      description: ErrorCount tracks consecutive failed attempts to
                        execute the step.
                      format: int32
                      type: integer
                    finishedAt:
                      description: 'FinishedAt is the time at which the final attempt
                        to execute the step

                        completed.'
                      format: date-time
                      type: string
                    message:
                      description: Message is a display message about the step, including
                        any errors.
                      type: string
                    startedAt:
                      description: 'StartedAt is the time at which the first attempt
                        to execute the step

                        began.'
                      format: date-time
                      type: string
                    status:
                      description: Status is the high-level outcome of the step.
                      type: string
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
    helm.sh/resource-policy: keep
  name: promotiontasks.kargo.akuity.io
spec:
  group: kargo.akuity.io
  names:
    kind: PromotionTask
    listKind: PromotionTaskList
    plural: promotiontasks
    shortNames:
    - promotask
    - promotasks
    singular: promotiontask
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object.

              Servers should convert recognized schemas to the latest internal value,
              and

              may reject unrecognized values.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents.

              Servers may infer this from the endpoint the client submits requests
              to.

              Cannot be updated.

              In CamelCase.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: 'Spec describes the composition of a PromotionTask, including
              the

              variables available to the task and the steps.'
            properties:
              steps:
                description: 'Steps specifies the directives to be executed as part
                  of this

                  PromotionTask. The steps as defined here are inflated into a

                  Promotion when it is built from a PromotionTemplate.'
                items:
                  description: PromotionStep describes a directive to be executed
                    as part of a Promotion.
                  properties:
                    as:
                      description: As is the alias this step can be referred to as.
                      type: string
                    config:
                      description: 'Config is opaque configuration for the PromotionStep
                        that is understood

                        only by each PromotionStep''s implementation. It is legal
                        to utilize

                        expressions in defining values at any level of this block.

                        See https://docs.kargo.io/user-guide/reference-docs/expressions
                        for details.'
                      x-kubernetes-preserve-unknown-fields: true
                    continueOnError:
                      description: 'ContinueOnError is a boolean value that, if set
                        to true, will cause the

                        Promotion to continue executing the next step even if this
                        step fails. It

                        also will not permit this failure to impact the overall status
                        of the

                        Promotion.'
                      type: boolean
                    if:
                      description: 'If is an optional expression that, if present,
                        must evaluate to a boolean

                        value. If the expression evaluates to false, the step will
                        be skipped.

                        If the expression does not evaluate to a boolean value, the
                        step will be

                        considered to have failed.'
                      type: string
                    retry:
                      description: Retry is the retry policy for this step.
                      properties:
                        errorThreshold:
                          description: 'ErrorThreshold is the number of consecutive
                            times the step must fail (for

                            any reason) before retries are abandoned and the entire
                            Promotion is marked

                            as failed.


                            If this field is set to 0, the effective default will
                            be a step-specific

                            one. If no step-specific default exists (i.e. is also
                            0), the effective

                            default will be the system-wide default of 1.


                            A value of 1 will cause the Promotion to be marked as
                            failed after just

                            a single failure; i.e. no retries will be attempted.


                            There is no option to specify an infinite number of retries
                            using a value

                            such as -1.


                            In a future release, Kargo is likely to become capable
                            of distinguishing

                            between recoverable and non-recoverable step failures.
                            At that time, it is

                            planned that unrecoverable failures will not be subject
                            to this threshold

                            and will immediately cause the Promotion to be marked
                            as failed without

                            further condition.'
                          format: int32
                          type: integer
                        timeout:
                          description: 'Timeout is the soft maximum interval in which
                            a step that returns a Running

                            status (which typically indicates it''s waiting for something
                            to happen)

                            may be retried.


                            The maximum is a soft one because the check for whether
                            the interval has

                            elapsed occurs AFTER the step has run. This effectively
                            means a step may

                            run ONCE beyond the close of the interval.


                            If this field is set to nil, the effective default will
                            be a step-specific

                            one. If no step-specific default exists (i.e. is also
                            nil), the effective

                            default will be the system-wide default of 0.


                            A value of 0 will cause the step to be retried indefinitely
                            unless the

                            ErrorThreshold is reached.'
                          type: string
                      type: object
                    task:
                      description: 'Task is a reference to a PromotionTask that should
                        be inflated into a

                        Promotion when it is built from a PromotionTemplate.'
                      properties:
                        kind:
                          description: 'Kind is the type of the PromotionTask. Can
                            be either PromotionTask or

                            ClusterPromotionTask, default is PromotionTask.'
                          enum:
                          - PromotionTask
                          - ClusterPromotionTask
                          type: string
                        name:
                          description: Name is the name of the (Cluster)PromotionTask.
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                          type: string
                      required:
                      - name
                      type: object
                    uses:
                      description: Uses identifies a runner that can execute this
                        step.
                      minLength: 1
                      type: string
                    vars:
                      description: 'Vars is a list of variables that can be referenced
                        by expressions in

                        the step''s Config. The values override the values specified
                        in the

                        PromotionSpec.'
                      items:
                        description: 'ExpressionVariable describes a single variable
                          that may be referenced by

                          expressions in the context of a ClusterPromotionTask, PromotionTask,

                          Promotion, AnalysisRun arguments, or other objects that
                          support expressions.


                          It is used to pass information to the expression evaluation
                          engine, and to

                          allow for dynamic evaluation of expressions based on the
                          variable values.'
                        properties:
                          name:
                            description: Name is the name of the variable.
                            minLength: 1
                            pattern: ^[a-zA-Z_]\w*$
                            type: string
                          value:
                            description: 'Value is the value of the variable. It is
                              allowed to utilize expressions

                              in the value.

                              See https://docs.kargo.io/user-guide/reference-docs/expressions
                              for details.'
                            type: string
                        required:
                        - name
                        type: object
                      type: array
                  type: object
                  x-kubernetes-validations:
                  - message: PromotionTask step must have uses set and must not reference
                      another task
                    rule: has(self.uses) && !has(self.task)
                minItems: 1
                type: array
              vars:
                description: 'Vars specifies the variables available to the PromotionTask.
                  The

                  values of these variables are the default values that can be

                  overridden by the step referencing the task.'
                items:
                  description: 'ExpressionVariable describes a single variable that
                    may be referenced by

                    expressions in the context of a ClusterPromotionTask, PromotionTask,

                    Promotion, AnalysisRun arguments, or other objects that support
                    expressions.


                    It is used to pass information to the expression evaluation engine,
                    and to

                    allow for dynamic evaluation of expressions based on the variable
                    values.'
                  properties:
                    name:
                      description: Name is the name of the variable.
                      minLength: 1
                      pattern: ^[a-zA-Z_]\w*$
                      type: string
                    value:
                      description: 'Value is the value of the variable. It is allowed
                        to utilize expressions

                        in the value.

                        See https://docs.kargo.io/user-guide/reference-docs/expressions
                        for details.'
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - steps
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
    helm.sh/resource-policy: keep
  name: stages.kargo.akuity.io
spec:
  group: kargo.akuity.io
  names:
    kind: Stage
    listKind: StageList
    plural: stages
    singular: stage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.shard
      name: Shard
      type: string
    - jsonPath: .status.freightSummary
      name: Current Freight
      type: string
    - jsonPath: .status.health.status
      name: Health
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Stage is the Kargo API's main type.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object.

              Servers should convert recognized schemas to the latest internal value,
              and

              may reject unrecognized values.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents.

              Servers may infer this from the endpoint the client submits requests
              to.

              Cannot be updated.

              In CamelCase.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: 'Spec describes sources of Freight used by the Stage and
              how to incorporate

              Freight into the Stage.'
            properties:
              promotionTemplate:
                description: 'PromotionTemplate describes how to incorporate Freight
                  into the Stage

                  using a Promotion.'
                properties:
                  spec:
                    description: 'PromotionTemplateSpec describes the (partial) specification
                      of a Promotion

                      for a Stage. This is a template that can be used to create a
                      Promotion for a

                      Stage.'
                    properties:
                      steps:
                        description: 'Steps specifies the directives to be executed
                          as part of a Promotion.

                          The order in which the directives are executed is the order
                          in which they

                          are listed in this field.'
                        items:
                          description: PromotionStep describes a directive to be executed
                            as part of a Promotion.
                          properties:
                            as:
                              description: As is the alias this step can be referred
                                to as.
                              type: string
                            config:
                              description: 'Config is opaque configuration for the
                                PromotionStep that is understood

                                only by each PromotionStep''s implementation. It is
                                legal to utilize

                                expressions in defining values at any level of this
                                block.

                                See https://docs.kargo.io/user-guide/reference-docs/expressions
                                for details.'
                              x-kubernetes-preserve-unknown-fields: true
                            continueOnError:
                              description: 'ContinueOnError is a boolean value that,
                                if set to true, will cause the

                                Promotion to continue executing the next step even
                                if this step fails. It

                                also will not permit this failure to impact the overall
                                status of the

                                Promotion.'
                              type: boolean
                            if:
                              description: 'If is an optional expression that, if
                                present, must evaluate to a boolean

                                value. If the expression evaluates to false, the step
                                will be skipped.

                                If the expression does not evaluate to a boolean value,
                                the step will be

                                considered to have failed.'
                              type: string
                            retry:
                              description: Retry is the retry policy for this step.
                              properties:
                                errorThreshold:
                                  description: 'ErrorThreshold is the number of consecutive
                                    times the step must fail (for

                                    any reason) before retries are abandoned and the
                                    entire Promotion is marked

                                    as failed.


                                    If this field is set to 0, the effective default
                                    will be a step-specific

                                    one. If no step-specific default exists (i.e.
                                    is also 0), the effective

                                    default will be the system-wide default of 1.


                                    A value of 1 will cause the Promotion to be marked
                                    as failed after just

                                    a single failure; i.e. no retries will be attempted.


                                    There is no option to specify an infinite number
                                    of retries using a value

                                    such as -1.


                                    In a future release, Kargo is likely to become
                                    capable of distinguishing

                                    between recoverable and non-recoverable step failures.
                                    At that time, it is

                                    planned that unrecoverable failures will not be
                                    subject to this threshold

                                    and will immediately cause the Promotion to be
                                    marked as failed without

                                    further condition.'
                                  format: int32
                                  type: integer
                                timeout:
                                  description: 'Timeout is the soft maximum interval
                                    in which a step that returns a Running

                                    status (which typically indicates it''s waiting
                                    for something to happen)

                                    may be retried.


                                    The maximum is a soft one because the check for
                                    whether the interval has

                                    elapsed occurs AFTER the step has run. This effectively
                                    means a step may

                                    run ONCE beyond the close of the interval.


                                    If this field is set to nil, the effective default
                                    will be a step-specific

                                    one. If no step-specific default exists (i.e.
                                    is also nil), the effective

                                    default will be the system-wide default of 0.


                                    A value of 0 will cause the step to be retried
                                    indefinitely unless the

                                    ErrorThreshold is reached.'
                                  type: string
                              type: object
                            task:
                              description: 'Task is a reference to a PromotionTask
                                that should be inflated into a

                                Promotion when it is built from a PromotionTemplate.'
                              properties:
                                kind:
                                  description: 'Kind is the type of the PromotionTask.
                                    Can be either PromotionTask or

                                    ClusterPromotionTask, default is PromotionTask.'
                                  enum:
                                  - PromotionTask
                                  - ClusterPromotionTask
                                  type: string
                                name:
                                  description: Name is the name of the (Cluster)PromotionTask.
                                  maxLength: 253
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                  type: string
                              required:
                              - name
                              type: object
                            uses:
                              description: Uses identifies a runner that can execute
                                this step.
                              minLength: 1
                              type: string
                            vars:
                              description: 'Vars is a list of variables that can be
                                referenced by expressions in

                                the step''s Config. The values override the values
                                specified in the

                                PromotionSpec.'
                              items:
                                description: 'ExpressionVariable describes a single
                                  variable that may be referenced by

                                  expressions in the context of a ClusterPromotionTask,
                                  PromotionTask,

                                  Promotion, AnalysisRun arguments, or other objects
                                  that support expressions.


                                  It is used to pass information to the expression
                                  evaluation engine, and to

                                  allow for dynamic evaluation of expressions based
                                  on the variable values.'
                                properties:
                                  name:
                                    description: Name is the name of the variable.
                                    minLength: 1
                                    pattern: ^[a-zA-Z_]\w*$
                                    type: string
                                  value:
                                    description: 'Value is the value of the variable.
                                      It is allowed to utilize expressions

                                      in the value.

                                      See https://docs.kargo.io/user-guide/reference-docs/expressions
                                      for details.'
                                    type: string
                                required:
                                - name
                                type: object
                              type: array
                          type: object
                          x-kubernetes-validations:
                          - message: PromotionTemplate step must have exactly one
                              of uses or task set
                            rule: '(has(self.uses) ? !has(self.task) : has(self.task))'
                          - message: PromotionTemplate step referencing a task cannot
                              set continueOnError
                            rule: '!has(self.task) || !has(self.continueOnError)'
                          - message: PromotionTemplate step referencing a task cannot
                              set retry
                            rule: '!has(self.task) || !has(self.retry)'
                        minItems: 1
                        type: array
                      vars:
                        description: 'Vars is a list of variables that can be referenced
                          by expressions in

                          promotion steps.'
                        items:
                          description: 'ExpressionVariable describes a single variable
                            that may be referenced by

                            expressions in the context of a ClusterPromotionTask,
                            PromotionTask,

                            Promotion, AnalysisRun arguments, or other objects that
                            support expressions.


                            It is used to pass information to the expression evaluation
                            engine, and to

                            allow for dynamic evaluation of expressions based on the
                            variable values.'
                          properties:
                            name:
                              description: Name is the name of the variable.
                              minLength: 1
                              pattern: ^[a-zA-Z_]\w*$
                              type: string
                            value:
                              description: 'Value is the value of the variable. It
                                is allowed to utilize expressions

                                in the value.

                                See https://docs.kargo.io/user-guide/reference-docs/expressions
                                for details.'
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                    type: object
                required:
                - spec
                type: object
              requestedFreight:
                description: 'RequestedFreight expresses the Stage''s need for certain
                  pieces of Freight,

                  each having originated from a particular Warehouse. This list must
                  be

                  non-empty. In the common case, a Stage will request Freight having

                  originated from just one specific Warehouse. In advanced cases,
                  requesting

                  Freight from multiple Warehouses provides a method of advancing
                  new

                  artifacts of different types through parallel pipelines at different

                  speeds. This can be useful, for instance, if a Stage is home to
                  multiple

                  microservices that are independently versioned.'
                items:
                  description: 'FreightRequest expresses a Stage''s need for Freight
                    having originated from a

                    particular Warehouse.'
                  properties:
                    origin:
                      description: 'Origin specifies from where the requested Freight
                        must have originated.

                        This is a required field.'
                      properties:
                        kind:
                          description: 'Kind is the kind of resource from which Freight
                            may have originated. At

                            present, this can only be "Warehouse".'
                          enum:
                          - Warehouse
                          type: string
                        name:
                          description: 'Name is the name of the resource of the kind
                            indicated by the Kind field

                            from which Freight may originate.'
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                    sources:
                      description: 'Sources describes where the requested Freight
                        may be obtained from. This is

                        a required field.'
                      properties:
                        autoPromotionOptions:
                          description: 'AutoPromotionOptions specifies options pertaining
                            to auto-promotion. These

                            settings have no effect if auto-promotion is not enabled
                            for this Stage at

                            the ProjectConfig level.'
                          properties:
                            selectionPolicy:
                              description: "SelectionPolicy specifies the rules for\
                                \ identifying new Freight that is\neligible for auto-promotion\
                                \ to this Stage. This field is optional. When\nleft\
                                \ unspecified, the field is implicitly treated as\
                                \ if its value were\n\"NewestFreight\".\n\nAccepted\
                                \ Values:\n\n- \"NewestFreight\": The newest Freight\
                                \ that is available to the Stage is\n  eligible for\
                                \ auto-promotion.\n\n- \"MatchUpstream\": Only the\
                                \ Freight currently used immediately upstream\n  from\
                                \ this Stage is eligible for auto-promotion. This\
                                \ policy may only\n  be applied when the Stage has\
                                \ exactly one upstream Stage."
                              enum:
                              - NewestFreight
                              - MatchUpstream
                              type: string
                          type: object
                        availabilityStrategy:
                          description: "AvailabilityStrategy specifies the semantics\
                            \ for how requested Freight is\nmade available to the\
                            \ Stage. This field is optional. When left unspecified,\n\
                            the field is implicitly treated as if its value were \"\
                            OneOf\".\n\nAccepted Values:\n\n- \"All\": Freight must\
                            \ be verified and, if applicable, soaked in all\n  upstream\
                            \ Stages to be considered available for promotion.\n-\
                            \ \"OneOf\": Freight must be verified and, if applicable,\
                            \ soaked in at least\n   one upstream Stage to be considered\
                            \ available for promotion.\n- \"\": Treated the same as\
                            \ \"OneOf\"."
                          enum:
                          - All
                          - OneOf
                          - ''
                          type: string
                        direct:
                          description: 'Direct indicates the requested Freight may
                            be obtained directly from the

                            Warehouse from which it originated. If this field''s value
                            is false, then

                            the value of the Stages field must be non-empty. i.e.
                            Between the two

                            fields, at least one source must be specified.'
                          type: boolean
                        requiredSoakTime:
                          description: 'RequiredSoakTime specifies a minimum duration
                            for which the requested

                            Freight must have continuously occupied ("soaked in")
                            in an upstream Stage

                            before becoming available for promotion to this Stage.
                            This is an optional

                            field. If nil or zero, no soak time is required. Any soak
                            time requirement

                            is in ADDITION to the requirement that Freight be verified
                            in an upstream

                            Stage to become available for promotion to this Stage,
                            although a manual

                            approval for promotion to this Stage will supersede any
                            soak time

                            requirement.'
                          pattern: ^([0-9]+(\.[0-9]+)?(s|m|h))+$
                          type: string
                        stages:
                          description: 'Stages identifies other "upstream" Stages
                            as potential sources of the

                            requested Freight. If this field''s value is empty, then
                            the value of the

                            Direct field must be true. i.e. Between the two fields,
                            at least on source

                            must be specified.'
                          items:
                            type: string
                          type: array
                      type: object
                  required:
                  - origin
                  - sources
                  type: object
                minItems: 1
                type: array
              shard:
                description: 'Shard is the name of the shard that this Stage belongs
                  to. This is an

                  optional field. If not specified, the Stage will belong to the default

                  shard. A defaulting webhook will sync the value of the

                  kargo.akuity.io/shard label with the value of this field. When this
                  field

                  is empty, the webhook will ensure that label is absent.'
                type: string
              vars:
                description: 'Vars is a list of variables that can be referenced anywhere
                  in the

                  StageSpec that supports expressions. For example, the PromotionTemplate

                  and arguments of the Verification.'
                items:
                  description: 'ExpressionVariable describes a single variable that
                    may be referenced by

                    expressions in the context of a ClusterPromotionTask, PromotionTask,

                    Promotion, AnalysisRun arguments, or other objects that support
                    expressions.


                    It is used to pass information to the expression evaluation engine,
                    and to

                    allow for dynamic evaluation of expressions based on the variable
                    values.'
                  properties:
                    name:
                      description: Name is the name of the variable.
                      minLength: 1
                      pattern: ^[a-zA-Z_]\w*$
                      type: string
                    value:
                      description: 'Value is the value of the variable. It is allowed
                        to utilize expressions

                        in the value.

                        See https://docs.kargo.io/user-guide/reference-docs/expressions
                        for details.'
                      type: string
                  required:
                  - name
                  type: object
                type: array
              verification:
                description: 'Verification describes how to verify a Stage''s current
                  Freight is fit for

                  promotion downstream.'
                properties:
                  analysisRunMetadata:
                    description: 'AnalysisRunMetadata contains optional metadata that
                      should be applied to

                      all AnalysisRuns.'
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Additional annotations to apply to an AnalysisRun.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Additional labels to apply to an AnalysisRun.
                        type: object
                    type: object
                  analysisTemplates:
                    description: 'AnalysisTemplates is a list of AnalysisTemplates
                      from which AnalysisRuns

                      should be created to verify a Stage''s current Freight is fit
                      to be promoted

                      downstream.'
                    items:
                      description: AnalysisTemplateReference is a reference to an
                        AnalysisTemplate.
                      properties:
                        kind:
                          description: 'Kind is the type of the AnalysisTemplate.
                            Can be either AnalysisTemplate or

                            ClusterAnalysisTemplate, default is AnalysisTemplate.'
                          enum:
                          - AnalysisTemplate
                          - ClusterAnalysisTemplate
                          type: string
                        name:
                          description: 'Name is the name of the AnalysisTemplate in
                            the same project/namespace as

                            the Stage.'
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  args:
                    description: Args lists arguments that should be added to all
                      AnalysisRuns.
                    items:
                      description: AnalysisRunArgument represents an argument to be
                        added to an AnalysisRun.
                      properties:
                        name:
                          description: Name is the name of the argument.
                          type: string
                        value:
                          description: Value is the value of the argument.
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
            required:
            - requestedFreight
            type: object
          status:
            description: Status describes the Stage's current and recent Freight,
              health, and more.
            properties:
              autoPromotionEnabled:
                description: 'AutoPromotionEnabled indicates whether automatic promotion
                  is enabled

                  for the Stage based on the ProjectConfig.'
                type: boolean
              conditions:
                description: 'Conditions contains the last observations of the Stage''s
                  current

                  state.'
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: 'lastTransitionTime is the last time the condition
                        transitioned from one status to another.

                        This should be when the underlying condition changed.  If
                        that is not known, then using the time when the API field
                        changed is acceptable.'
                      format: date-time
                      type: string
                    message:
                      description: 'message is a human readable message indicating
                        details about the transition.

                        This may be an empty string.'
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: 'observedGeneration represents the .metadata.generation
                        that the condition was set based upon.

                        For instance, if .metadata.generation is currently 12, but
                        the .status.conditions[x].observedGeneration is 9, the condition
                        is out of date

                        with respect to the current state of the instance.'
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: 'reason contains a programmatic identifier indicating
                        the reason for the condition''s last transition.

                        Producers of specific condition types may define expected
                        values and meanings for this field,

                        and whether the values are considered a guaranteed API.

                        The value should be a CamelCase string.

                        This field may not be empty.'
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - 'True'
                      - 'False'
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentPromotion:
                description: CurrentPromotion is a reference to the currently Running
                  promotion.
                properties:
                  finishedAt:
                    description: FinishedAt is the time at which the Promotion was
                      completed.
                    format: date-time
                    type: string
                  freight:
                    description: Freight is the freight being promoted.
                    properties:
                      artifacts:
                        description: 'Artifacts describes specific versions of artifacts
                          other

                          than Git repository commits, container images, and Helm
                          charts.'
                        items:
                          description: ArtifactReference is a reference to a specific
                            version of an artifact.
                          properties:
                            artifactType:
                              description: 'ArtifactType specifies the type of artifact
                                this is. Often, but not always,

                                it will be the media type (MIME type) of the artifact
                                referenced by this

                                ArtifactReference.'
                              minLength: 1
                              type: string
                            metadata:
                              description: 'Metadata is a JSON object containing a
                                mostly opaque collection of artifact

                                attributes. (It must be an object. It may not be a
                                list or a scalar value.)

                                "Mostly" because Kargo may understand how to interpret
                                some documented,

                                well-known, top-level keys. Those aside, this metadata
                                is only understood

                                by a corresponding Subscriber implementation that
                                created it.'
                              x-kubernetes-preserve-unknown-fields: true
                            subscriptionName:
                              description: 'SubscriptionName is the name of the Subscription
                                that discovered this

                                artifact.'
                              minLength: 1
                              type: string
                            version:
                              description: Version identifies a specific revision
                                of this artifact.
                              minLength: 1
                              type: string
                          required:
                          - subscriptionName
                          - version
                          type: object
                        type: array
                      charts:
                        description: Charts describes specific versions of specific
                          Helm charts.
                        items:
                          description: Chart describes a specific version of a Helm
                            chart.
                          properties:
                            name:
                              description: Name specifies the name of the chart.
                              type: string
                            repoURL:
                              description: 'RepoURL specifies the URL of a Helm chart
                                repository. Classic chart

                                repositories (using HTTP/S) can contain differently
                                named charts. When this

                                field points to such a repository, the Name field
                                will specify the name of

                                the chart within the repository. In the case of a
                                repository within an OCI

                                registry, the URL implicitly points to a specific
                                chart and the Name field

                                will be empty.'
                              type: string
                            version:
                              description: Version specifies a particular version
                                of the chart.
                              type: string
                          type: object
                        type: array
                      commits:
                        description: Commits describes specific Git repository commits.
                        items:
                          description: GitCommit describes a specific commit from
                            a specific Git repository.
                          properties:
                            author:
                              description: Author is the author of the commit.
                              type: string
                            branch:
                              description: Branch denotes the branch of the repository
                                where this commit was found.
                              type: string
                            committer:
                              description: Committer is the person who committed the
                                commit.
                              type: string
                            id:
                              description: 'ID is the ID of a specific commit in the
                                Git repository specified by

                                RepoURL.'
                              type: string
                            message:
                              description: 'Message is the message associated with
                                the commit. At present, this only

                                contains the first line (subject) of the commit message.'
                              type: string
                            repoURL:
                              description: RepoURL is the URL of a Git repository.
                              type: string
                            tag:
                              description: 'Tag denotes a tag in the repository that
                                matched selection criteria and

                                resolved to this commit.'
                              type: string
                          type: object
                        type: array
                      images:
                        description: Images describes specific versions of specific
                          container images.
                        items:
                          description: Image describes a specific version of a container
                            image.
                          properties:
                            annotations:
                              additionalProperties:
                                type: string
                              description: Annotations is a map of arbitrary metadata
                                for the image.
                              type: object
                            digest:
                              description: 'Digest identifies a specific version of
                                the image in the repository

                                specified by RepoURL. This is a more precise identifier
                                than Tag.'
                              type: string
                            repoURL:
                              description: RepoURL describes the repository in which
                                the image can be found.
                              type: string
                            tag:
                              description: 'Tag identifies a specific version of the
                                image in the repository specified

                                by RepoURL.'
                              type: string
                          type: object
                        type: array
                      name:
                        description: 'Name is a system-assigned identifier derived
                          deterministically from

                          the contents of the Freight. I.e., two pieces of Freight
                          can be compared

                          for equality by comparing their Names.'
                        type: string
                      origin:
                        description: Origin describes a kind of Freight in terms of
                          its origin.
                        properties:
                          kind:
                            description: 'Kind is the kind of resource from which
                              Freight may have originated. At

                              present, this can only be "Warehouse".'
                            enum:
                            - Warehouse
                            type: string
                          name:
                            description: 'Name is the name of the resource of the
                              kind indicated by the Kind field

                              from which Freight may originate.'
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                    type: object
                  name:
                    description: Name is the name of the Promotion.
                    type: string
                  status:
                    description: Status is the (optional) status of the Promotion.
                    properties:
                      currentStep:
                        description: 'CurrentStep is the index of the current promotion
                          step being executed. This

                          permits steps that have already run successfully to be skipped
                          on

                          subsequent reconciliations attempts.'
                        format: int64
                        type: integer
                      finishedAt:
                        description: FinishedAt is the time when the promotion was
                          completed.
                        format: date-time
                        type: string
                      freight:
                        description: Freight is the detail of the piece of freight
                          that was referenced by this promotion.
                        properties:
                          artifacts:
                            description: 'Artifacts describes specific versions of
                              artifacts other

                              than Git repository commits, container images, and Helm
                              charts.'
                            items:
                              description: ArtifactReference is a reference to a specific
                                version of an artifact.
                              properties:
                                artifactType:
                                  description: 'ArtifactType specifies the type of
                                    artifact this is. Often, but not always,

                                    it will be the media type (MIME type) of the artifact
                                    referenced by this

                                    ArtifactReference.'
                                  minLength: 1
                                  type: string
                                metadata:
                                  description: 'Metadata is a JSON object containing
                                    a mostly opaque collection of artifact

                                    attributes. (It must be an object. It may not
                                    be a list or a scalar value.)

                                    "Mostly" because Kargo may understand how to interpret
                                    some documented,

                                    well-known, top-level keys. Those aside, this
                                    metadata is only understood

                                    by a corresponding Subscriber implementation that
                                    created it.'
                                  x-kubernetes-preserve-unknown-fields: true
                                subscriptionName:
                                  description: 'SubscriptionName is the name of the
                                    Subscription that discovered this

                                    artifact.'
                                  minLength: 1
                                  type: string
                                version:
                                  description: Version identifies a specific revision
                                    of this artifact.
                                  minLength: 1
                                  type: string
                              required:
                              - subscriptionName
                              - version
                              type: object
                            type: array
                          charts:
                            description: Charts describes specific versions of specific
                              Helm charts.
                            items:
                              description: Chart describes a specific version of a
                                Helm chart.
                              properties:
                                name:
                                  description: Name specifies the name of the chart.
                                  type: string
                                repoURL:
                                  description: 'RepoURL specifies the URL of a Helm
                                    chart repository. Classic chart

                                    repositories (using HTTP/S) can contain differently
                                    named charts. When this

                                    field points to such a repository, the Name field
                                    will specify the name of

                                    the chart within the repository. In the case of
                                    a repository within an OCI

                                    registry, the URL implicitly points to a specific
                                    chart and the Name field

                                    will be empty.'
                                  type: string
                                version:
                                  description: Version specifies a particular version
                                    of the chart.
                                  type: string
                              type: object
                            type: array
                          commits:
                            description: Commits describes specific Git repository
                              commits.
                            items:
                              description: GitCommit describes a specific commit from
                                a specific Git repository.
                              properties:
                                author:
                                  description: Author is the author of the commit.
                                  type: string
                                branch:
                                  description: Branch denotes the branch of the repository
                                    where this commit was found.
                                  type: string
                                committer:
                                  description: Committer is the person who committed
                                    the commit.
                                  type: string
                                id:
                                  description: 'ID is the ID of a specific commit
                                    in the Git repository specified by

                                    RepoURL.'
                                  type: string
                                message:
                                  description: 'Message is the message associated
                                    with the commit. At present, this only

                                    contains the first line (subject) of the commit
                                    message.'
                                  type: string
                                repoURL:
                                  description: RepoURL is the URL of a Git repository.
                                  type: string
                                tag:
                                  description: 'Tag denotes a tag in the repository
                                    that matched selection criteria and

                                    resolved to this commit.'
                                  type: string
                              type: object
                            type: array
                          images:
                            description: Images describes specific versions of specific
                              container images.
                            items:
                              description: Image describes a specific version of a
                                container image.
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: Annotations is a map of arbitrary metadata
                                    for the image.
                                  type: object
                                digest:
                                  description: 'Digest identifies a specific version
                                    of the image in the repository

                                    specified by RepoURL. This is a more precise identifier
                                    than Tag.'
                                  type: string
                                repoURL:
                                  description: RepoURL describes the repository in
                                    which the image can be found.
                                  type: string
                                tag:
                                  description: 'Tag identifies a specific version
                                    of the image in the repository specified

                                    by RepoURL.'
                                  type: string
                              type: object
                            type: array
                          name:
                            description: 'Name is a system-assigned identifier derived
                              deterministically from

                              the contents of the Freight. I.e., two pieces of Freight
                              can be compared

                              for equality by comparing their Names.'
                            type: string
                          origin:
                            description: Origin describes a kind of Freight in terms
                              of its origin.
                            properties:
                              kind:
                                description: 'Kind is the kind of resource from which
                                  Freight may have originated. At

                                  present, this can only be "Warehouse".'
                                enum:
                                - Warehouse
                                type: string
                              name:
                                description: 'Name is the name of the resource of
                                  the kind indicated by the Kind field

                                  from which Freight may originate.'
                                type: string
                            required:
                            - kind
                            - name
                            type: object
                        type: object
                      freightCollection:
                        description: 'FreightCollection contains the details of the
                          piece of Freight referenced

                          by this Promotion as well as any additional Freight that
                          is carried over

                          from the target Stage''s current state.'
                        properties:
                          id:
                            description: 'ID is a unique and deterministically calculated
                              identifier for the

                              FreightCollection. It is updated on each use of the
                              UpdateOrPush method.'
                            type: string
                          items:
                            additionalProperties:
                              description: 'FreightReference is a simplified representation
                                of a piece of Freight -- not

                                a root resource type.'
                              properties:
                                artifacts:
                                  description: 'Artifacts describes specific versions
                                    of artifacts other

                                    than Git repository commits, container images,
                                    and Helm charts.'
                                  items:
                                    description: ArtifactReference is a reference
                                      to a specific version of an artifact.
                                    properties:
                                      artifactType:
                                        description: 'ArtifactType specifies the type
                                          of artifact this is. Often, but not always,

                                          it will be the media type (MIME type) of
                                          the artifact referenced by this

                                          ArtifactReference.'
                                        minLength: 1
                                        type: string
                                      metadata:
                                        description: 'Metadata is a JSON object containing
                                          a mostly opaque collection of artifact

                                          attributes. (It must be an object. It may
                                          not be a list or a scalar value.)

                                          "Mostly" because Kargo may understand how
                                          to interpret some documented,

                                          well-known, top-level keys. Those aside,
                                          this metadata is only understood

                                          by a corresponding Subscriber implementation
                                          that created it.'
                                        x-kubernetes-preserve-unknown-fields: true
                                      subscriptionName:
                                        description: 'SubscriptionName is the name
                                          of the Subscription that discovered this

                                          artifact.'
                                        minLength: 1
                                        type: string
                                      version:
                                        description: Version identifies a specific
                                          revision of this artifact.
                                        minLength: 1
                                        type: string
                                    required:
                                    - subscriptionName
                                    - version
                                    type: object
                                  type: array
                                charts:
                                  description: Charts describes specific versions
                                    of specific Helm charts.
                                  items:
                                    description: Chart describes a specific version
                                      of a Helm chart.
                                    properties:
                                      name:
                                        description: Name specifies the name of the
                                          chart.
                                        type: string
                                      repoURL:
                                        description: 'RepoURL specifies the URL of
                                          a Helm chart repository. Classic chart

                                          repositories (using HTTP/S) can contain
                                          differently named charts. When this

                                          field points to such a repository, the Name
                                          field will specify the name of

                                          the chart within the repository. In the
                                          case of a repository within an OCI

                                          registry, the URL implicitly points to a
                                          specific chart and the Name field

                                          will be empty.'
                                        type: string
                                      version:
                                        description: Version specifies a particular
                                          version of the chart.
                                        type: string
                                    type: object
                                  type: array
                                commits:
                                  description: Commits describes specific Git repository
                                    commits.
                                  items:
                                    description: GitCommit describes a specific commit
                                      from a specific Git repository.
                                    properties:
                                      author:
                                        description: Author is the author of the commit.
                                        type: string
                                      branch:
                                        description: Branch denotes the branch of
                                          the repository where this commit was found.
                                        type: string
                                      committer:
                                        description: Committer is the person who committed
                                          the commit.
                                        type: string
                                      id:
                                        description: 'ID is the ID of a specific commit
                                          in the Git repository specified by

                                          RepoURL.'
                                        type: string
                                      message:
                                        description: 'Message is the message associated
                                          with the commit. At present, this only

                                          contains the first line (subject) of the
                                          commit message.'
                                        type: string
                                      repoURL:
                                        description: RepoURL is the URL of a Git repository.
                                        type: string
                                      tag:
                                        description: 'Tag denotes a tag in the repository
                                          that matched selection criteria and

                                          resolved to this commit.'
                                        type: string
                                    type: object
                                  type: array
                                images:
                                  description: Images describes specific versions
                                    of specific container images.
                                  items:
                                    description: Image describes a specific version
                                      of a container image.
                                    properties:
                                      annotations:
                                        additionalProperties:
                                          type: string
                                        description: Annotations is a map of arbitrary
                                          metadata for the image.
                                        type: object
                                      digest:
                                        description: 'Digest identifies a specific
                                          version of the image in the repository

                                          specified by RepoURL. This is a more precise
                                          identifier than Tag.'
                                        type: string
                                      repoURL:
                                        description: RepoURL describes the repository
                                          in which the image can be found.
                                        type: string
                                      tag:
                                        description: 'Tag identifies a specific version
                                          of the image in the repository specified

                                          by RepoURL.'
                                        type: string
                                    type: object
                                  type: array
                                name:
                                  description: 'Name is a system-assigned identifier
                                    derived deterministically from

                                    the contents of the Freight. I.e., two pieces
                                    of Freight can be compared

                                    for equality by comparing their Names.'
                                  type: string
                                origin:
                                  description: Origin describes a kind of Freight
                                    in terms of its origin.
                                  properties:
                                    kind:
                                      description: 'Kind is the kind of resource from
                                        which Freight may have originated. At

                                        present, this can only be "Warehouse".'
                                      enum:
                                      - Warehouse
                                      type: string
                                    name:
                                      description: 'Name is the name of the resource
                                        of the kind indicated by the Kind field

                                        from which Freight may originate.'
                                      type: string
                                  required:
                                  - kind
                                  - name
                                  type: object
                              type: object
                            description: 'Freight is a map of FreightReference objects,
                              indexed by their Warehouse

                              origin.'
                            type: object
                          verificationHistory:
                            description: 'VerificationHistory is a stack of recent
                              VerificationInfo. By default,

                              the last ten VerificationInfo are stored.'
                            items:
                              description: 'VerificationInfo contains the details
                                of an instance of a Verification

                                process.'
                              properties:
                                actor:
                                  description: 'Actor is the name of the entity that
                                    initiated or aborted the

                                    Verification process.'
                                  type: string
                                analysisRun:
                                  description: 'AnalysisRun is a reference to the
                                    Argo Rollouts AnalysisRun that implements

                                    the Verification process.'
                                  properties:
                                    name:
                                      description: Name is the name of the AnalysisRun.
                                      type: string
                                    namespace:
                                      description: Namespace is the namespace of the
                                        AnalysisRun.
                                      type: string
                                    phase:
                                      description: Phase is the last observed phase
                                        of the AnalysisRun referenced by Name.
                                      type: string
                                  required:
                                  - name
                                  - namespace
                                  - phase
                                  type: object
                                finishTime:
                                  description: FinishTime is the time at which the
                                    Verification process finished.
                                  format: date-time
                                  type: string
                                id:
                                  description: ID is the identifier of the Verification
                                    process.
                                  type: string
                                message:
                                  description: 'Message may contain additional information
                                    about why the verification

                                    process is in its current phase.'
                                  type: string
                                phase:
                                  description: 'Phase describes the current phase
                                    of the Verification process. Generally,

                                    this will be a reflection of the underlying AnalysisRun''s
                                    phase, however,

                                    there are exceptions to this, such as in the case
                                    where an AnalysisRun

                                    cannot be launched successfully.'
                                  type: string
                                startTime:
                                  description: StartTime is the time at which the
                                    Verification process was started.
                                  format: date-time
                                  type: string
                              type: object
                            type: array
                        required:
                        - id
                        type: object
                      healthChecks:
                        description: 'HealthChecks contains the health check directives
                          to be executed after

                          the Promotion has completed.'
                        items:
                          description: 'HealthCheckStep describes a health check directive
                            which can be executed by

                            a Stage to verify the health of a Promotion result.'
                          properties:
                            config:
                              description: Config is the configuration for the directive.
                              x-kubernetes-preserve-unknown-fields: true
                            uses:
                              description: Uses identifies a runner that can execute
                                this step.
                              minLength: 1
                              type: string
                          required:
                          - uses
                          type: object
                        type: array
                      lastHandledRefresh:
                        description: 'LastHandledRefresh holds the value of the most
                          recent AnnotationKeyRefresh

                          annotation that was handled by the controller. This field
                          can be used to

                          determine whether the request to refresh the resource has
                          been handled.'
                        type: string
                      message:
                        description: 'Message is a display message about the promotion,
                          including any errors

                          preventing the Promotion controller from executing this
                          Promotion.

                          i.e. If the Phase field has a value of Failed, this field
                          can be expected

                          to explain why.'
                        type: string
                      phase:
                        description: Phase describes where the Promotion currently
                          is in its lifecycle.
                        type: string
                      startedAt:
                        description: StartedAt is the time when the promotion started.
                        format: date-time
                        type: string
                      state:
                        description: 'State stores the state of the promotion process
                          between reconciliation

                          attempts.'
                        x-kubernetes-preserve-unknown-fields: true
                      stepExecutionMetadata:
                        description: 'StepExecutionMetadata tracks metadata pertaining
                          to the execution

                          of individual promotion steps.'
                        items:
                          description: 'StepExecutionMetadata tracks metadata pertaining
                            to the execution of

                            a promotion step.'
                          properties:
                            alias:
                              description: Alias is the alias of the step.
                              type: string
                            continueOnError:
                              description: 'ContinueOnError is a boolean value that,
                                if set to true, will cause the

                                Promotion to continue executing the next step even
                                if this step fails. It

                                also will not permit this failure to impact the overall
                                status of the

                                Promotion.'
                              type: boolean
                            errorCount:
                              description: ErrorCount tracks consecutive failed attempts
                                to execute the step.
                              format: int32
                              type: integer
                            finishedAt:
                              description: 'FinishedAt is the time at which the final
                                attempt to execute the step

                                completed.'
                              format: date-time
                              type: string
                            message:
                              description: Message is a display message about the
                                step, including any errors.
                              type: string
                            startedAt:
                              description: 'StartedAt is the time at which the first
                                attempt to execute the step

                                began.'
                              format: date-time
                              type: string
                            status:
                              description: Status is the high-level outcome of the
                                step.
                              type: string
                          type: object
                        type: array
                    type: object
                required:
                - name
                type: object
              freightHistory:
                description: 'FreightHistory is a list of recent Freight selections
                  that were deployed

                  to the Stage. By default, the last ten Freight selections are stored.

                  The first item in the list is the most recent Freight selection
                  and

                  currently deployed to the Stage, subsequent items are older selections.'
                items:
                  description: 'FreightCollection is a collection of FreightReferences,
                    each of which

                    represents a piece of Freight that has been selected for deployment
                    to a

                    Stage.'
                  properties:
                    id:
                      description: 'ID is a unique and deterministically calculated
                        identifier for the

                        FreightCollection. It is updated on each use of the UpdateOrPush
                        method.'
                      type: string
                    items:
                      additionalProperties:
                        description: 'FreightReference is a simplified representation
                          of a piece of Freight -- not

                          a root resource type.'
                        properties:
                          artifacts:
                            description: 'Artifacts describes specific versions of
                              artifacts other

                              than Git repository commits, container images, and Helm
                              charts.'
                            items:
                              description: ArtifactReference is a reference to a specific
                                version of an artifact.
                              properties:
                                artifactType:
                                  description: 'ArtifactType specifies the type of
                                    artifact this is. Often, but not always,

                                    it will be the media type (MIME type) of the artifact
                                    referenced by this

                                    ArtifactReference.'
                                  minLength: 1
                                  type: string
                                metadata:
                                  description: 'Metadata is a JSON object containing
                                    a mostly opaque collection of artifact

                                    attributes. (It must be an object. It may not
                                    be a list or a scalar value.)

                                    "Mostly" because Kargo may understand how to interpret
                                    some documented,

                                    well-known, top-level keys. Those aside, this
                                    metadata is only understood

                                    by a corresponding Subscriber implementation that
                                    created it.'
                                  x-kubernetes-preserve-unknown-fields: true
                                subscriptionName:
                                  description: 'SubscriptionName is the name of the
                                    Subscription that discovered this

                                    artifact.'
                                  minLength: 1
                                  type: string
                                version:
                                  description: Version identifies a specific revision
                                    of this artifact.
                                  minLength: 1
                                  type: string
                              required:
                              - subscriptionName
                              - version
                              type: object
                            type: array
                          charts:
                            description: Charts describes specific versions of specific
                              Helm charts.
                            items:
                              description: Chart describes a specific version of a
                                Helm chart.
                              properties:
                                name:
                                  description: Name specifies the name of the chart.
                                  type: string
                                repoURL:
                                  description: 'RepoURL specifies the URL of a Helm
                                    chart repository. Classic chart

                                    repositories (using HTTP/S) can contain differently
                                    named charts. When this

                                    field points to such a repository, the Name field
                                    will specify the name of

                                    the chart within the repository. In the case of
                                    a repository within an OCI

                                    registry, the URL implicitly points to a specific
                                    chart and the Name field

                                    will be empty.'
                                  type: string
                                version:
                                  description: Version specifies a particular version
                                    of the chart.
                                  type: string
                              type: object
                            type: array
                          commits:
                            description: Commits describes specific Git repository
                              commits.
                            items:
                              description: GitCommit describes a specific commit from
                                a specific Git repository.
                              properties:
                                author:
                                  description: Author is the author of the commit.
                                  type: string
                                branch:
                                  description: Branch denotes the branch of the repository
                                    where this commit was found.
                                  type: string
                                committer:
                                  description: Committer is the person who committed
                                    the commit.
                                  type: string
                                id:
                                  description: 'ID is the ID of a specific commit
                                    in the Git repository specified by

                                    RepoURL.'
                                  type: string
                                message:
                                  description: 'Message is the message associated
                                    with the commit. At present, this only

                                    contains the first line (subject) of the commit
                                    message.'
                                  type: string
                                repoURL:
                                  description: RepoURL is the URL of a Git repository.
                                  type: string
                                tag:
                                  description: 'Tag denotes a tag in the repository
                                    that matched selection criteria and

                                    resolved to this commit.'
                                  type: string
                              type: object
                            type: array
                          images:
                            description: Images describes specific versions of specific
                              container images.
                            items:
                              description: Image describes a specific version of a
                                container image.
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: Annotations is a map of arbitrary metadata
                                    for the image.
                                  type: object
                                digest:
                                  description: 'Digest identifies a specific version
                                    of the image in the repository

                                    specified by RepoURL. This is a more precise identifier
                                    than Tag.'
                                  type: string
                                repoURL:
                                  description: RepoURL describes the repository in
                                    which the image can be found.
                                  type: string
                                tag:
                                  description: 'Tag identifies a specific version
                                    of the image in the repository specified

                                    by RepoURL.'
                                  type: string
                              type: object
                            type: array
                          name:
                            description: 'Name is a system-assigned identifier derived
                              deterministically from

                              the contents of the Freight. I.e., two pieces of Freight
                              can be compared

                              for equality by comparing their Names.'
                            type: string
                          origin:
                            description: Origin describes a kind of Freight in terms
                              of its origin.
                            properties:
                              kind:
                                description: 'Kind is the kind of resource from which
                                  Freight may have originated. At

                                  present, this can only be "Warehouse".'
                                enum:
                                - Warehouse
                                type: string
                              name:
                                description: 'Name is the name of the resource of
                                  the kind indicated by the Kind field

                                  from which Freight may originate.'
                                type: string
                            required:
                            - kind
                            - name
                            type: object
                        type: object
                      description: 'Freight is a map of FreightReference objects,
                        indexed by their Warehouse

                        origin.'
                      type: object
                    verificationHistory:
                      description: 'VerificationHistory is a stack of recent VerificationInfo.
                        By default,

                        the last ten VerificationInfo are stored.'
                      items:
                        description: 'VerificationInfo contains the details of an
                          instance of a Verification

                          process.'
                        properties:
                          actor:
                            description: 'Actor is the name of the entity that initiated
                              or aborted the

                              Verification process.'
                            type: string
                          analysisRun:
                            description: 'AnalysisRun is a reference to the Argo Rollouts
                              AnalysisRun that implements

                              the Verification process.'
                            properties:
                              name:
                                description: Name is the name of the AnalysisRun.
                                type: string
                              namespace:
                                description: Namespace is the namespace of the AnalysisRun.
                                type: string
                              phase:
                                description: Phase is the last observed phase of the
                                  AnalysisRun referenced by Name.
                                type: string
                            required:
                            - name
                            - namespace
                            - phase
                            type: object
                          finishTime:
                            description: FinishTime is the time at which the Verification
                              process finished.
                            format: date-time
                            type: string
                          id:
                            description: ID is the identifier of the Verification
                              process.
                            type: string
                          message:
                            description: 'Message may contain additional information
                              about why the verification

                              process is in its current phase.'
                            type: string
                          phase:
                            description: 'Phase describes the current phase of the
                              Verification process. Generally,

                              this will be a reflection of the underlying AnalysisRun''s
                              phase, however,

                              there are exceptions to this, such as in the case where
                              an AnalysisRun

                              cannot be launched successfully.'
                            type: string
                          startTime:
                            description: StartTime is the time at which the Verification
                              process was started.
                            format: date-time
                            type: string
                        type: object
                      type: array
                  required:
                  - id
                  type: object
                type: array
              freightSummary:
                description: 'FreightSummary is human-readable text maintained by
                  the controller that

                  summarizes what Freight is currently deployed to the Stage. For
                  Stages that

                  request a single piece of Freight AND the request has been fulfilled,
                  this

                  field will simply contain the name of the Freight. For Stages that
                  request

                  a single piece of Freight AND the request has NOT been fulfilled,
                  or for

                  Stages that request multiple pieces of Freight, this field will
                  contain a

                  summary of fulfilled/requested Freight. The existence of this field
                  is a

                  workaround for kubectl limitations so that this complex but valuable

                  information can be displayed in a column in response to `kubectl
                  get

                  stages`.'
                type: string
              health:
                description: Health is the Stage's last observed health.
                properties:
                  config:
                    description: 'Config is the opaque configuration of all health
                      checks performed on this

                      Stage.'
                    x-kubernetes-preserve-unknown-fields: true
                  issues:
                    description: 'Issues clarifies why a Stage in any state other
                      than Healthy is in that

                      state. This field will always be the empty when a Stage is Healthy.'
                    items:
                      type: string
                    type: array
                  output:
                    description: Output is the opaque output of all health checks
                      performed on this Stage.
                    x-kubernetes-preserve-unknown-fields: true
                  status:
                    description: Status describes the health of the Stage.
                    type: string
                type: object
              lastHandledRefresh:
                description: 'LastHandledRefresh holds the value of the most recent
                  AnnotationKeyRefresh

                  annotation that was handled by the controller. This field can be
                  used to

                  determine whether the request to refresh the resource has been handled.'
                type: string
              lastPromotion:
                description: LastPromotion is a reference to the last completed promotion.
                properties:
                  finishedAt:
                    description: FinishedAt is the time at which the Promotion was
                      completed.
                    format: date-time
                    type: string
                  freight:
                    description: Freight is the freight being promoted.
                    properties:
                      artifacts:
                        description: 'Artifacts describes specific versions of artifacts
                          other

                          than Git repository commits, container images, and Helm
                          charts.'
                        items:
                          description: ArtifactReference is a reference to a specific
                            version of an artifact.
                          properties:
                            artifactType:
                              description: 'ArtifactType specifies the type of artifact
                                this is. Often, but not always,

                                it will be the media type (MIME type) of the artifact
                                referenced by this

                                ArtifactReference.'
                              minLength: 1
                              type: string
                            metadata:
                              description: 'Metadata is a JSON object containing a
                                mostly opaque collection of artifact

                                attributes. (It must be an object. It may not be a
                                list or a scalar value.)

                                "Mostly" because Kargo may understand how to interpret
                                some documented,

                                well-known, top-level keys. Those aside, this metadata
                                is only understood

                                by a corresponding Subscriber implementation that
                                created it.'
                              x-kubernetes-preserve-unknown-fields: true
                            subscriptionName:
                              description: 'SubscriptionName is the name of the Subscription
                                that discovered this

                                artifact.'
                              minLength: 1
                              type: string
                            version:
                              description: Version identifies a specific revision
                                of this artifact.
                              minLength: 1
                              type: string
                          required:
                          - subscriptionName
                          - version
                          type: object
                        type: array
                      charts:
                        description: Charts describes specific versions of specific
                          Helm charts.
                        items:
                          description: Chart describes a specific version of a Helm
                            chart.
                          properties:
                            name:
                              description: Name specifies the name of the chart.
                              type: string
                            repoURL:
                              description: 'RepoURL specifies the URL of a Helm chart
                                repository. Classic chart

                                repositories (using HTTP/S) can contain differently
                                named charts. When this

                                field points to such a repository, the Name field
                                will specify the name of

                                the chart within the repository. In the case of a
                                repository within an OCI

                                registry, the URL implicitly points to a specific
                                chart and the Name field

                                will be empty.'
                              type: string
                            version:
                              description: Version specifies a particular version
                                of the chart.
                              type: string
                          type: object
                        type: array
                      commits:
                        description: Commits describes specific Git repository commits.
                        items:
                          description: GitCommit describes a specific commit from
                            a specific Git repository.
                          properties:
                            author:
                              description: Author is the author of the commit.
                              type: string
                            branch:
                              description: Branch denotes the branch of the repository
                                where this commit was found.
                              type: string
                            committer:
                              description: Committer is the person who committed the
                                commit.
                              type: string
                            id:
                              description: 'ID is the ID of a specific commit in the
                                Git repository specified by

                                RepoURL.'
                              type: string
                            message:
                              description: 'Message is the message associated with
                                the commit. At present, this only

                                contains the first line (subject) of the commit message.'
                              type: string
                            repoURL:
                              description: RepoURL is the URL of a Git repository.
                              type: string
                            tag:
                              description: 'Tag denotes a tag in the repository that
                                matched selection criteria and

                                resolved to this commit.'
                              type: string
                          type: object
                        type: array
                      images:
                        description: Images describes specific versions of specific
                          container images.
                        items:
                          description: Image describes a specific version of a container
                            image.
                          properties:
                            annotations:
                              additionalProperties:
                                type: string
                              description: Annotations is a map of arbitrary metadata
                                for the image.
                              type: object
                            digest:
                              description: 'Digest identifies a specific version of
                                the image in the repository

                                specified by RepoURL. This is a more precise identifier
                                than Tag.'
                              type: string
                            repoURL:
                              description: RepoURL describes the repository in which
                                the image can be found.
                              type: string
                            tag:
                              description: 'Tag identifies a specific version of the
                                image in the repository specified

                                by RepoURL.'
                              type: string
                          type: object
                        type: array
                      name:
                        description: 'Name is a system-assigned identifier derived
                          deterministically from

                          the contents of the Freight. I.e., two pieces of Freight
                          can be compared

                          for equality by comparing their Names.'
                        type: string
                      origin:
                        description: Origin describes a kind of Freight in terms of
                          its origin.
                        properties:
                          kind:
                            description: 'Kind is the kind of resource from which
                              Freight may have originated. At

                              present, this can only be "Warehouse".'
                            enum:
                            - Warehouse
                            type: string
                          name:
                            description: 'Name is the name of the resource of the
                              kind indicated by the Kind field

                              from which Freight may originate.'
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                    type: object
                  name:
                    description: Name is the name of the Promotion.
                    type: string
                  status:
                    description: Status is the (optional) status of the Promotion.
                    properties:
                      currentStep:
                        description: 'CurrentStep is the index of the current promotion
                          step being executed. This

                          permits steps that have already run successfully to be skipped
                          on

                          subsequent reconciliations attempts.'
                        format: int64
                        type: integer
                      finishedAt:
                        description: FinishedAt is the time when the promotion was
                          completed.
                        format: date-time
                        type: string
                      freight:
                        description: Freight is the detail of the piece of freight
                          that was referenced by this promotion.
                        properties:
                          artifacts:
                            description: 'Artifacts describes specific versions of
                              artifacts other

                              than Git repository commits, container images, and Helm
                              charts.'
                            items:
                              description: ArtifactReference is a reference to a specific
                                version of an artifact.
                              properties:
                                artifactType:
                                  description: 'ArtifactType specifies the type of
                                    artifact this is. Often, but not always,

                                    it will be the media type (MIME type) of the artifact
                                    referenced by this

                                    ArtifactReference.'
                                  minLength: 1
                                  type: string
                                metadata:
                                  description: 'Metadata is a JSON object containing
                                    a mostly opaque collection of artifact

                                    attributes. (It must be an object. It may not
                                    be a list or a scalar value.)

                                    "Mostly" because Kargo may understand how to interpret
                                    some documented,

                                    well-known, top-level keys. Those aside, this
                                    metadata is only understood

                                    by a corresponding Subscriber implementation that
                                    created it.'
                                  x-kubernetes-preserve-unknown-fields: true
                                subscriptionName:
                                  description: 'SubscriptionName is the name of the
                                    Subscription that discovered this

                                    artifact.'
                                  minLength: 1
                                  type: string
                                version:
                                  description: Version identifies a specific revision
                                    of this artifact.
                                  minLength: 1
                                  type: string
                              required:
                              - subscriptionName
                              - version
                              type: object
                            type: array
                          charts:
                            description: Charts describes specific versions of specific
                              Helm charts.
                            items:
                              description: Chart describes a specific version of a
                                Helm chart.
                              properties:
                                name:
                                  description: Name specifies the name of the chart.
                                  type: string
                                repoURL:
                                  description: 'RepoURL specifies the URL of a Helm
                                    chart repository. Classic chart

                                    repositories (using HTTP/S) can contain differently
                                    named charts. When this

                                    field points to such a repository, the Name field
                                    will specify the name of

                                    the chart within the repository. In the case of
                                    a repository within an OCI

                                    registry, the URL implicitly points to a specific
                                    chart and the Name field

                                    will be empty.'
                                  type: string
                                version:
                                  description: Version specifies a particular version
                                    of the chart.
                                  type: string
                              type: object
                            type: array
                          commits:
                            description: Commits describes specific Git repository
                              commits.
                            items:
                              description: GitCommit describes a specific commit from
                                a specific Git repository.
                              properties:
                                author:
                                  description: Author is the author of the commit.
                                  type: string
                                branch:
                                  description: Branch denotes the branch of the repository
                                    where this commit was found.
                                  type: string
                                committer:
                                  description: Committer is the person who committed
                                    the commit.
                                  type: string
                                id:
                                  description: 'ID is the ID of a specific commit
                                    in the Git repository specified by

                                    RepoURL.'
                                  type: string
                                message:
                                  description: 'Message is the message associated
                                    with the commit. At present, this only

                                    contains the first line (subject) of the commit
                                    message.'
                                  type: string
                                repoURL:
                                  description: RepoURL is the URL of a Git repository.
                                  type: string
                                tag:
                                  description: 'Tag denotes a tag in the repository
                                    that matched selection criteria and

                                    resolved to this commit.'
                                  type: string
                              type: object
                            type: array
                          images:
                            description: Images describes specific versions of specific
                              container images.
                            items:
                              description: Image describes a specific version of a
                                container image.
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: Annotations is a map of arbitrary metadata
                                    for the image.
                                  type: object
                                digest:
                                  description: 'Digest identifies a specific version
                                    of the image in the repository

                                    specified by RepoURL. This is a more precise identifier
                                    than Tag.'
                                  type: string
                                repoURL:
                                  description: RepoURL describes the repository in
                                    which the image can be found.
                                  type: string
                                tag:
                                  description: 'Tag identifies a specific version
                                    of the image in the repository specified

                                    by RepoURL.'
                                  type: string
                              type: object
                            type: array
                          name:
                            description: 'Name is a system-assigned identifier derived
                              deterministically from

                              the contents of the Freight. I.e., two pieces of Freight
                              can be compared

                              for equality by comparing their Names.'
                            type: string
                          origin:
                            description: Origin describes a kind of Freight in terms
                              of its origin.
                            properties:
                              kind:
                                description: 'Kind is the kind of resource from which
                                  Freight may have originated. At

                                  present, this can only be "Warehouse".'
                                enum:
                                - Warehouse
                                type: string
                              name:
                                description: 'Name is the name of the resource of
                                  the kind indicated by the Kind field

                                  from which Freight may originate.'
                                type: string
                            required:
                            - kind
                            - name
                            type: object
                        type: object
                      freightCollection:
                        description: 'FreightCollection contains the details of the
                          piece of Freight referenced

                          by this Promotion as well as any additional Freight that
                          is carried over

                          from the target Stage''s current state.'
                        properties:
                          id:
                            description: 'ID is a unique and deterministically calculated
                              identifier for the

                              FreightCollection. It is updated on each use of the
                              UpdateOrPush method.'
                            type: string
                          items:
                            additionalProperties:
                              description: 'FreightReference is a simplified representation
                                of a piece of Freight -- not

                                a root resource type.'
                              properties:
                                artifacts:
                                  description: 'Artifacts describes specific versions
                                    of artifacts other

                                    than Git repository commits, container images,
                                    and Helm charts.'
                                  items:
                                    description: ArtifactReference is a reference
                                      to a specific version of an artifact.
                                    properties:
                                      artifactType:
                                        description: 'ArtifactType specifies the type
                                          of artifact this is. Often, but not always,

                                          it will be the media type (MIME type) of
                                          the artifact referenced by this

                                          ArtifactReference.'
                                        minLength: 1
                                        type: string
                                      metadata:
                                        description: 'Metadata is a JSON object containing
                                          a mostly opaque collection of artifact

                                          attributes. (It must be an object. It may
                                          not be a list or a scalar value.)

                                          "Mostly" because Kargo may understand how
                                          to interpret some documented,

                                          well-known, top-level keys. Those aside,
                                          this metadata is only understood

                                          by a corresponding Subscriber implementation
                                          that created it.'
                                        x-kubernetes-preserve-unknown-fields: true
                                      subscriptionName:
                                        description: 'SubscriptionName is the name
                                          of the Subscription that discovered this

                                          artifact.'
                                        minLength: 1
                                        type: string
                                      version:
                                        description: Version identifies a specific
                                          revision of this artifact.
                                        minLength: 1
                                        type: string
                                    required:
                                    - subscriptionName
                                    - version
                                    type: object
                                  type: array
                                charts:
                                  description: Charts describes specific versions
                                    of specific Helm charts.
                                  items:
                                    description: Chart describes a specific version
                                      of a Helm chart.
                                    properties:
                                      name:
                                        description: Name specifies the name of the
                                          chart.
                                        type: string
                                      repoURL:
                                        description: 'RepoURL specifies the URL of
                                          a Helm chart repository. Classic chart

                                          repositories (using HTTP/S) can contain
                                          differently named charts. When this

                                          field points to such a repository, the Name
                                          field will specify the name of

                                          the chart within the repository. In the
                                          case of a repository within an OCI

                                          registry, the URL implicitly points to a
                                          specific chart and the Name field

                                          will be empty.'
                                        type: string
                                      version:
                                        description: Version specifies a particular
                                          version of the chart.
                                        type: string
                                    type: object
                                  type: array
                                commits:
                                  description: Commits describes specific Git repository
                                    commits.
                                  items:
                                    description: GitCommit describes a specific commit
                                      from a specific Git repository.
                                    properties:
                                      author:
                                        description: Author is the author of the commit.
                                        type: string
                                      branch:
                                        description: Branch denotes the branch of
                                          the repository where this commit was found.
                                        type: string
                                      committer:
                                        description: Committer is the person who committed
                                          the commit.
                                        type: string
                                      id:
                                        description: 'ID is the ID of a specific commit
                                          in the Git repository specified by

                                          RepoURL.'
                                        type: string
                                      message:
                                        description: 'Message is the message associated
                                          with the commit. At present, this only

                                          contains the first line (subject) of the
                                          commit message.'
                                        type: string
                                      repoURL:
                                        description: RepoURL is the URL of a Git repository.
                                        type: string
                                      tag:
                                        description: 'Tag denotes a tag in the repository
                                          that matched selection criteria and

                                          resolved to this commit.'
                                        type: string
                                    type: object
                                  type: array
                                images:
                                  description: Images describes specific versions
                                    of specific container images.
                                  items:
                                    description: Image describes a specific version
                                      of a container image.
                                    properties:
                                      annotations:
                                        additionalProperties:
                                          type: string
                                        description: Annotations is a map of arbitrary
                                          metadata for the image.
                                        type: object
                                      digest:
                                        description: 'Digest identifies a specific
                                          version of the image in the repository

                                          specified by RepoURL. This is a more precise
                                          identifier than Tag.'
                                        type: string
                                      repoURL:
                                        description: RepoURL describes the repository
                                          in which the image can be found.
                                        type: string
                                      tag:
                                        description: 'Tag identifies a specific version
                                          of the image in the repository specified

                                          by RepoURL.'
                                        type: string
                                    type: object
                                  type: array
                                name:
                                  description: 'Name is a system-assigned identifier
                                    derived deterministically from

                                    the contents of the Freight. I.e., two pieces
                                    of Freight can be compared

                                    for equality by comparing their Names.'
                                  type: string
                                origin:
                                  description: Origin describes a kind of Freight
                                    in terms of its origin.
                                  properties:
                                    kind:
                                      description: 'Kind is the kind of resource from
                                        which Freight may have originated. At

                                        present, this can only be "Warehouse".'
                                      enum:
                                      - Warehouse
                                      type: string
                                    name:
                                      description: 'Name is the name of the resource
                                        of the kind indicated by the Kind field

                                        from which Freight may originate.'
                                      type: string
                                  required:
                                  - kind
                                  - name
                                  type: object
                              type: object
                            description: 'Freight is a map of FreightReference objects,
                              indexed by their Warehouse

                              origin.'
                            type: object
                          verificationHistory:
                            description: 'VerificationHistory is a stack of recent
                              VerificationInfo. By default,

                              the last ten VerificationInfo are stored.'
                            items:
                              description: 'VerificationInfo contains the details
                                of an instance of a Verification

                                process.'
                              properties:
                                actor:
                                  description: 'Actor is the name of the entity that
                                    initiated or aborted the

                                    Verification process.'
                                  type: string
                                analysisRun:
                                  description: 'AnalysisRun is a reference to the
                                    Argo Rollouts AnalysisRun that implements

                                    the Verification process.'
                                  properties:
                                    name:
                                      description: Name is the name of the AnalysisRun.
                                      type: string
                                    namespace:
                                      description: Namespace is the namespace of the
                                        AnalysisRun.
                                      type: string
                                    phase:
                                      description: Phase is the last observed phase
                                        of the AnalysisRun referenced by Name.
                                      type: string
                                  required:
                                  - name
                                  - namespace
                                  - phase
                                  type: object
                                finishTime:
                                  description: FinishTime is the time at which the
                                    Verification process finished.
                                  format: date-time
                                  type: string
                                id:
                                  description: ID is the identifier of the Verification
                                    process.
                                  type: string
                                message:
                                  description: 'Message may contain additional information
                                    about why the verification

                                    process is in its current phase.'
                                  type: string
                                phase:
                                  description: 'Phase describes the current phase
                                    of the Verification process. Generally,

                                    this will be a reflection of the underlying AnalysisRun''s
                                    phase, however,

                                    there are exceptions to this, such as in the case
                                    where an AnalysisRun

                                    cannot be launched successfully.'
                                  type: string
                                startTime:
                                  description: StartTime is the time at which the
                                    Verification process was started.
                                  format: date-time
                                  type: string
                              type: object
                            type: array
                        required:
                        - id
                        type: object
                      healthChecks:
                        description: 'HealthChecks contains the health check directives
                          to be executed after

                          the Promotion has completed.'
                        items:
                          description: 'HealthCheckStep describes a health check directive
                            which can be executed by

                            a Stage to verify the health of a Promotion result.'
                          properties:
                            config:
                              description: Config is the configuration for the directive.
                              x-kubernetes-preserve-unknown-fields: true
                            uses:
                              description: Uses identifies a runner that can execute
                                this step.
                              minLength: 1
                              type: string
                          required:
                          - uses
                          type: object
                        type: array
                      lastHandledRefresh:
                        description: 'LastHandledRefresh holds the value of the most
                          recent AnnotationKeyRefresh

                          annotation that was handled by the controller. This field
                          can be used to

                          determine whether the request to refresh the resource has
                          been handled.'
                        type: string
                      message:
                        description: 'Message is a display message about the promotion,
                          including any errors

                          preventing the Promotion controller from executing this
                          Promotion.

                          i.e. If the Phase field has a value of Failed, this field
                          can be expected

                          to explain why.'
                        type: string
                      phase:
                        description: Phase describes where the Promotion currently
                          is in its lifecycle.
                        type: string
                      startedAt:
                        description: StartedAt is the time when the promotion started.
                        format: date-time
                        type: string
                      state:
                        description: 'State stores the state of the promotion process
                          between reconciliation

                          attempts.'
                        x-kubernetes-preserve-unknown-fields: true
                      stepExecutionMetadata:
                        description: 'StepExecutionMetadata tracks metadata pertaining
                          to the execution

                          of individual promotion steps.'
                        items:
                          description: 'StepExecutionMetadata tracks metadata pertaining
                            to the execution of

                            a promotion step.'
                          properties:
                            alias:
                              description: Alias is the alias of the step.
                              type: string
                            continueOnError:
                              description: 'ContinueOnError is a boolean value that,
                                if set to true, will cause the

                                Promotion to continue executing the next step even
                                if this step fails. It

                                also will not permit this failure to impact the overall
                                status of the

                                Promotion.'
                              type: boolean
                            errorCount:
                              description: ErrorCount tracks consecutive failed attempts
                                to execute the step.
                              format: int32
                              type: integer
                            finishedAt:
                              description: 'FinishedAt is the time at which the final
                                attempt to execute the step

                                completed.'
                              format: date-time
                              type: string
                            message:
                              description: Message is a display message about the
                                step, including any errors.
                              type: string
                            startedAt:
                              description: 'StartedAt is the time at which the first
                                attempt to execute the step

                                began.'
                              format: date-time
                              type: string
                            status:
                              description: Status is the high-level outcome of the
                                step.
                              type: string
                          type: object
                        type: array
                    type: object
                required:
                - name
                type: object
              metadata:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                description: 'Metadata is a map of arbitrary metadata associated with
                  the Stage.

                  This is useful for storing additional information about the Stage

                  that can be shared across promotions, verifications, or other processes.'
                type: object
              observedGeneration:
                description: 'ObservedGeneration represents the .metadata.generation
                  that this Stage

                  status was reconciled against.'
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
    helm.sh/resource-policy: keep
  name: warehouses.kargo.akuity.io
spec:
  group: kargo.akuity.io
  names:
    kind: Warehouse
    listKind: WarehouseList
    plural: warehouses
    singular: warehouse
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.shard
      name: Shard
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Warehouse is a source of Freight.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object.

              Servers should convert recognized schemas to the latest internal value,
              and

              may reject unrecognized values.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents.

              Servers may infer this from the endpoint the client submits requests
              to.

              Cannot be updated.

              In CamelCase.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec describes sources of artifacts.
            properties:
              freightCreationCriteria:
                description: 'FreightCreationCriteria defines criteria that must be
                  satisfied for Freight

                  to be created automatically from new artifacts following discovery.
                  This

                  field has no effect when the FreightCreationPolicy is `Manual`.'
                properties:
                  expression:
                    description: 'Expression is an expr-lang expression that must
                      evaluate to true for

                      Freight to be created automatically from new artifacts following
                      discovery.'
                    type: string
                type: object
              freightCreationPolicy:
                default: Automatic
                description: "FreightCreationPolicy describes how Freight is created\
                  \ by this Warehouse.\nThis field is optional. When left unspecified,\
                  \ the field is implicitly\ntreated as if its value were \"Automatic\"\
                  .\n\nAccepted values:\n\n- \"Automatic\": New Freight is created\
                  \ automatically when any new artifact\n  is discovered.\n- \"Manual\"\
                  : New Freight is never created automatically."
                enum:
                - Automatic
                - Manual
                type: string
              interval:
                default: 5m0s
                description: 'Interval is the reconciliation interval for this Warehouse.
                  On each

                  reconciliation, the Warehouse will discover new artifacts and optionally

                  produce new Freight. This field is optional. When left unspecified,
                  the

                  field is implicitly treated as if its value were "5m0s".'
                pattern: ^([0-9]+(\.[0-9]+)?(s|m|h))+$
                type: string
              shard:
                description: 'Shard is the name of the shard that this Warehouse belongs
                  to. This is an

                  optional field. If not specified, the Warehouse will belong to the
                  default

                  shard. A defaulting webhook will sync this field with the value
                  of the

                  kargo.akuity.io/shard label. When the shard label is not present
                  or differs

                  from the value of this field, the defaulting webhook will set the
                  label to

                  the value of this field. If the shard label is present and this
                  field is

                  empty, the defaulting webhook will set the value of this field to
                  the value

                  of the shard label.'
                type: string
              subscriptions:
                description: 'Subscriptions describes sources of artifacts to be included
                  in Freight

                  produced by this Warehouse.'
                items:
                  x-kubernetes-preserve-unknown-fields: true
                minItems: 1
                type: array
            required:
            - interval
            - subscriptions
            type: object
          status:
            description: Status describes the Warehouse's most recently observed state.
            properties:
              conditions:
                description: 'Conditions contains the last observations of the Warehouse''s
                  current

                  state.'
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: 'lastTransitionTime is the last time the condition
                        transitioned from one status to another.

                        This should be when the underlying condition changed.  If
                        that is not known, then using the time when the API field
                        changed is acceptable.'
                      format: date-time
                      type: string
                    message:
                      description: 'message is a human readable message indicating
                        details about the transition.

                        This may be an empty string.'
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: 'observedGeneration represents the .metadata.generation
                        that the condition was set based upon.

                        For instance, if .metadata.generation is currently 12, but
                        the .status.conditions[x].observedGeneration is 9, the condition
                        is out of date

                        with respect to the current state of the instance.'
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: 'reason contains a programmatic identifier indicating
                        the reason for the condition''s last transition.

                        Producers of specific condition types may define expected
                        values and meanings for this field,

                        and whether the values are considered a guaranteed API.

                        The value should be a CamelCase string.

                        This field may not be empty.'
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - 'True'
                      - 'False'
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              discoveredArtifacts:
                description: DiscoveredArtifacts holds the artifacts discovered by
                  the Warehouse.
                properties:
                  charts:
                    description: 'Charts holds the charts discovered by the Warehouse
                      for the chart

                      subscriptions.'
                    items:
                      description: 'ChartDiscoveryResult represents the result of
                        a chart discovery operation for

                        a ChartSubscription.'
                      properties:
                        name:
                          description: Name is the name of the Helm chart, as specified
                            in the ChartSubscription.
                          type: string
                        repoURL:
                          description: 'RepoURL is the repository URL of the Helm
                            chart, as specified in the

                            ChartSubscription.'
                          minLength: 1
                          type: string
                        semverConstraint:
                          description: 'SemverConstraint is the constraint for which
                            versions were discovered.

                            This field is optional, and only populated if the ChartSubscription

                            specifies a SemverConstraint.'
                          type: string
                        versions:
                          description: 'Versions is a list of versions discovered
                            by the Warehouse for the

                            ChartSubscription. An empty list indicates that the discovery
                            operation was

                            successful, but no versions matching the ChartSubscription
                            criteria were

                            found.'
                          items:
                            type: string
                          type: array
                      required:
                      - repoURL
                      type: object
                    type: array
                  discoveredAt:
                    description: DiscoveredAt is the time at which the Warehouse discovered
                      the artifacts.
                    format: date-time
                    type: string
                  git:
                    description: 'Git holds the commits discovered by the Warehouse
                      for the Git

                      subscriptions.'
                    items:
                      description: 'GitDiscoveryResult represents the result of a
                        Git discovery operation for a

                        GitSubscription.'
                      properties:
                        commits:
                          description: 'Commits is a list of commits discovered by
                            the Warehouse for the

                            GitSubscription. An empty list indicates that the discovery
                            operation was

                            successful, but no commits matching the GitSubscription
                            criteria were found.'
                          items:
                            description: 'DiscoveredCommit represents a commit discovered
                              by a Warehouse for a

                              GitSubscription.'
                            properties:
                              author:
                                description: Author is the author of the commit.
                                type: string
                              branch:
                                description: 'Branch is the branch in which the commit
                                  was found. This field is

                                  optional, and populated based on the CommitSelectionStrategy
                                  of the

                                  GitSubscription.'
                                type: string
                              committer:
                                description: Committer is the person who committed
                                  the commit.
                                type: string
                              creatorDate:
                                description: 'CreatorDate is the commit creation date
                                  as specified by the commit, or

                                  the tagger date if the commit belongs to an annotated
                                  tag.'
                                format: date-time
                                type: string
                              id:
                                description: ID is the identifier of the commit. This
                                  typically is a SHA-1 hash.
                                minLength: 1
                                type: string
                              subject:
                                description: 'Subject is the subject of the commit
                                  (i.e. the first line of the commit

                                  message).'
                                type: string
                              tag:
                                description: 'Tag is the tag that resolved to this
                                  commit. This field is optional, and

                                  populated based on the CommitSelectionStrategy of
                                  the GitSubscription.'
                                type: string
                            type: object
                          type: array
                        repoURL:
                          description: 'RepoURL is the repository URL of the GitSubscription.

                            '
                          minLength: 1
                          pattern: (?:^(ssh|https?)://(?:([\w-]+)(:(.+))?@)?([\w-]+(?:\.[\w-]+)*)(?::(\d{1,5}))?(/.*)$)|(?:^([\w-]+)@([\w+]+(?:\.[\w-]+)*):(/?.*))
                          type: string
                      required:
                      - repoURL
                      type: object
                    type: array
                  images:
                    description: 'Images holds the image references discovered by
                      the Warehouse for the

                      image subscriptions.'
                    items:
                      description: 'ImageDiscoveryResult represents the result of
                        an image discovery operation

                        for an ImageSubscription.'
                      properties:
                        platform:
                          description: 'Platform is the target platform constraint
                            of the ImageSubscription

                            for which references were discovered. This field is optional,
                            and

                            only populated if the ImageSubscription specifies a Platform.'
                          type: string
                        references:
                          description: 'References is a list of image references discovered
                            by the Warehouse for

                            the ImageSubscription. An empty list indicates that the
                            discovery

                            operation was successful, but no images matching the ImageSubscription

                            criteria were found.'
                          items:
                            description: 'DiscoveredImageReference represents an image
                              reference discovered by a

                              Warehouse for an ImageSubscription.'
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: 'Annotations is a map of key-value pairs
                                  that provide additional

                                  information about the image.'
                                type: object
                              createdAt:
                                description: 'CreatedAt is the time the image was
                                  created. This field is optional, and

                                  not populated for every ImageSelectionStrategy.'
                                format: date-time
                                type: string
                              digest:
                                description: Digest is the digest of the image.
                                minLength: 1
                                pattern: ^[a-z0-9]+:[a-f0-9]+$
                                type: string
                              tag:
                                description: Tag is the tag of the image.
                                maxLength: 128
                                minLength: 1
                                pattern: ^[\w.\-\_]+$
                                type: string
                            required:
                            - digest
                            - tag
                            type: object
                          type: array
                        repoURL:
                          description: 'RepoURL is the repository URL of the image,
                            as specified in the

                            ImageSubscription.'
                          minLength: 1
                          type: string
                      required:
                      - repoURL
                      type: object
                    type: array
                  results:
                    description: Results holds the artifact references discovered
                      by the Warehouse.
                    items:
                      description: 'DiscoveryResult represents the result of an artifact
                        discovery operation for

                        some subscription.'
                      properties:
                        artifactReferences:
                          description: 'ArtifactReferences is a list of references
                            to specific versions of an

                            artifact.'
                          items:
                            description: ArtifactReference is a reference to a specific
                              version of an artifact.
                            properties:
                              artifactType:
                                description: 'ArtifactType specifies the type of artifact
                                  this is. Often, but not always,

                                  it will be the media type (MIME type) of the artifact
                                  referenced by this

                                  ArtifactReference.'
                                minLength: 1
                                type: string
                              metadata:
                                description: 'Metadata is a JSON object containing
                                  a mostly opaque collection of artifact

                                  attributes. (It must be an object. It may not be
                                  a list or a scalar value.)

                                  "Mostly" because Kargo may understand how to interpret
                                  some documented,

                                  well-known, top-level keys. Those aside, this metadata
                                  is only understood

                                  by a corresponding Subscriber implementation that
                                  created it.'
                                x-kubernetes-preserve-unknown-fields: true
                              subscriptionName:
                                description: 'SubscriptionName is the name of the
                                  Subscription that discovered this

                                  artifact.'
                                minLength: 1
                                type: string
                              version:
                                description: Version identifies a specific revision
                                  of this artifact.
                                minLength: 1
                                type: string
                            required:
                            - subscriptionName
                            - version
                            type: object
                          type: array
                        name:
                          description: 'SubscriptionName is the name of the Subscription
                            that discovered these

                            results.'
                          minLength: 1
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                type: object
              lastFreightID:
                description: 'LastFreightID is a reference to the system-assigned
                  identifier (name) of

                  the most recent Freight produced by the Warehouse.'
                type: string
              lastHandledRefresh:
                description: 'LastHandledRefresh holds the value of the most recent
                  AnnotationKeyRefresh

                  annotation that was handled by the controller. This field can be
                  used to

                  determine whether the request to refresh the resource has been handled.'
                type: string
              observedGeneration:
                description: 'ObservedGeneration represents the .metadata.generation
                  that this Warehouse

                  was reconciled against.'
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-api
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: api
rules:
- apiGroups:
  - ''
  resources:
  - namespaces
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ''
  resources:
  - serviceaccounts
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ''
  resources:
  - events
  verbs:
  - get
  - list
  - watch
  - create
  - patch
- apiGroups:
  - ''
  resources:
  - configmaps
  verbs:
  - list
  - get
  - create
  - watch
  - delete
  - update
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
- apiGroups:
  - kargo.akuity.io
  resources:
  - freights
  verbs:
  - create
  - get
  - list
  - patch
  - watch
  - delete
- apiGroups:
  - kargo.akuity.io
  resources:
  - freights/status
  verbs:
  - update
  - patch
- apiGroups:
  - kargo.akuity.io
  resources:
  - clusterconfigs
  - clusterpromotiontasks
  - projects
  - promotiontasks
  - stages
  - warehouses
  - projectconfigs
  verbs:
  - '*'
- apiGroups:
  - kargo.akuity.io
  resources:
  - promotions
  verbs:
  - create
  - get
  - list
  - watch
  - patch
- apiGroups:
  - kargo.akuity.io
  resources:
  - stages/finalizers
  verbs:
  - patch
  - update
- apiGroups:
  - rbac.authorization.k8s.io
  resources:
  - clusterroles
  - rolebindings
  - roles
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - argoproj.io
  resources:
  - analysistemplates
  - clusteranalysistemplates
  verbs:
  - '*'
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-api-rollouts
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: api
rules:
- apiGroups:
  - argoproj.io
  resources:
  - analysisruns
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-project-admin
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ''
  resources:
  - configmaps
  - secrets
  - serviceaccounts
  verbs:
  - '*'
- apiGroups:
  - rbac.authorization.k8s.io
  resources:
  - rolebindings
  - roles
  verbs:
  - '*'
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-project-secrets-reader
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ''
  resources:
  - secrets
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-controller
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
rules:
- apiGroups:
  - ''
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ''
  resources:
  - events
  verbs:
  - create
  - patch
- apiGroups:
  - kargo.akuity.io
  resources:
  - clusterconfigs
  - projects
  - projectconfigs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - promotions
  verbs:
  - create
  - get
  - list
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - freights
  verbs:
  - create
  - get
  - list
  - watch
  - patch
  - update
- apiGroups:
  - kargo.akuity.io
  resources:
  - stages
  verbs:
  - get
  - list
  - patch
  - promote
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - clusterpromotiontasks
  - promotiontasks
  - warehouses
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - freights/status
  - promotions/status
  - stages/status
  - warehouses/finalizers
  - warehouses/status
  verbs:
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-controller-read-secrets
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
rules:
- apiGroups:
  - ''
  resources:
  - secrets
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-controller-argocd
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
rules:
- apiGroups:
  - argoproj.io
  resources:
  - applications
  verbs:
  - get
  - list
  - patch
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-controller-rollouts
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
rules:
- apiGroups:
  - argoproj.io
  resources:
  - analysistemplates
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - argoproj.io
  resources:
  - clusteranalysistemplates
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - argoproj.io
  resources:
  - analysisruns
  verbs:
  - create
  - get
  - list
  - patch
  - watch
  - deletecollection
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-external-webhooks-server
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: external-webhooks-server
rules:
- apiGroups:
  - kargo.akuity.io
  resources:
  - clusterconfigs
  - projectconfigs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - warehouses
  verbs:
  - get
  - list
  - patch
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-garbage-collector
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: garbage-collector
rules:
- apiGroups:
  - ''
  resources:
  - namespaces
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - stages
  verbs:
  - promote
- apiGroups:
  - kargo.akuity.io
  resources:
  - stages
  - warehouses
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - freights
  - promotions
  verbs:
  - delete
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-webhooks-server
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: kubernetes-webhooks-server
rules:
- apiGroups:
  - ''
  resources:
  - namespaces
  verbs:
  - create
  - get
  - list
  - update
  - watch
- apiGroups:
  - ''
  resources:
  - events
  verbs:
  - create
  - patch
- apiGroups:
  - ''
  resources:
  - configmaps
  - secrets
  - serviceaccounts
  verbs:
  - '*'
- apiGroups:
  - rbac.authorization.k8s.io
  resources:
  - rolebindings
  - roles
  verbs:
  - '*'
- apiGroups:
  - kargo.akuity.io
  resources:
  - clusterconfigs
  - clusterpromotiontasks
  - freights
  - projects
  - projectconfigs
  - promotiontasks
  - stages
  - warehouses
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - project/status
  verbs:
  - patch
  - update
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-webhooks-server-ns-controller
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: kubernetes-webhooks-server
rules:
- apiGroups:
  - kargo.akuity.io
  resources:
  - stages
  verbs:
  - promote
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-webhooks-server-generic-gc
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: kubernetes-webhooks-server
rules:
- apiGroups:
  - kargo.akuity.io
  resources:
  - stages
  verbs:
  - promote
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-management-controller
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: management-controller
rules:
- apiGroups:
  - ''
  resources:
  - namespaces
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - watch
- apiGroups:
  - ''
  resources:
  - configmaps
  - secrets
  - serviceaccounts
  verbs:
  - '*'
- apiGroups:
  - rbac.authorization.k8s.io
  resources:
  - clusterroles
  - clusterrolebindings
  - rolebindings
  - roles
  verbs:
  - '*'
- apiGroups:
  - kargo.akuity.io
  resources:
  - projects
  verbs:
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - clusterconfigs/status
  - projects/status
  - projectconfigs/status
  verbs:
  - patch
- apiGroups:
  - kargo.akuity.io
  resources:
  - projectconfigs
  verbs:
  - create
- apiGroups:
  - kargo.akuity.io
  resources:
  - clusterconfigs
  - stages
  - warehouses
  - projectconfigs
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-admin
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ''
  resources:
  - events
  - namespaces
  - serviceaccounts
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - rbac.authorization.k8s.io
  resources:
  - rolebindings
  - roles
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - clusterconfigs
  - clusterpromotiontasks
  - freights
  - projects
  - projectconfigs
  - promotiontasks
  - stages
  - warehouses
  verbs:
  - '*'
- apiGroups:
  - kargo.akuity.io
  resources:
  - stages
  verbs:
  - promote
- apiGroups:
  - kargo.akuity.io
  resources:
  - promotions
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - freights/status
  verbs:
  - patch
- apiGroups:
  - argoproj.io
  resources:
  - analysisruns
  verbs:
  - delete
  - get
  - list
  - watch
- apiGroups:
  - argoproj.io
  resources:
  - analysistemplates
  verbs:
  - '*'
- apiGroups:
  - argoproj.io
  resources:
  - clusteranalysistemplates
  verbs:
  - '*'
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-project-creator
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ''
  resources:
  - namespaces
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - clusterconfigs
  - clusterpromotiontasks
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - projects
  verbs:
  - create
  - get
  - list
  - watch
- apiGroups:
  - argoproj.io
  resources:
  - clusteranalysistemplates
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-user
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ''
  resources:
  - namespaces
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - clusterconfigs
  - clusterpromotiontasks
  - projects
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - argoproj.io
  resources:
  - clusteranalysistemplates
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kargo-viewer
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ''
  resources:
  - events
  - namespaces
  - serviceaccounts
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - rbac.authorization.k8s.io
  resources:
  - rolebindings
  - roles
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kargo.akuity.io
  resources:
  - clusterconfigs
  - clusterpromotiontasks
  - freights
  - projects
  - projectconfigs
  - promotions
  - promotiontasks
  - stages
  - warehouses
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - argoproj.io
  resources:
  - analysisruns
  - analysistemplates
  - clusteranalysistemplates
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-api
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: api
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-api
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-api
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-api-rollouts
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: api
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-api-rollouts
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-api
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-controller
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-controller
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-controller
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-controller-argocd
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-controller-argocd
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-controller
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-controller-rollouts
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-controller-rollouts
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-controller
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-external-webhooks-server
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: external-webhooks-server
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-external-webhooks-server
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-external-webhooks-server
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-garbage-collector
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: garbage-collector
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-garbage-collector
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-garbage-collector
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-webhooks-server
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: kubernetes-webhooks-server
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-webhooks-server
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-webhooks-server
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-webhooks-server-ns-controller
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: kubernetes-webhooks-server
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-webhooks-server-ns-controller
subjects:
- kind: ServiceAccount
  namespace: kube-system
  name: namespace-controller
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-webhooks-server-generic-gc
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: kubernetes-webhooks-server
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-webhooks-server-generic-gc
subjects:
- kind: ServiceAccount
  namespace: kube-system
  name: generic-garbage-collector
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-management-controller
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: management-controller
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-management-controller
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-management-controller
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-admin
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-admin
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-admin
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-project-creator
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-project-creator
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-project-creator
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-user
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-user
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-user
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kargo-viewer
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kargo-viewer
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-viewer
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kargo-api
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ''
  resources:
  - secrets
  verbs:
  - '*'
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kargo-cluster-secrets-reader
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ''
  resources:
  - secrets
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kargo-cluster-secrets-admin
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ''
  resources:
  - secrets
  verbs:
  - '*'
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kargo-controller
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
rules:
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - create
  - update
  - patch
  - delete
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kargo-shared-resources-reader
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ''
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kargo-shared-resources-admin
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ''
  resources:
  - secrets
  - configmaps
  verbs:
  - '*'
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kargo-system-resources-reader
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ''
  resources:
  - secrets
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kargo-system-resources-admin
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ''
  resources:
  - secrets
  verbs:
  - '*'
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kargo-admin
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ''
  resources:
  - secrets
  verbs:
  - '*'
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kargo-api
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kargo-api
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-api
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kargo-cluster-secrets-reader
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kargo-cluster-secrets-reader
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-controller
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-external-webhooks-server
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kargo-cluster-secrets-admin
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kargo-cluster-secrets-admin
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-api
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-admin
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kargo-controller
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kargo-controller
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-controller
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kargo-shared-resources-reader
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kargo-shared-resources-reader
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-user
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-viewer
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-project-creator
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kargo-shared-resources-admin
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kargo-shared-resources-admin
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-admin
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-controller
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-api
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kargo-system-resources-reader
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kargo-system-resources-reader
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-controller
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-external-webhooks-server
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kargo-system-resources-admin
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kargo-system-resources-admin
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-api
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-admin
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kargo-admin
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kargo-admin
subjects:
- kind: ServiceAccount
  namespace: adhar-system
  name: kargo-admin
---
apiVersion: v1
kind: Service
metadata:
  name: kargo-api
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 443
    targetPort: 8080
  selector:
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/component: api
---
apiVersion: v1
kind: Service
metadata:
  name: kargo-external-webhooks-server
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: external-webhooks-server
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 443
    targetPort: 8080
  selector:
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/component: external-webhooks-server
---
apiVersion: v1
kind: Service
metadata:
  name: kargo-webhooks-server
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: kubernetes-webhooks-server
spec:
  type: ClusterIP
  ports:
  - port: 443
    protocol: TCP
    targetPort: 9443
  selector:
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/component: kubernetes-webhooks-server
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kargo-api
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: api
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app.kubernetes.io/name: kargo
      app.kubernetes.io/instance: kargo
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        helm.sh/chart: kargo-1.10.5
        app.kubernetes.io/name: kargo
        app.kubernetes.io/instance: kargo
        app.kubernetes.io/version: v1.10.5
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/component: api
      annotations:
        configmap/checksum: 04984b4d94f2e4badad8447b2f04c967c852fe945813bed8eb108d7de215c8ee
        secret/checksum: 44136fa355b3678a1146ad16f7e8649e94fb4fc21fe77e8310c060f61caaff8a
    spec:
      serviceAccount: kargo-api
      containers:
      - name: api
        image: ghcr.io/akuity/kargo:v1.10.5
        imagePullPolicy: IfNotPresent
        command:
        - /usr/local/bin/kargo
        - api
        env:
        - name: GOMEMLIMIT
          valueFrom:
            resourceFieldRef:
              containerName: api
              divisor: '1'
              resource: limits.memory
        - name: GOMAXPROCS
          valueFrom:
            resourceFieldRef:
              containerName: api
              divisor: '1'
              resource: limits.cpu
        envFrom:
        - configMapRef:
            name: kargo-api
        - secretRef:
            name: kargo-api
        ports:
        - name: h2c
          containerPort: 8080
          protocol: TCP
        startupProbe:
          exec:
            command:
            - /usr/local/bin/grpc_health_probe
            - -addr=:8080
            - -tls
            - -tls-no-verify
          failureThreshold: 30
          initialDelaySeconds: 10
        livenessProbe:
          exec:
            command:
            - /usr/local/bin/grpc_health_probe
            - -addr=:8080
            - -tls
            - -tls-no-verify
        readinessProbe:
          exec:
            command:
            - /usr/local/bin/grpc_health_probe
            - -addr=:8080
            - -tls
            - -tls-no-verify
          initialDelaySeconds: 5
        volumeMounts:
        - mountPath: /etc/kargo
          name: config
          readOnly: true
        resources: {}
      volumes:
      - name: config
        projected:
          sources:
          - secret:
              name: kargo-api-cert
              items:
              - key: tls.crt
                path: tls.crt
              - key: tls.key
                path: tls.key
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kargo-controller
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: kargo
      app.kubernetes.io/instance: kargo
      app.kubernetes.io/component: controller
  template:
    metadata:
      labels:
        helm.sh/chart: kargo-1.10.5
        app.kubernetes.io/name: kargo
        app.kubernetes.io/instance: kargo
        app.kubernetes.io/version: v1.10.5
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/component: controller
      annotations:
        configmap/checksum: 1852d7e3589706cdcb644e009c2302b533adbcb9b31c390b528632e627b1f140
    spec:
      serviceAccount: kargo-controller
      containers:
      - name: controller
        image: ghcr.io/akuity/kargo:v1.10.5
        imagePullPolicy: IfNotPresent
        command:
        - /sbin/tini
        - --
        - /usr/local/bin/kargo
        args:
        - controller
        env:
        - name: GOMEMLIMIT
          valueFrom:
            resourceFieldRef:
              containerName: controller
              divisor: '1'
              resource: limits.memory
        - name: GOMAXPROCS
          valueFrom:
            resourceFieldRef:
              containerName: controller
              divisor: '1'
              resource: limits.cpu
        envFrom:
        - configMapRef:
            name: kargo-controller
        volumeMounts:
        - mountPath: /tmp
          name: tmp-data
        resources: {}
      volumes:
      - name: tmp-data
        emptyDir: {}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kargo-external-webhooks-server
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: external-webhooks-server
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app.kubernetes.io/name: kargo
      app.kubernetes.io/instance: kargo
      app.kubernetes.io/component: external-webhooks-server
  template:
    metadata:
      labels:
        helm.sh/chart: kargo-1.10.5
        app.kubernetes.io/name: kargo
        app.kubernetes.io/instance: kargo
        app.kubernetes.io/version: v1.10.5
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/component: external-webhooks-server
      annotations:
        configmap/checksum: f89e33c4b0037b83eaf684f1e67fc7823bb06b5c21cf63e20cf11b096aa8af19
    spec:
      serviceAccount: kargo-external-webhooks-server
      containers:
      - name: webhooks-server
        image: ghcr.io/akuity/kargo:v1.10.5
        imagePullPolicy: IfNotPresent
        command:
        - /sbin/tini
        - --
        - /usr/local/bin/kargo
        args:
        - external-webhooks-server
        env:
        - name: GOMEMLIMIT
          valueFrom:
            resourceFieldRef:
              containerName: webhooks-server
              divisor: '1'
              resource: limits.memory
        - name: GOMAXPROCS
          valueFrom:
            resourceFieldRef:
              containerName: webhooks-server
              divisor: '1'
              resource: limits.cpu
        envFrom:
        - configMapRef:
            name: kargo-external-webhooks-server
        ports:
        - containerPort: 8080
          name: https
          protocol: TCP
        livenessProbe:
          httpGet:
            scheme: HTTPS
            port: 8080
            path: /healthz
          initialDelaySeconds: 10
        readinessProbe:
          httpGet:
            scheme: HTTPS
            port: 8080
            path: /healthz
          initialDelaySeconds: 5
        volumeMounts:
        - mountPath: /etc/kargo
          name: config
          readOnly: true
        resources: {}
      volumes:
      - name: config
        projected:
          sources:
          - secret:
              name: kargo-external-webhooks-server-cert
              items:
              - key: tls.crt
                path: tls.crt
              - key: tls.key
                path: tls.key
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kargo-webhooks-server
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: kubernetes-webhooks-server
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app.kubernetes.io/name: kargo
      app.kubernetes.io/instance: kargo
      app.kubernetes.io/component: kubernetes-webhooks-server
  template:
    metadata:
      labels:
        helm.sh/chart: kargo-1.10.5
        app.kubernetes.io/name: kargo
        app.kubernetes.io/instance: kargo
        app.kubernetes.io/version: v1.10.5
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/component: kubernetes-webhooks-server
      annotations:
        configmap/checksum: b6ea12b23463342ef35587e40f7719e13ae2b932dd236f13e2b9c7554590aaec
    spec:
      serviceAccount: kargo-webhooks-server
      containers:
      - name: webhooks-server
        image: ghcr.io/akuity/kargo:v1.10.5
        imagePullPolicy: IfNotPresent
        command:
        - /sbin/tini
        - --
        - /usr/local/bin/kargo
        args:
        - kubernetes-webhooks-server
        env:
        - name: GOMEMLIMIT
          valueFrom:
            resourceFieldRef:
              containerName: webhooks-server
              divisor: '1'
              resource: limits.memory
        - name: GOMAXPROCS
          valueFrom:
            resourceFieldRef:
              containerName: webhooks-server
              divisor: '1'
              resource: limits.cpu
        envFrom:
        - configMapRef:
            name: kargo-webhooks-server
        ports:
        - containerPort: 9443
          name: webhooks-server
          protocol: TCP
        volumeMounts:
        - mountPath: /tmp/k8s-webhook-server/serving-certs
          name: cert
          readOnly: true
        resources: {}
      volumes:
      - name: cert
        secret:
          defaultMode: 420
          secretName: kargo-webhooks-server-cert
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kargo-management-controller
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: management-controller
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: kargo
      app.kubernetes.io/instance: kargo
      app.kubernetes.io/component: management-controller
  template:
    metadata:
      labels:
        helm.sh/chart: kargo-1.10.5
        app.kubernetes.io/name: kargo
        app.kubernetes.io/instance: kargo
        app.kubernetes.io/version: v1.10.5
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/component: management-controller
      annotations:
        configmap/checksum: cf2c7c4d6b4b06b177bc8d70e19dbb229aa7f4a8997efdbbf77b9d987837eaaf
    spec:
      serviceAccount: kargo-management-controller
      containers:
      - name: management-controller
        image: ghcr.io/akuity/kargo:v1.10.5
        imagePullPolicy: IfNotPresent
        command:
        - /sbin/tini
        - --
        - /usr/local/bin/kargo
        args:
        - management-controller
        env:
        - name: GOMEMLIMIT
          valueFrom:
            resourceFieldRef:
              containerName: management-controller
              divisor: '1'
              resource: limits.memory
        - name: GOMAXPROCS
          valueFrom:
            resourceFieldRef:
              containerName: management-controller
              divisor: '1'
              resource: limits.cpu
        envFrom:
        - configMapRef:
            name: kargo-management-controller
        resources: {}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: kargo-garbage-collector
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: garbage-collector
spec:
  schedule: 0 * * * *
  concurrencyPolicy: Forbid
  jobTemplate:
    metadata:
      labels:
        helm.sh/chart: kargo-1.10.5
        app.kubernetes.io/name: kargo
        app.kubernetes.io/instance: kargo
        app.kubernetes.io/version: v1.10.5
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/component: garbage-collector
      annotations:
        configmap/checksum: e419ca54357429ab3d16030d62442027b10abc3dd6f10cfeb75cc2716883256f
    spec:
      template:
        metadata:
          labels:
            helm.sh/chart: kargo-1.10.5
            app.kubernetes.io/name: kargo
            app.kubernetes.io/instance: kargo
            app.kubernetes.io/version: v1.10.5
            app.kubernetes.io/managed-by: Helm
            app.kubernetes.io/component: garbage-collector
          annotations:
            configmap/checksum: e419ca54357429ab3d16030d62442027b10abc3dd6f10cfeb75cc2716883256f
        spec:
          serviceAccountName: kargo-garbage-collector
          containers:
          - name: garbage-collector
            image: ghcr.io/akuity/kargo:v1.10.5
            imagePullPolicy: IfNotPresent
            command:
            - /sbin/tini
            - --
            - /usr/local/bin/kargo
            args:
            - garbage-collector
            env:
            - name: GOMEMLIMIT
              valueFrom:
                resourceFieldRef:
                  containerName: garbage-collector
                  divisor: '1'
                  resource: limits.memory
            - name: GOMAXPROCS
              valueFrom:
                resourceFieldRef:
                  containerName: garbage-collector
                  divisor: '1'
                  resource: limits.cpu
            envFrom:
            - configMapRef:
                name: kargo-garbage-collector
            resources: {}
          restartPolicy: Never
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kargo-api
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: api
spec:
  dnsNames:
  - adhar.localtest.me
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: kargo-selfsigned-cert-issuer
  secretName: kargo-api-cert
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kargo-external-webhooks-server
  namespace: adhar-system
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: external-webhooks-server
spec:
  dnsNames:
  - localhost
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: kargo-selfsigned-cert-issuer
  secretName: kargo-external-webhooks-server-cert
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kargo-webhooks-server
  namespace: adhar-system
spec:
  dnsNames:
  - kargo-webhooks-server.adhar-system.svc
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: kargo-selfsigned-cert-issuer
  secretName: kargo-webhooks-server-cert
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: kargo-selfsigned-cert-issuer
  namespace: adhar-system
spec:
  selfSigned: {}
---
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: kargo
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
  annotations:
    cert-manager.io/inject-ca-from: kargo/kargo-webhooks-server
webhooks:
- name: freight.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /mutate-kargo-akuity-io-v1alpha1-freight
  rules:
  - scope: Namespaced
    apiGroups:
    - kargo.akuity.io
    apiVersions:
    - v1alpha1
    resources:
    - freights
    operations:
    - CREATE
    - UPDATE
  failurePolicy: Fail
- name: promotion.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /mutate-kargo-akuity-io-v1alpha1-promotion
  rules:
  - scope: Namespaced
    apiGroups:
    - kargo.akuity.io
    apiVersions:
    - v1alpha1
    resources:
    - promotions
    operations:
    - CREATE
    - UPDATE
  failurePolicy: Fail
- name: stage.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /mutate-kargo-akuity-io-v1alpha1-stage
  rules:
  - scope: Namespaced
    apiGroups:
    - kargo.akuity.io
    apiVersions:
    - v1alpha1
    resources:
    - stages
    operations:
    - CREATE
    - UPDATE
  failurePolicy: Fail
- name: warehouse.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /mutate-kargo-akuity-io-v1alpha1-warehouse
  rules:
  - scope: Namespaced
    apiGroups:
    - kargo.akuity.io
    apiVersions:
    - v1alpha1
    resources:
    - warehouses
    operations:
    - CREATE
    - UPDATE
  failurePolicy: Fail
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: kargo
  labels:
    helm.sh/chart: kargo-1.10.5
    app.kubernetes.io/name: kargo
    app.kubernetes.io/instance: kargo
    app.kubernetes.io/version: v1.10.5
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
  annotations:
    cert-manager.io/inject-ca-from: kargo/kargo-webhooks-server
webhooks:
- name: clusterconfig.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /validate-kargo-akuity-io-v1alpha1-clusterconfig
  rules:
  - scope: Cluster
    apiGroups:
    - kargo.akuity.io
    apiVersions:
    - v1alpha1
    resources:
    - clusterconfigs
    operations:
    - CREATE
    - UPDATE
  failurePolicy: Fail
- name: clusterpromotiontask.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /validate-kargo-akuity-io-v1alpha1-clusterpromotiontask
  rules:
  - scope: Cluster
    apiGroups:
    - kargo.akuity.io
    apiVersions:
    - v1alpha1
    resources:
    - clusterpromotiontasks
    operations:
    - CREATE
    - UPDATE
  failurePolicy: Fail
- name: freight.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /validate-kargo-akuity-io-v1alpha1-freight
  rules:
  - scope: Namespaced
    apiGroups:
    - kargo.akuity.io
    apiVersions:
    - v1alpha1
    resources:
    - freights
    - freights/status
    operations:
    - CREATE
    - UPDATE
    - DELETE
  failurePolicy: Fail
- name: project.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: NoneOnDryRun
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /validate-kargo-akuity-io-v1alpha1-project
  rules:
  - scope: Cluster
    apiGroups:
    - kargo.akuity.io
    apiVersions:
    - v1alpha1
    resources:
    - projects
    operations:
    - CREATE
    - UPDATE
  failurePolicy: Fail
- name: projectconfig.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: NoneOnDryRun
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /validate-kargo-akuity-io-v1alpha1-projectconfig
  rules:
  - scope: Namespaced
    apiGroups:
    - kargo.akuity.io
    apiVersions:
    - v1alpha1
    resources:
    - projectconfigs
    operations:
    - CREATE
    - UPDATE
  failurePolicy: Fail
- name: promotion.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /validate-kargo-akuity-io-v1alpha1-promotion
  rules:
  - scope: Namespaced
    apiGroups:
    - kargo.akuity.io
    apiVersions:
    - v1alpha1
    resources:
    - promotions
    operations:
    - CREATE
    - UPDATE
    - DELETE
  failurePolicy: Fail
- name: promotiontask.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /validate-kargo-akuity-io-v1alpha1-promotiontask
  rules:
  - scope: Namespaced
    apiGroups:
    - kargo.akuity.io
    apiVersions:
    - v1alpha1
    resources:
    - promotiontasks
    operations:
    - CREATE
    - UPDATE
  failurePolicy: Fail
- name: stage.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /validate-kargo-akuity-io-v1alpha1-stage
  rules:
  - scope: Namespaced
    apiGroups:
    - kargo.akuity.io
    apiVersions:
    - v1alpha1
    resources:
    - stages
    operations:
    - CREATE
    - UPDATE
  failurePolicy: Fail
- name: warehouse.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /validate-kargo-akuity-io-v1alpha1-warehouse
  rules:
  - scope: Namespaced
    apiGroups:
    - kargo.akuity.io
    apiVersions:
    - v1alpha1
    resources:
    - warehouses
    operations:
    - CREATE
    - UPDATE
  failurePolicy: Fail
- name: replicated-resource-mutate.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /validate-v1-replicated-resource
  rules:
  - scope: Namespaced
    apiGroups:
    - ''
    apiVersions:
    - v1
    resources:
    - secrets
    - configmaps
    operations:
    - CREATE
    - UPDATE
  objectSelector:
    matchExpressions:
    - key: kargo.akuity.io/replicated-from
      operator: Exists
  namespaceSelector:
    matchExpressions:
    - key: kargo.akuity.io/project
      operator: Exists
  failurePolicy: Fail
- name: replicated-resource-delete.kargo.akuity.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  clientConfig:
    service:
      namespace: adhar-system
      name: kargo-webhooks-server
      path: /validate-v1-replicated-resource
  rules:
  - scope: Namespaced
    apiGroups:
    - ''
    apiVersions:
    - v1
    resources:
    - secrets
    - configmaps
    operations:
    - DELETE
  objectSelector:
    matchExpressions:
    - key: kargo.akuity.io/replicated-from
      operator: Exists
  namespaceSelector:
    matchExpressions:
    - key: kargo.akuity.io/project
      operator: Exists
  failurePolicy: Ignore
