---
# Kind and On-Premises Kubernetes Cluster Composition
# Manages existing Kubernetes clusters (Kind, on-prem) via provider-kubernetes /
# provider-helm. Reads from spec.parameters.* per the CompositeCluster XRD schema,
# and targets the shared kubernetes-provider / helm-provider ClusterProviderConfigs.
apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: kind-kubernetes
  labels:
    feature: cluster
    provider: kind
    cluster.adhar.io/managed: "false" # existing clusters
    cluster.adhar.io/type: development
spec:
  compositeTypeRef:
    apiVersion: platform.adhar.io/v1alpha1
    kind: CompositeCluster

  mode: Pipeline
  pipeline:
    - step: render-templates
      functionRef:
        name: function-go-templating
      input:
        apiVersion: gotemplating.fn.crossplane.io/v1beta1
        kind: GoTemplate
        source: Inline
        inline:
          template: |
            {{- $xr := .observed.composite.resource -}}
            {{- $name := $xr.metadata.name -}}
            {{- $params := dig "spec" "parameters" dict $xr -}}
            {{- $type := dig "environmentType" "development" $params -}}
            {{- $addons := dig "addons" (list) $params -}}
            {{- $k8sProvider := dig "spec" "providerConfigRef" "name" "kubernetes-provider" $xr -}}
            {{- $helmProvider := dig "spec" "helmProviderConfigRef" "name" "helm-provider" $xr -}}
            ---
            # ConfigMap recording cluster metadata. A native, namespaced resource
            # composed directly (Crossplane v2): it lives in the XR's own
            # namespace. (A Namespace cannot be composed here — namespaced XRs
            # may not own cluster-scoped objects — and every composed resource
            # must carry the composition-resource-name annotation, plus an
            # explicit ready marker for kinds that have no status conditions.)
            apiVersion: v1
            kind: ConfigMap
            metadata:
              name: {{ $name }}-info
              namespace: {{ $xr.metadata.namespace }}
              annotations:
                gotemplating.fn.crossplane.io/composition-resource-name: cluster-info
                gotemplating.fn.crossplane.io/ready: "True"
              labels:
                cluster.adhar.io/name: {{ $name }}
                cluster.adhar.io/provider: kind
            data:
              clusterName: {{ $name }}
              provider: kind
              type: {{ $type }}
              region: local
            ---
            # Object to verify cluster connectivity via the shared kubernetes ClusterProviderConfig
            apiVersion: kubernetes.m.crossplane.io/v1alpha1
            kind: Object
            metadata:
              name: {{ $name }}-healthcheck
              annotations:
                gotemplating.fn.crossplane.io/composition-resource-name: healthcheck
            spec:
              providerConfigRef:
                name: {{ $k8sProvider }}
                kind: ClusterProviderConfig
              forProvider:
                manifest:
                  apiVersion: v1
                  kind: ConfigMap
                  metadata:
                    name: {{ $name }}-healthcheck
                    namespace: kube-system
                    labels:
                      managed-by: adhar
                  data:
                    status: healthy
            {{ if has "cilium" $addons }}
            ---
            apiVersion: helm.m.crossplane.io/v1beta1
            kind: Release
            metadata:
              name: {{ $name }}-cilium
              annotations:
                gotemplating.fn.crossplane.io/composition-resource-name: cilium
            spec:
              providerConfigRef:
                name: {{ $helmProvider }}
                kind: ClusterProviderConfig
              forProvider:
                chart:
                  name: cilium
                  repository: https://helm.cilium.io/
                  version: "1.15.0"
                namespace: kube-system
                values:
                  operator:
                    replicas: 1
                  hubble:
                    enabled: true
                    relay:
                      enabled: true
                    ui:
                      enabled: true
            {{ end }}
            {{ if has "metrics-server" $addons }}
            ---
            apiVersion: helm.m.crossplane.io/v1beta1
            kind: Release
            metadata:
              name: {{ $name }}-metrics-server
              annotations:
                gotemplating.fn.crossplane.io/composition-resource-name: metrics-server
            spec:
              providerConfigRef:
                name: {{ $helmProvider }}
                kind: ClusterProviderConfig
              forProvider:
                chart:
                  name: metrics-server
                  repository: https://kubernetes-sigs.github.io/metrics-server/
                  version: "3.11.0"
                namespace: kube-system
                values:
                  args:
                    - --kubelet-insecure-tls
                    - --kubelet-preferred-address-types=InternalIP
            {{ end }}

    - step: automatically-detect-ready-composed-resources
      functionRef:
        name: function-auto-ready
