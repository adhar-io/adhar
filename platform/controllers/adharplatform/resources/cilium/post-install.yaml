# Cilium post-install resources
# Note: Hubble UI cannot be served under a sub-path (SPA with hardcoded <base href="/">).
# Access Hubble UI via: kubectl port-forward -n adhar-system svc/hubble-ui 12000:80
