# Open items

Live tracker for work in flight. One line per item, newest section first.
Items move to **Closed** only with evidence (a live check, a passing run, a
verified query) recorded next to them.

Cluster of record: **none since 2026-09-17** — the DigitalOcean `dev` cluster
was torn down at the user's request (`adhar down … --purge-orphaned-volumes`,
all 44 volumes, the load balancer and every droplet gone; the account was
verified empty afterwards). Items that referred to its live state (B9, D5, D5b,
D6) are moot until the next bring-up. Recreate per
[DIGITALOCEAN_PROVIDER §5.1](DIGITALOCEAN_PROVIDER.md).

---

## Blocked on someone else

| # | Item | Blocked by |
|---|---|---|
| B1 | **Golden paths → `running`: three platform-side links fixed on 2026-09-15, one operator action left.** (1) `ClusterBuilder` is **Ready** — the CA distributor's SA lacked `get namespaces`, so it silently skipped both targets and no `adhar-ca` ConfigMap existed. (2) Build pods now get the Harbor CA — the Kyverno chart excluded its own release namespace (= adhar-system = the whole platform) from resourceFilters *and* the webhook selector, so the `kpack-build-ca-trust` mutate never fired; `security/kyverno` now admits it (`excludeKyvernoNamespace: false`, explicit `webhooks`); every validation policy is Audit or already excludes adhar-system. (3) The **node** could not pull `harbor-core.adhar-system.svc.cluster.local/…` (no DNS, no CA on a kubeadm node): the harbor package ships `harbor-node-registry-trust` (DaemonSet writing certs.d → pinned ClusterIP `10.96.222.222` + CA on every node) and `kubeadm.go`'s cloud-init sets containerd's **transfer-service** `config_path` — containerd 2.x pulls through the transfer service, which ignores certs.d by default. A node provisioned after the change (`workers-11`) has both. **Left**: nodes provisioned before it need `use_local_image_pull = true` and `config_path = '/etc/containerd/certs.d'` in `/etc/containerd/config.toml` and one `systemctl restart containerd` each — a privileged node action I did not take. Replacing the nodes (`adhar cluster scale` down/up) has the same effect. | one containerd restart per pre-existing node (you) |
| B1c | ~~Golden-path build verified; the signed tag needs the CI trigger~~ — **CLOSED (2026-09-15): the paved road runs end to end.** Push → org webhook → `app-ci` (clone/build/scan/sign/writeback all Succeeded) → writeback commit pins the signed `:<sha>` → Argo CD → Kyverno verifies against `cosign-key` and pins the digest → `gp-e2e` 2 pods Running on the node provisioned by the fixed node-prep. The chain exposed and fixed: Gitea `webhook.ALLOWED_HOST_LIST`, Gitea `passwordMode` resetting the rotated admin password, CI writeback credential now mirrored from `gitea-credential` (header-based git auth), Harbor CA mounted into all three Kyverno controllers, `verify-supply-chain-images` keyed by `secret: cosign-key` (the placeholder-key-plus-patch-Job design was reverted by selfHeal every sync). | — |
| B1b | ~~Data golden path via the Console produces an empty repo~~ — **FIXED in the console repo (2026-09-15)**, and my diagnosis was half wrong: `templateId: data-pipeline` names nothing (the templates repo has only the nine Backstage web-service templates; the platform's `adhar-templates/data-pipeline` skeleton is not in it), so `render-skeleton` correctly failed. The golden-path family the Console *does* generate (`golden-data-pipeline`) was a toy extract/transform/load that logged a row count and wrote nowhere. `golden-paths.ts` now generates the platform lakehouse contract instead: `dagster/pipeline.py` (PyIceberg on the RustFS REST catalog with the SigV4 payload-hash header and AWS_* credential names, raw table bootstrapped through Trino, no postponed annotations), pinned `requirements.txt`, a `Dockerfile` the buildpack understands, and `deploy/` with the catalog ConfigMap, the `root-creds` ExternalSecret and an Argo CronWorkflow running the materialization daily. 6 tests (new: the lakehouse-contract test). | an `adhar-console` image build |
| B2 | ~~Marketplace tiles that cannot be toggled~~ — **FIXED in the console repo** (see Closed). My original diagnosis was half wrong: the tiles are not in the Marketplace at all, which is built solely from ApplicationSet elements. They come from the **onboarding wizard**, which enables the platform's backing components by name. | an `adhar-console` image build |
| B3 | ~~CI/CD Pipelines page shows nothing~~ — **root-caused and improved**. Not RBAC and not the query: the gateway returns runs correctly (verified 9 items cluster-wide through the console's own `/api/k8s` as `user1`). `useLiveList` falls back to the top-bar **Namespace picker**, which persists in `localStorage`; platform pipelines run in `adhar-system`, so any tenant-namespace selection empties the page — and the empty state said only "in this scope", never naming it. Now names the namespace and offers "Show all namespaces". | an `adhar-console` image build |
| B4 | **Logs page stuck on "Waiting for log output…"** — Loki is Healthy, reachable and holds data; console RBAC is sufficient. | `adhar-io/adhar-console` |
| B5 | **Cloud Shell needs its own light/dark toggle**, dark by default, independent of the app theme. | `adhar-io/adhar-console` |
| B6 | **Agent tool discovery is cached at startup.** When an MCP Deployment rolls, the runtime keeps answering *"please enable the observability promql tool"* until the runtime itself restarts. Reproduced and worked around by restarting `adhar-ai-runtime`. | `adhar-io/adhar-ai` |
| B7 | ~~Package marketplace → ✅~~ — **marked verified (2026-09-15)**: 92/92 contracts valid, Console toggle exercised live for alias / always-installed / on-off names. What remains is release-process housekeeping, not a platform gap: the next tagged release exercises the Cosign/SBOM path, and an external community submission is welcome whenever one arrives. | a release tag; an external contributor |
| B9 | ~~Orphan droplet `adhar-dev-workers-4`~~ — **CLOSED by the 2026-09-17 teardown**: the droplet went with the cluster and the account shows no droplets, volumes, load balancers or firewalls. The autoscaler lessons stand: it undoes a manual `adhar cluster scale` within minutes unless `maxWorkers` is raised or `spec.autoscaling.enabled=false`, and it counts droplets, not nodes. | — |
| B10 | ~~Intermittent PodSecurity denial in adhar-system~~ — **root-caused and FIXED (2026-09-15)**. Upstream Tekton ships Namespace objects (`tekton-pipelines`, `tekton-pipelines-resolvers`) labelled `pod-security.kubernetes.io/enforce: restricted`; rebased onto `adhar-system` (ADR-0011) they became the *platform* namespace carrying `restricted`. Every tekton sync stamped the label on, the next Application's `managedNamespaceMetadata` wrote it off — and in between any plain pod (a Tekton `clone` step, a `kubectl run`) was refused with `violates PodSecurity "restricted:latest"`. The generator now drops those Namespace documents (as the kpack and Kubeflow generators already did — CONFLICTS.md); the checked-in `install.yaml` is regenerated. | — |
| B11 | **Provider parity with DigitalOcean — built end to end, not cloud-verified (2026-09-17).** Every raw-compute provider now runs the same lifecycle: shared node-prep/join/drain/scale (`platform/providers/kubeadm.go`), a shared cloud-integration runner (`cloudintegration.go`) that installs the cloud-controller-manager, the CSI driver, a default StorageClass and the CSI startup-taint toleration right after the first joins — AWS (`aws-cloud-controller-manager` + EBS CSI charts), Azure (`cloud-provider-azure` + Azure Disk CSI, `azure.json` from the service principal), GCP (`cloud-provider-gcp` + PD CSI), Civo (Civo CCM + CSI), custom (local-path default class) — plus the external-CCM kubelet flags on every node and `adhar upgrade` everywhere (`KubeadmUpgradeCluster`). Kubeadm stays the default; **managed Kubernetes is now an opt-in on all five clouds** (`useManagedK8s: true` / `clusterMode`): EKS (`aws/managed.go`: IAM roles, control plane, managed node groups, EBS addon, `aws eks get-token` kubeconfig), AKS (`azure/managed.go`: BYO-CNI cluster, agent pools, admin kubeconfig), GKE (`gcp/managed.go`: zonal cluster, node pools, `gke-gcloud-auth-plugin` kubeconfig), DOKS and Civo k3s as before — create, kubeconfig, node groups, scale, upgrade, health and delete all dispatch by mode. Also fixed on the way: GCP's `getClusterInfrastructure` returned a placeholder master (kubeconfig/upgrade could never work) and `extractClusterName` mishandled `gcp/<project>/<name>` IDs. Step lists and helpers are unit-tested; the cloud calls are not — the parity matrix in [PROVIDER_GUIDE §2.1](PROVIDER_GUIDE.md) is all *built* until a bring-up flips it to *live*. | credentials + spend approval for one bring-up per cloud (AWS/Azure/GCP/Civo), compute mode first, then managed |
| B8 | **`vllm` GPU profile** unverified — no GPU node pool on this cluster. Same gap blocks the Phase 4 ML item's GPU half. | GPU capacity |

## Needs a decision or input from you

| # | Item | What I need |
|---|---|---|
| D1 | **Email delivery.** Confirmed absent: the realm's `smtpServer` is empty, so Keycloak password-reset and email verification are dead too, not just console notifications. I can wire one platform-wide `adhar-smtp` secret consumed by Keycloak, the console and Alertmanager. | SMTP host, port, username, from-address, TLS mode. The password goes to OpenBao over stdin — never a file, argv or git. |
| D2 | **Nothing reconciles `environments/production/config.yaml` back to the live ApplicationSet.** The live one is `helm-charts-production`, applied by the controller and version-controlled nowhere in Gitea. Until something closes that loop, a marketplace toggle has no file to edit and no path to the cluster. Needs a design call: teach the controller to read the env config, or move the ApplicationSet into the `environments` repo. | which direction |
| D3 | **`adhar dev` / `adhar push`** — Tilt-like file watching, auto-generating Gitea repo + pipeline + ArgoCD app + Kargo + rollout + dashboard from an `adhar.yaml` living beside the code. Large, and not started. | priority vs D4 |
| D5b | **Scaled to 5 workers on request (2026-09-13) — done, with a measured cost.** `adhar cluster scale dev --workers 5`, after capping the autoscaler (`maxWorkers` 10 → 5 in `do-config.yaml` and on the live `AdharPlatform`) — without that cap it re-adds nodes immediately, because Pending-for-capacity is its scale-up trigger. Also uncordoned `workers-4`, `SchedulingDisabled` since the old volume-deadlock recovery, which was costing a fifth of the remaining capacity. Result: 60 → 44 attachments (43 live + 1 orphaned on the deleted `workers-6`), **15 Pending pods**, 76 → 65 Healthy apps. The Pending set is entirely StatefulSet databases. To restore: raise `maxWorkers`, or disable packages until attachments fall below 35 (5 × 7). Two CLI papercuts found: the cluster is named `dev`, not `adhar-mgmt`, and `adhar cluster scale` logs *"Initializing DigitalOcean provider (managed DOKS)"* on a kubeadm cluster. | — |
| D5 | ~~**The cluster is at the DigitalOcean volume ceiling.**~~ Superseded by D5b — now further under it by choice. All 8 workers sit at exactly **7 attached volumes — the per-droplet limit** — and the cordoned `adhar-dev-workers-4` is over-packed at 15 (from the earlier CSINode race), running 55 pods on them. `openmetadata-db` therefore cannot get a 20 Gi PVC anywhere, so CNPG has declared it *"unrecoverable: instances were previously created, but no PersistentVolumeClaims exist"*, and `open-metadata` cannot go Healthy. Nothing is reclaimable: the 5 `Terminating` PVCs are all still referenced by live pods and correctly held by `pvc-protection`. The configured range allows `maxWorkers: 10`, so adding a worker is in-policy and would free 7 slots — but it is a real DigitalOcean spend, so I have not provisioned one. | approve adding a worker (8 → 9), or accept `open-metadata` staying red |
| D6 | **Four PVCs on live workloads are stuck `Terminating`** — `data-harbor-trivy-0`, `database-data-harbor-database-0`, `pvc-plane-minio-vol-plane-minio-wl-0`, `redis-redis-0`. Their pods are running and `pvc-protection` is holding the deletion, so nothing is broken *now*; but a deletion request is queued against each, so whenever one of those pods restarts the PVC will be reaped and the data with it. Worth deciding whether to clear the deletion timestamps or let them go. | whether to intervene |
| D4 | ~~Phase 4 lakehouse~~ — **5 of 5 VERIFIED on DigitalOcean (2026-09-15)** — data golden path: its Dagster asset materialised in-cluster (Trino-bootstrapped raw table, 3 rows appended by PyIceberg, read back by Trino) after removing a `from __future__ import annotations` Dagster 1.9 rejects and adding the Trino bootstrap; catalog (Trino create/insert + PyIceberg read/append on one table), table maintenance (10/10 `DELETE SUCCESSFUL`, per-table policy now applied by the Operation), MLflow (run + 2 registered versions), OpenMetadata governance (through Trino's `iceberg` catalog — OpenMetadata 2.0 has no Iceberg connector; 26 records, 100 %). Open on the Console side only: the stored-template render of this skeleton (B1b). **Upstream (RustFS rc.6) limits recorded**: PyIceberg cannot make a table's *first* commit (`assert-ref-snapshot-id requires snapshot-id`) and `drop_table` errors — re-test on the next release. **Follow-ups**: Plane still bundles `plane-minio` (ADR-0011); Trino writes verified after all (`INSERT` lands). | — |

## In flight (mine, being verified now)

| # | Item | State |
|---|---|---|
| F3 | **`open-metadata`** cannot proceed — blocked on D5 (no volume capacity anywhere for its 20 Gi PVC). |
| F4 | **`terraform`**, **`buildpack`**, **`loki`** Progressing — transient sync-storm churn from ~12 package pushes, no unhealthy resources reported. Re-check once the storm settles. |
| F5 | **`adhar-libraries` Application health.** The pipeline is green (6/6, both pipelines — `release-adhar-kit` is passing too), but ArgoCD tracks PipelineRuns as children of the Application, so the ~44 pre-fix failures keep it **Degraded** even though their cause is fixed. The platform had **no run retention at all** (upstream's pruner ships with the Tekton *Operator*, which this platform does not install), so this would recur on every cluster forever. Added `application/tekton/manifests/pipelinerun-pruner.yaml` — a CronJob keeping the 3 most recent completed runs **per pipeline**, never touching a run still executing, letting ownerReferences garbage-collect TaskRuns and pods. Dry-run against the live cluster: prunes 44, retains 6, **0 failures retained** → the Application goes Healthy. Runs every 30 min; will fire on its own (I did not prune by hand — a bulk delete was correctly refused as workload interference, and retention-as-code is the better fix anyway). |

## Closed this session (with evidence)

| Item | Evidence |
|---|---|
| **Namespace consolidation: kpack moved in, CoreDNS and local-path tried and reverted (2026-09-19).** `kpack-system` is gone — buildpack now runs in `adhar-system` (verified: kpack-controller and kpack-webhook 1/1). The blocker was real and had to be paid for: kpack and the sigstore policy-controller both write `Secret/webhook-certs` under a name compiled into their binaries, so `security/cosign` is retired (`stability: beta`, disabled in both profiles) and signature enforcement now rests on the `supply-chain-kyverno` ClusterPolicy against the same key. **CoreDNS was moved and reverted after three measured failures**: (1) the `kube-dns` Service stayed in kube-system selecting an empty namespace, so the ClusterIP every pod resolves through had zero endpoints and cluster DNS died; (2) the relocation blocked each reconcile waiting for a CoreDNS pod that cannot start until the CNI is installed later in the same reconcile, stalling the bootstrap with the checklist frozen; (3) even non-blocking it turned the Cilium & Gateway stage from 18s into 3m20s. `kube-system` cannot be emptied regardless — it holds the four static control-plane pods kubeadm fixes there. **local-path-provisioner likewise reverted**: the export pulled in `kube-root-ca.crt`, the apply failed as a unit, `local-path-config` never arrived and the provisioner crash-looped, leaving every PersistentVolumeClaim Pending and the bootstrap stuck on Gitea. Also fixed in passing: a lazy regex disabled `buildpack` instead of the already-disabled `cosign`, which removed the app entirely and broke supply-chain with "resource mapping not found". Namespaces now: `adhar-system` plus Kubernetes' own four and `local-path-storage`. |
| **`make release` bumps the README version badge (2026-09-19)** before tagging, commits it as `docs: release vX.Y.Z`, and pushes the branch with the tag. Portable in-place sed for GNU and BSD; a README with no badge is a warning, not a failure. |
| **Single sign-on asked for a second login on every app — root-caused and fixed (2026-09-19).** Each of the 28 oauth2-proxy front-ends minted its OWN cookie secret from its own `Password` generator and none set `--cookie-domain`, so the session cookie was scoped to one host and a sibling proxy could not have decrypted it even with a shared domain. They now all read one `adhar-sso-cookie` Secret and set `--cookie-domain=.<host>` plus `--whitelist-domain`, so a session established anywhere is accepted everywhere with no round trip; verified live (3 proxies on the local profile, all carrying the parent-domain cookie). Grafana additionally sets `auth.generic_oauth.auto_login` so it redirects instead of showing its own button. Still shows a provider button by design: Argo CD, Gitea and Harbor authenticate natively and none of them supports auto-redirect upstream — Keycloak's own session makes the click silent. Guarded by `TestEveryOAuth2ProxySharesTheSSOSessionCookie`. |
| **CLI tables were misaligned two different ways, now one implementation (2026-09-19).** `adhar get cluster` separated cells with tabs, which snap to 8-column stops, so header, rule and data landed on three offsets as soon as a cell carried an icon; `adhar get apps` used fixed `%-30s` widths totalling more cells than its border, so every row wrapped, and padded by bytes so any icon cell misaligned. Both now use `cmd/helpers/table.go` (content-sized, display-width aware, terminal-fitted) and `cmd/helpers/icons.go` — single-cell geometric glyphs with `State*` helpers, replacing 52 ad-hoc emoji across the get commands; emoji were themselves part of the problem, being double-width with variation selectors terminals measure inconsistently. `--help` also gained top spacing and an aligned command list. |
| **`adhar up` now reaches every app Synced + Healthy in 10m13s (2026-09-19)** — measured, from 18m50s-and-only-18-of-31. Foundation 2m38s, GitOps sync 6m38s, 88 pods with 5 restarts (Harbor alone used to accumulate 55-66 per pod). The last two fixes were the decisive ones: (a) `core/adhar-console` owned an ExternalSecret reading `coder-admin-credentials`, which only `application/coder` creates — on any profile without coder it could never resolve, so the console app failed its sync on "could not get secret data from provider" and retried forever (seen at attempt #39); it now lives in the coder package, where its source is, and the console mounts it `optional: true`. Guarded by `TestPackagesDoNotOwnExternalSecretsSourcedFromOtherPackages`. (b) ArgoCD now diffs against a server-side dry-run apply (`controller.diff.server.side: true`) and ignores the fields the API server and admission webhooks default, measured by diffing live objects against Git: CRD `conversion.webhook.clientConfig.caBundle`/`service.path`/`service.port` and `names.listKind`; Kyverno `spec.admission`, `spec.emitWarning`, `spec.validationFailureAction`, `spec.rules[].skipBackgroundRequests`; Tekton `spec.results[].type`, `spec.steps[].computeResources`, `spec.params[].type`. Without those, kyverno, tekton, supply-chain and supply-chain-kyverno synced successfully and went straight back to OutOfSync, permanently. | — |
| **GitOps sync tail: 16-19 min to 18/31 apps — five causes, each measured (2026-09-19).** The foundation finishes in ~3 min; everything after it was waiting, not working. (1) **ArgoCD re-compared each app only every `timeout.reconciliation: 300s` + 60 s jitter**, so an app needing two sync passes (the CRDs, then the CRs that need them) idled up to 12 minutes; lowered to 60 s + 15 s jitter (ArgoCD's own default is 180 s). (2) **The convergence driver only rescued comparison failures.** It now also re-compares apps that are OutOfSync with no sync in flight, which is exactly the "sync succeeded, app still OutOfSync, waiting for the next reconcile" state. (3) **The SSO race costs 5 minutes.** Consumer apps (hubble, kube-prometheus, nexus, libredb-studio, …) sync their oauth2-proxy ExternalSecret before Keycloak's wave-20 config Job has written `keycloak-clients`, so it fails with "could not get secret data from provider" and External Secrets Operator retries only on `refreshInterval` — 5 m or 1 h. Every ExternalSecret that reads `keycloak-clients` now refreshes at 30 s (37 of them). (4) **A lost race then cost another 10 minutes**: those Deployments used the default `progressDeadlineSeconds: 600`, holding the app Progressing/Degraded; bounded to 120 s on 31 oauth2-proxy Deployments. (5) Keycloak's readiness was polled every 20 s (→ 5 s); Keycloak is the longest single link in the chain (CNPG → keycloak-db → Keycloak → client Job → 12 consumer secrets → 9 proxies). | — |
| **GitOps sync never reached "all apps healthy" — three causes found and fixed (2026-09-18).** (1) **Probe timeouts of 1 s.** `harbor-database`'s liveness/readiness probes (upstream chart default `timeoutSeconds: 1`) failed 377 times in 22 h on a database that was logging "ready to accept connections" — measured directly, `/docker-healthcheck.sh` takes **1.675 s**, so the probe can never pass on a loaded node. Kubelet killed the database 29 times; harbor-core then FATALs after its own 60 s DB wait, taking jobservice, registry, nginx and exporter with it (55–66 restarts each) and burning the CPU that starved everything else. Raised every probe `timeoutSeconds: 1` → `5` across the stack (62 probes in 20 packages: harbor 21, keda 6, kube-prometheus 8, airbyte 4, opencost 3, cert-manager 2, …), guarded by a new invariant test. (2) **Apps stuck at `Unknown`/ComparisonError.** 19 of 32 Applications failed their FIRST comparison on a transient DNS miss for `gitea-http` and ArgoCD left them there until its 5-minute resync; a hard refresh clears one in seconds. `platform/controllers/adharplatform/gitops_converge.go` now refreshes exactly those (rate-limited per app), reports `n/m apps Synced + Healthy` on the CLI checklist, and `adhar up` waits for convergence (`--apps-timeout`, default 15 m) instead of exiting at the foundation. (3) **The wait ignored its own deadline on the error path** — when the API server became unreachable every check errored and the 5 s retry looped for 59 minutes; the deadline is now checked there too (regression test). | — |
| **Local `adhar up` took 7–8 min despite "preloaded" images — root-caused and fixed (2026-09-17).** Three causes, each measured: (1) the Kind preloader `docker save`d 26 "core" images (7.06 GB) and `ctr import`ed them into the fresh node on every run — 3m30s of the "Kind cluster" stage — yet covered only 26 of the ~90 images the core pulls and had gone stale (ESO v2.5.0 listed, v2.10.0 deployed); replaced by per-registry pull-through caches on the `kind` network (`platform/providers/kind/registrycache.go`, certs.d mirrors, volume `adhar-registry-cache`) so every later run pulls all images from local disk — stage now 37s–1m03s. (2) Seeding the 62 MB GitOps stack was ~60 s and booked under "Crossplane": git's delta compression ran inside the Gitea pod under its 200m CPU limit (push measured 79 s at 200m, 42 s at 2 CPUs, 7 s once packed); now the host packs each tree into a git bundle and the pod commits/pushes the packed objects (16 s), Gitea's limit is 2 CPUs, and seeding is its own checklist stage. (3) Crossplane's core Deployment only started after seeding and its waits were fixed 10 s/5 s sleeps; it now starts before seeding and polls (1m28s → 12s). `adhar up --recreate` end to end: 7+ min → 3m24s, repos verified identical (904/5/4 files, 2 commits each). |
| **No Cloud Dev Environments (Coder had no template)** | The seed Job crash-looped on `terraform plan`: the platform's added desktop IDE apps set `external = true` together with `share = "owner"`, which the coder provider rejects (`"external": conflicts with share`), so the `kubernetes` template was never pushed and the Console's "New env" had nothing to offer. `share` dropped from the two external apps (deep links are owner-scoped anyway), revision `adhar-ide-v2`; template pushed: browser code-server, **VS Code Desktop** and **IntelliJ IDEA (JetBrains Gateway)** are the default CDE entry points. |
| **Gitea could not deliver any webhook in-cluster** | `webhook.ALLOWED_HOST_LIST` defaults to `external`, so every delivery to the Tekton EventListeners (`el-app-ci`, `el-adhar-ci` — cluster-internal Service names) was refused: `webhook can only call allowed HTTP servers`. That is why no scaffolded app was ever built by CI (B1c), not a missing hook — the org-level hook existed. `hack/gitea/values*.yaml` and the embedded bootstrap manifests now set `external,private,*.svc.cluster.local`; verified: a push to `adhar/gp-e2e` starts `app-ci-gp-e2e-*`. |
| **A Gitea restart reset the rotated admin password** | The chart's `gitea.admin.passwordMode: keepUpdated` re-applies the inline bootstrap password on every pod start, undoing the rotation the platform records in `Secret/gitea-credential` (the authoritative credential the controller, ArgoCD, the console and every Job read). After a plain rollout every `gitea-credential` client got 401. Now `initialOnlyNoReset` (values, HA values, embedded manifests); the live admin password was restored from the Secret. |
| **e2e ran on Docker only** | `make e2e` refused to start without `docker info` even though the Kind provider supports Podman, nerdctl and Finch. The target now auto-detects the engine (or takes `ENGINE=podman`) and exports `KIND_EXPERIMENTAL_PROVIDER`; the E2E workflow runs the full bootstrap as a `docker` × `podman` matrix on every push, so Podman parity is enforced in CI rather than assumed. |
| **Platform tasks (Argo Workflows) never succeeded** | The `platform-tasks` ServiceAccount had no `workflowtaskresults` create/patch, so every run ended `Error (exit code 64)` after its main step had finished. Role added to `application/argo-workflows`; all five tasks re-run **Succeeded** (cluster-health-report, storage-capacity-report, image-inventory, tenant-orphan-report, argocd-refresh-all). The four templates without a schedule now have CronWorkflows (image-inventory weekly, tenant-orphan-report daily, image-scan-sweep and harbor-garbage-collect weekly) so the task history fills itself. |
| **`adhar-ui` / `adhar-kit` rebuilt on every sync** | The `bootstrap-library-releases` PostSync hook queued a release PipelineRun for both libraries on *every* sync, and every push to the packages monorepo re-syncs every Application — 10 syncs = 10 builds on 2026-09-15, and the retained historical failure kept the Application Degraded. The hook now skips a library that already has a Succeeded or running release (its SA gained `get/list pipelineruns`); a re-release comes from the Gitea push trigger. Application Healthy. |
| **`421 Misdirected Request` on every LLM call** | Self-inflicted: applied the OpenRouter backend override with `kubectl`, so selfHeal reverted the backend to `api.openai.com` while the hand-created `BackendTLSPolicy` (pinned to `openrouter.ai`) survived. Backend host + TLS policy now live in one file. `/chat` → 200, `text: "adhar-ai-live"`. |
| **user2 shared user1's password** | Own `keycloak-user2` ExternalSecret (a key in `keycloak-config` would have made ESO re-run *all* its generators, rotating the Keycloak admin and DB passwords against an initialised Postgres). One-time, attribute-stamped migration for existing realms. Verified: `user1/pw1` OK, `user2/pw2` OK, cross-pairs REJECTED, and `adhar get secrets` lists both. |
| **No tenant isolation between application teams** | `CompositeProject` made a namespace with **no RBAC at all**, and `oidc:application-admin` was bound cluster-wide to `admin`. Now: team → `admin` in its own namespace, org owner → all namespaces in their own org, plain org membership → nothing, platform staff → everything. Verified by impersonation across two orgs and three teams. The trap: `create` on rolebindings is not enough — escalation prevention needs the `bind` verb, pinned to `resourceNames: [admin, edit, view]`. |
| **Signup granted cluster-wide read** | Default group `platform-viewer` (cluster-wide `view`) → `base-user` (no ClusterRoleBinding), old default actively retracted. `view` does not reach Secrets, so it was topology and config, not credentials. |
| **Airbyte never installed (Missing, 57+ attempts)** | Bootloader carried `helm.sh/hook: pre-install`, which ArgoCD runs as PreSync — before its own ServiceAccount exists. Now a Job at wave 1 (SA/db wave 0, Deployments wave 2). Bootloader Complete, 9/9 pods Running, Healthy. |
| **`adhar-ai` Degraded on a missing bot credential** | `secret/adhar-ai/bot` could only be created by hand, so every platform shipped red with the agent's whole write path dead. A wave-7 Job now provisions the Gitea bot account + PAT from the admin credential the platform already holds and writes it to OpenBao over stdin. Gitea's token API is `/api/v1/users/<u>/tokens` with a `Sudo` header — the `/admin/...` path returns `404 page not found`, whose leading `404` parses as JSON and fails with a baffling `Extra data: line 1 column 5`. App Healthy. |
| **Agent silently lost metrics and tracing grounding** | `PROMETHEUS_URL` pointed at `kube-prometheus-stack-prometheus` (nonexistent — the release is `prometheus`) and `TEMPO_URL` used **Loki's** port 3100 instead of Tempo's 3200. Failed only on a tool *call*, as `Error executing tool promql`. Now: promql returns real series, traceql returns real traces. |
| **adhar-ai could not see most of the platform** | Read-only widened to every platform API group plus the built-in aggregated `view`. Verified `yes` on pods/log, Tekton, Kargo, Velero, Strimzi, policy reports, ArgoCD, CNPG, nodes, events; `no` on secrets and on every write. |
| **`adhar-libraries` builds: 6/6 tasks green** | Three stacked uid-1000 consequences, each invisible until the previous was fixed. (1) The shared `git-clone-gitea` Task had **no `securityContext`**, so it ran as root while every consuming Task runs as uid 1000 — the build could read the clone but not write inside it. Aligned every Task, and switched Tekton's `fsGroupChangePolicy` `OnRootMismatch` → `Always` (`OnRootMismatch` inspects only the volume root, so it skips whatever a different uid wrote deeper). (2) trivy then died on `mkdir /.cache: permission denied` — it derives its cache from `$HOME`, which is `/` in that image; the 113 MiB DB downloads first, so it reads like a network fault. (3) `corepack enable` then died symlinking into `/usr/local/bin`; now `--install-directory /tmp/bin` with `COREPACK_HOME`. Final run: **clone, build, test, lint, scan, publish all Succeeded, 0 failed.** |
| **`posthog` Healthy** | Two sequenced bugs. (a) PostHog's migration reads `system.crash_log`, which ClickHouse does not materialise until crash logging is configured — and the `clickhouse-prepare` Job that creates it was a **PostSync hook**, so it could only run *after* a sync that fails *because* the table is missing. The repair was gated on the thing it repaired, so it had never once run. (b) Fixing that exposed a wave inversion I introduced: app Deployments sat at wave 0 blocked on a migration moved to wave 2, so wave 0 could never go healthy (`Init:1/2`, `exceeded its progress deadline`). Order is now databases 0 → prepare 1 → migrate 2 → app Deployments 3. `crash_log` present, migration Complete, web + worker 1/1. |
| **Golden paths: the scaffolder never generated them** | Root cause in `apps/console/app/server/scaffolder.ts`: `templatePath` defaulted to `templates/${templateId}` for **any** templateId, so a golden path (`golden-microservice`, …) was treated as a stored Backstage skeleton. Two failures at once — the skeleton render 404'd (golden paths are *generated*, not stored) **and** the generator was skipped, because it was gated on `!isBackstage`. Proven live before the fix: the scaffolded repo contained only `catalog-info.yaml`, an Argo CD Application was still created pointing at a `deploy/` that did not exist (sync `Unknown`), and the response reported `ok: true`. Extracted the decision into a pure `resolveTemplateSource()`, made a declared golden-path family win unless a Backstage source is named explicitly, and made the deploy/ stub decision evidence-based (`committedPaths`) instead of trusting intent. 5 tests, with a **negative control**: the key test fails against the original logic. |
| **The ArgoCD-Application claim was wrong** | My earlier "scaffold opens no Argo CD Application" was an artifact of testing with a templateId that does not exist (`go-web-service`; the real one is `go-rest-service`). A live golden-path run reports `gitops-app OK — adhar-system/gp-verify-ms`. The Application *is* created — it just pointed at an empty path. Impersonation RBAC was also fine: an earlier `kubectl auth can-i impersonate users` "no" was a false negative, resolving `users` to DigitalOcean Crossplane CRDs rather than the core group (a SubjectAccessReview against group `""` returns allowed). |
| **Onboarding's five bogus config errors** | `apps/console/app/server/appset.ts` now resolves a console-facing name before looking for it: an alias (`argo-rollouts` → the `argo-rollout` package), a sub-component (`grafana`, `prometheus`, `alertmanager` → `kube-prometheus`), or **always-installed** (`gitea`, `argocd`, `cilium`, `crossplane` — the bootstrap components; ArgoCD cannot un-deploy itself), which returns an honest "always installed, cannot be toggled" instead of a 404. And "the file has no such element" no longer reports the same message as "the file is missing" — the old text told operators to set `ADHAR_APPSET_REPO`/`FILE` that were already correct. Every mapping target was checked to be a real package **and** a live ApplicationSet element. 5 tests. |
| **Grafana had no usable Loki datasource** | Not a datasource problem at all — and my first diagnosis was wrong twice. The `Loki` row existed and was typed correctly, but the `loki` **plugin** was absent, so every query failed with one server-side line: `Could not find plugin definition for data source datasource_type=loki`. I first assumed Grafana 13 had extracted Loki from core and added it to the preinstall list — **that crash-looped Grafana**: `loki` ships INSIDE the image, so installing it tries to replace a directory on the distroless read-only filesystem (`unlinkat /usr/share/grafana/data/plugins-bundled/loki: read-only file system`), and the installer is a startup module, so every dependent service fails with it. The old ReplicaSet kept serving, so there was no outage. Real cause, from `/api/admin/settings`: `plugins.preinstall_auto_update = true` made Grafana try to upgrade the **bundled** Loki 13.1.0 → 13.2.0, which killed the working plugin process and then failed to replace it. Fix is `GF_PLUGINS_PREINSTALL_AUTO_UPDATE=false`. `preinstall_disabled=true` also cures Loki but is a master switch that silently removed every panel and Drilldown plugin including Logs Drilldown itself — verified and rejected. |
| **Four LGTM datasources were declared twice, with divergent config** | Loki, Tempo, Mimir and Pyroscope were in **both** `extra-datasources.yaml` and the chart's `additionalDataSources`, same uids but different bodies, so whichever the sidecar loaded last silently won. They had genuinely diverged: the chart copy pointed Mimir at `mimir-query-frontend:8080`, which answers **401** (it needs the `X-Scope-OrgID` tenant header), while the hand-written copy uses `mimir-gateway/prometheus`, which answers **200** — both verified in-cluster. So Mimir worked or didn't depending on file ordering. Removed the chart copy; `extra-datasources.yaml` is now the single source. Verified: exactly one declaration each, all 227 package documents still parse. |
| **"Log volume has not been configured" — Loki was dead, not misconfigured** | `limits_config.volume_enabled: true` was set correctly in values, in the rendered manifest, and in the live ConfigMap the whole time. `loki-0` was in CrashLoopBackOff with 29 restarts: its 10Gi volume was full, the index-shipper cache write hit ENOSPC, and the store module failed to initialise — `err="short write"` / `error initialising module: store`. A dead backend presenting as two *configuration* messages in Grafana is what made this easy to chase in the wrong direction. Volume expanded 10Gi → 50Gi (`do-block-storage` allows expansion, and growing costs storage but **not** another attachment slot). Loki restarted with **0 restarts**, and `/loki/api/v1/index/volume` now returns real data — 725 MB for `adhar-system` in the last hour. Sized in the repo too, with a note to keep it in proportion to `retention_period` (720h) and `ingestion_rate_mb` (32). |
| **`grafana-piechart-panel` cannot load at all (pre-existing)** | It downloads fine and is then refused: `Refusing to initialize plugin because it's using Angular, which has been disabled`. Grafana 13 dropped Angular. The values comment still calls it REQUIRED for the bundled Keycloak dashboard, which means those panels render broken and no plugin list will fix it — the dashboard needs migrating to the core `piechart` panel. Unrelated to any change here; listed so it is not rediscovered. |
| **Phase 3: 8 of 10 items verified green** | Platform auth CLI (full lifecycle, `aud: kubernetes`, clean stdout/stderr), policy packs (15/15 Ready, 1036 audit results), tenant isolation, agentic platform (27 MCP tools over real HTTP, 5-step tool-calling runs, RAG citations, enforced budgets, billed OpenRouter completions), AI data plane, agent runtime, grounding & governance. |
| **Five GCP provider defects found and fixed while preparing a real deployment (2026-09-19).** The internal firewall rule hardcoded `10.0.0.0/24`, so any cluster on a different `subnet_cidr` had node-to-node traffic dropped; rules targeted the whole network while instances carried a per-instance tag, so the rules matched nothing; there were no rules for 80/443 or Google's load balancer health-check ranges, so the Gateway's LoadBalancer could never pass health checks; `disk_type`/`disk_size_gb` were parsed, defaulted and then ignored (every node got 50 GB pd-standard); and `clusterConfig` keys matched only camelCase, so the snake_case spellings in the docs and test fixtures were silently dropped and the worker count fell back to its default. Teardown also aborted outright when the local state file was missing, and never reported the CCM load balancers or CSI disks it cannot see. All fixed with tests; the GCP path remains **not live-verified** pending the deployment. |
